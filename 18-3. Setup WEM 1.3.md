# WEM 1.3 설치 및 구성 가이드

이 문서는 WarehousePG Enterprise Manager 1.3.0 RC2 기준 신규 설치 및 Safeguard 구성 절차입니다.

> WEM 1.3.0 RC2는 정식 릴리즈 전 버전입니다. 정식 릴리즈 RPM에서는 패키지 구성, 서비스명, 설정 항목이 달라질 수 있으므로 최종 문서 작성 전 재검증이 필요합니다.

## 0. 환경

| 항목 | 구성 |
| --- | --- |
| OS | Rocky Linux 9.5 |
| WEM Host | scdw / 192.168.10.75 |
| WHPG Primary Coordinator | cdw / 192.168.10.74 |
| WHPG Standby Coordinator | scdw / 192.168.10.75 |
| Segment Host | sdw1, sdw2, sdw3, sdw4 |
| WEM DB | PostgreSQL 13, scdw local, port 5433 |
| Prometheus | scdw, port 9090 |
| Loki | scdw, port 3100 |
| WEM UI | scdw, port 8080 |
| WEM Remote Write Receiver | scdw, port 9091 |

```text
                           WEM / Observability
                         scdw / 192.168.10.75
          ┌─────────────────────────────────────────────────┐
          │ WEM 1.3 RC2                                     │
          │ PostgreSQL for WEM DB : 127.0.0.1:5433          │
          │ Prometheus             : 192.168.10.75:9090     │
          │ Loki                   : 192.168.10.75:3100     │
          │ WEM UI                 : 192.168.10.75:8080     │
          │ Remote Write Receiver  : 192.168.10.75:9091     │
          └─────────────────────────────────────────────────┘

                         WarehousePG Cluster
          ┌────────────┬────────────┬────────────┬────────────┐
          │ cdw        │ scdw       │ sdw1~sdw4  │            │
          │ Primary    │ Standby    │ Segments   │            │
          └────────────┴────────────┴────────────┴────────────┘
```

## 1. 설치 파일

테스트에 사용한 WEM 1.3 RC2 RPM은 다음과 같습니다.

```text
whpg-enterprise-manager-1.3.0~rc.2-1.el9.x86_64.rpm
edb-whpg-observability-collector-1.4.0.rc.1-1.el9.x86_64.rpm
edb-acp-host-agent-1.0.0_rc.2-1.el9.x86_64.rpm
```

RC2 패키지에는 Temporal과 Alertmanager가 `whpg-enterprise-manager` RPM에 포함되어 있습니다.

따라서 아래 RPM은 WEM 1.3 RC2 신규 설치 시 별도로 설치하지 않습니다.

```text
edb-alertmanager-0.33.1-1edb1.x86_64.rpm
edb-temporal-1.31.2-1edb1.x86_64.rpm
```

`whpg-enterprise-manager-1.3.0~rc.2`는 `edb-alertmanager`, `edb-temporal` 별도 RPM과 충돌합니다.


## 2. Prometheus 설치

`scdw`에서 실행합니다.

```bash
cd /tmp

curl -LO https://github.com/prometheus/prometheus/releases/download/v3.5.0/prometheus-3.5.0.linux-amd64.tar.gz
tar -xzf prometheus-3.5.0.linux-amd64.tar.gz

sudo useradd --no-create-home --shell /sbin/nologin prometheus || true
sudo mkdir -p /etc/prometheus /var/lib/prometheus /etc/wem/prometheus/alerts
sudo cp prometheus-3.5.0.linux-amd64/prometheus /usr/local/bin/
sudo cp prometheus-3.5.0.linux-amd64/promtool /usr/local/bin/
sudo chown -R prometheus:prometheus /etc/prometheus /var/lib/prometheus /etc/wem/prometheus
```

Prometheus 설정 파일을 생성합니다.

```bash
sudo tee /etc/prometheus/prometheus.yml > /dev/null <<'EOF'
global:
  scrape_interval: 15s

# WEM RPM 설치 후 alert rule을 복사한 뒤 활성화합니다.
# rule_files:
#   - /etc/wem/prometheus/alerts/*.rules.yml

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: wem
    metrics_path: /prom/metrics
    static_configs:
      - targets: ["localhost:8080"]
EOF
```

systemd 서비스를 생성합니다.

```bash
sudo tee /etc/systemd/system/prometheus.service > /dev/null <<'EOF'
[Unit]
Description=Prometheus
After=network-online.target
Wants=network-online.target

[Service]
User=prometheus
Group=prometheus
Type=simple
ExecStart=/usr/local/bin/prometheus \
  --config.file=/etc/prometheus/prometheus.yml \
  --storage.tsdb.path=/var/lib/prometheus \
  --web.listen-address=0.0.0.0:9090 \
  --web.enable-remote-write-receiver

Restart=on-failure

[Install]
WantedBy=multi-user.target
EOF
```

기동 및 확인:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now prometheus
sudo systemctl status prometheus --no-pager
curl http://127.0.0.1:9090/-/healthy
```

정상 응답:

```text
Prometheus Server is Healthy.
```

## 3. Loki 설치

`scdw`에서 실행합니다.

```bash
cd /tmp

curl -LO https://github.com/grafana/loki/releases/download/v3.5.0/loki-linux-amd64.zip
sudo dnf install -y unzip || true
unzip loki-linux-amd64.zip

sudo useradd --no-create-home --shell /sbin/nologin loki || true
sudo mkdir -p /etc/loki /var/lib/loki
sudo install -m 0755 loki-linux-amd64 /usr/local/bin/loki
sudo chown -R loki:loki /etc/loki /var/lib/loki
```

Loki 설정 파일을 생성합니다.

```bash
sudo tee /etc/loki/loki-local-config.yaml > /dev/null <<'EOF'
auth_enabled: false

server:
  http_listen_address: 0.0.0.0
  http_listen_port: 3100

common:
  path_prefix: /var/lib/loki
  storage:
    filesystem:
      chunks_directory: /var/lib/loki/chunks
      rules_directory: /var/lib/loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  allow_structured_metadata: false
EOF
```

systemd 서비스를 생성합니다.

```bash
sudo tee /etc/systemd/system/loki.service > /dev/null <<'EOF'
[Unit]
Description=Loki
After=network-online.target
Wants=network-online.target

[Service]
User=loki
Group=loki
Type=simple
ExecStart=/usr/local/bin/loki -config.file=/etc/loki/loki-local-config.yaml
Restart=on-failure

[Install]
WantedBy=multi-user.target
EOF
```

기동 및 확인:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now loki
sudo systemctl status loki --no-pager
curl http://127.0.0.1:3100/ready
```

정상 응답:

```text
ready
```

## 4. WEM 1.3 RC2 설치

`scdw`에 RPM을 준비합니다.

```bash
sudo mkdir -p /tmp/wem13-rpms
ls -lh /tmp/wem13-rpms
```

WEM RPM만 설치합니다.

```bash
cd /tmp/wem13-rpms

sudo dnf install -y \
  ./whpg-enterprise-manager-1.3.0~rc.2-1.el9.x86_64.rpm
```

설치 후 확인:

```bash
rpm -qa | egrep 'whpg-enterprise-manager|edb-temporal|edb-alertmanager|temporal|alertmanager'
systemctl list-unit-files | egrep -i 'wem|temporal|alert'
rpm -ql whpg-enterprise-manager | egrep -i 'temporal|alert|prometheus|wem.conf' | head -200
```

WEM 1.3 RC2는 `wem.service`만 systemd unit으로 등록합니다. Temporal과 Alertmanager는 별도 systemd unit이 아니라 `wem.service`의 child process로 실행됩니다.

WEM 설치 후 Prometheus alert rule을 복사합니다.

```bash
sudo mkdir -p /etc/wem/prometheus/alerts

sudo cp -v /usr/local/greenplum-db/wem/config/prometheus/alerts/*.rules.yml \
  /etc/wem/prometheus/alerts/ 2>/dev/null || true

sudo chown -R prometheus:prometheus /etc/wem/prometheus
sudo systemctl restart prometheus
curl http://127.0.0.1:9090/-/healthy
```

## 5. WEM DB용 PostgreSQL 설치

`scdw`는 WHPG standby coordinator로 5432 포트를 사용 중일 수 있습니다. WEM application DB용 PostgreSQL은 5433 포트로 구성합니다.

`scdw`에서 실행합니다.

```bash
sudo dnf install -y postgresql-server postgresql-contrib
sudo postgresql-setup --initdb
```

서비스 시작 전에 PostgreSQL 포트를 변경합니다.

```bash
sudo vi /var/lib/pgsql/data/postgresql.conf
```

다음 값을 설정합니다.

```conf
listen_addresses = '127.0.0.1'
port = 5433
```

기동:

```bash
sudo systemctl enable --now postgresql
sudo systemctl status postgresql --no-pager
ss -lntp | egrep '5432|5433'
```

`pg_hba.conf`에서 localhost password 인증을 설정합니다.

```bash
sudo vi /var/lib/pgsql/data/pg_hba.conf
```

예시:

```conf
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256
```

PostgreSQL reload:

```bash
sudo systemctl reload postgresql
```

WEM DB user와 database를 생성합니다. `pg_hba.conf`가 `scram-sha-256`이면 비밀번호를 SCRAM secret으로 저장해야 합니다.

```bash
sudo -iu postgres psql -p 5433
```

```sql
SET password_encryption = 'scram-sha-256';
CREATE USER wem_admin WITH PASSWORD '<wem_admin_password>';
CREATE DATABASE wem OWNER wem_admin;
GRANT ALL PRIVILEGES ON DATABASE wem TO wem_admin;
\q
```

접속 확인:

```bash
PGPASSWORD='<wem_admin_password>' psql -h 127.0.0.1 -p 5433 -U wem_admin -d wem -c "select current_user, current_database();"
PGPASSWORD='<wem_admin_password>' psql -h 127.0.0.1 -p 5433 -U wem_admin -d postgres -c "select current_user, current_database();"
```

## 6. WEM 설정

`scdw`에서 `/etc/wem/wem.conf`를 수정합니다.

```bash
sudo cp -a /etc/wem/wem.conf /etc/wem/wem.conf.before-start.$(date +%Y%m%d%H%M%S)
sudo vi /etc/wem/wem.conf
```

예시:

```bash
# SECURITY
WEM_INSECURE_COOKIES=1
WEM_COOKIE_SECRET=<openssl rand -base64 32>
WEM_ADMIN_PASSWORD=<admin_password>

# Monitored WHPG Database
WHPG_HOST=192.168.10.74
WHPG_PORT=5432
WHPG_DATABASE=postgres
WHPG_USER=gpadmin
WHPG_PASSWORD=<gpadmin_db_password>

# WEM Application State Database
WEM_HOST=127.0.0.1
WEM_PORT=5433
WEM_DATABASE=wem
WEM_USER=wem_admin
WEM_PASSWORD=<wem_admin_password>

# Web
PORT=8080
WEM_AGENT_PORT=8081
HIGH_LOAD_MODE=true

# Observability
PROMETHEUS_URL=http://192.168.10.75:9090
LOKI_URL=http://192.168.10.75:3100
METRICS_BACKEND=prometheus

# Remote Write Receiver
WEM_REMOTE_WRITE_RECEIVER_ENABLED=true
WEM_REMOTE_WRITE_RECEIVER_PORT=9091
WEM_REMOTE_WRITE_RECEIVER_TTL=5m
WEM_REMOTE_WRITE_RECEIVER_TOKEN=<openssl rand -base64 32>

# Agent callback
WEM_AGENT_CALLBACK_BASE_URL=http://192.168.10.75:8080
```

WEM 기동:

```bash
sudo systemctl reset-failed wem
sudo systemctl enable --now wem
sudo systemctl status wem --no-pager
```

확인:

```bash
ss -lntp | egrep '8080|8081|9091|9090|3100|5433'
curl http://127.0.0.1:9090/-/healthy
curl http://127.0.0.1:3100/ready
curl http://127.0.0.1:9091/-/healthy
```

WEM 1.3 RC2에서 host agent는 WEM internal job execution engine 포트로 접속합니다. 기본값은 `PORT + 1`입니다.

```text
WEM UI PORT     : 8080
WEM Agent PORT  : 8081
```

브라우저 접속:

```text
http://192.168.10.75:8080
```

## 7. Observability Collector 설치

collector RPM은 WHPG cluster 전체 노드에 설치합니다.

대상:

```text
cdw
scdw
sdw1
sdw2
sdw3
sdw4
```

host 파일:

```bash
cat > /tmp/all_hosts <<'EOF'
cdw
scdw
sdw1
sdw2
sdw3
sdw4
EOF
```

`gpscp`, `gpssh`가 없는 경우 `scp`, `ssh` 반복문을 사용합니다.

```bash
for h in $(cat /tmp/all_hosts); do
  echo "==== $h ===="
  scp /tmp/wem13-rpms/edb-whpg-observability-collector-1.4.0.rc.1-1.el9.x86_64.rpm gpadmin@$h:/tmp/
done

for h in $(cat /tmp/all_hosts); do
  echo "==== $h ===="
  ssh gpadmin@$h "sudo dnf install -y /tmp/edb-whpg-observability-collector-1.4.0.rc.1-1.el9.x86_64.rpm"
done
```

설치 확인:

```bash
for h in $(cat /tmp/all_hosts); do
  echo "==== $h ===="
  ssh gpadmin@$h "rpm -qa | grep edb-whpg-observability-collector || true"
done
```

collector 설정은 WHPG primary coordinator인 `cdw`에서 수행합니다.

```bash
cd /var/lib/whpg-observability-collector
sudo cp -a collector.conf collector.conf.before-wem13.$(date +%Y%m%d%H%M%S)
sudo vi collector.conf
```

예시:

```bash
WHPG_OBS_DSN="host=192.168.10.74 port=5432 dbname=postgres user=gpadmin password=<gpadmin_db_password> sslmode=disable"

LOKI_ENDPOINT="http://192.168.10.75:3100/loki/api/v1/push"
PROMETHEUS_ENDPOINT="http://192.168.10.75:9091/api/v1/write"
PROMETHEUS_BEARER_TOKEN="<WEM_REMOTE_WRITE_RECEIVER_TOKEN>"
```

`WEM_REMOTE_WRITE_RECEIVER_TOKEN`은 `scdw`의 `/etc/wem/wem.conf`에서 확인합니다.

```bash
grep '^WEM_REMOTE_WRITE_RECEIVER_TOKEN=' /etc/wem/wem.conf
```

`deploy-observability`는 `sudo`로 실행하지 않고 `gpadmin`으로 실행합니다.

```bash
cd /var/lib/whpg-observability-collector
sudo chown -R gpadmin:gpadmin /var/lib/whpg-observability-collector
./deploy-observability
```

Alloy 상태 확인:

```bash
for h in cdw scdw sdw1 sdw2 sdw3 sdw4; do
  echo "==== $h ===="
  ssh gpadmin@$h "sudo systemctl is-active alloy"
done
```

## 8. WEM 설치 검증

`scdw`에서:

```bash
sudo systemctl is-active wem prometheus loki postgresql

curl http://127.0.0.1:9090/-/healthy
curl http://127.0.0.1:3100/ready
curl http://127.0.0.1:9091/-/healthy

ss -lntp | egrep '8080|8081|9091|9090|3100|5433'
```

`cdw`에서:

```bash
for h in cdw scdw sdw1 sdw2 sdw3 sdw4; do
  echo "==== $h ===="
  ssh gpadmin@$h "systemctl is-active alloy"
done
```

WEM UI:

```text
http://192.168.10.75:8080
```

확인 항목:

```text
admin 로그인 가능
Dashboard 표시
Prometheus target 확인
Loki ready 확인
Alloy active
WEM Remote Write Receiver healthy
```

## 9. Safeguard 구성

Safeguard는 WEM UI에서 WHPG DR 작업을 실행하기 위한 기능입니다.

구성 예시:

| 역할 | Host |
| --- | --- |
| WEM | scdw / 192.168.10.75 |
| Primary coordinator | cdw |
| DR coordinator | cdw-dr |
| DR segment host | sdw1-dr, sdw2-dr |
| Backup storage | /backup/whpg-dr |

### 9.1 acp-host-agent 설치

`edb-acp-host-agent`는 Safeguard 작업을 실제 coordinator 노드에서 실행하는 agent입니다.

설치 대상:

```text
cdw
cdw-dr
```

`scdw`는 WEM 서버이므로 DR 작업 실행 대상이 아니면 Safeguard용 host agent 설치가 필요하지 않습니다.

RPM 복사:

```bash
scp /tmp/wem13-rpms/edb-acp-host-agent-1.0.0_rc.2-1.el9.x86_64.rpm gpadmin@cdw:/tmp/
scp /tmp/wem13-rpms/edb-acp-host-agent-1.0.0_rc.2-1.el9.x86_64.rpm gpadmin@cdw-dr:/tmp/
```

설치:

```bash
ssh gpadmin@cdw "sudo dnf install -y /tmp/edb-acp-host-agent-1.0.0_rc.2-1.el9.x86_64.rpm"
ssh gpadmin@cdw-dr "sudo dnf install -y /tmp/edb-acp-host-agent-1.0.0_rc.2-1.el9.x86_64.rpm"
```

### 9.2 Primary agent 설정

`cdw`에서 `/etc/edb/acp-host-agent/acp-host-agent.conf`를 수정합니다.

```bash
sudo vi /etc/edb/acp-host-agent/acp-host-agent.conf
```

예시:

```bash
WEM_CONNECT_ADDRESS=192.168.10.75:8081
WEM_TASK_QUEUES=coordinator-tasks
WEM_DATABASE=postgres
AGENT_LOG_LEVEL=info
```

### 9.3 DR agent 설정

`cdw-dr`에서 `/etc/edb/acp-host-agent/acp-host-agent.conf`를 수정합니다.

```bash
sudo vi /etc/edb/acp-host-agent/acp-host-agent.conf
```

예시:

```bash
WEM_CONNECT_ADDRESS=192.168.10.75:8081
WEM_TASK_QUEUES=dr-standby-tasks
WEM_DATABASE=postgres
AGENT_LOG_LEVEL=info
```


agent 기동:

```bash
sudo systemctl enable --now acp-host-agent
sudo systemctl status acp-host-agent --no-pager
sudo tail -n 100 /var/log/edb/acp-host-agent/acp-host-agent.log
```

정상 로그 예시:

```text
worker running task_queue=coordinator-tasks
worker running task_queue=dr-standby-tasks
```

### 9.4 acp-host-agent 권한

Safeguard 작업 중 agent가 `sudo`, gpadmin home, `.whpg-dr` 디렉토리, YAML config 파일에 접근해야 합니다.

테스트 환경에서는 `acp-host-agent`에 passwordless sudo를 부여합니다.

`cdw`, `cdw-dr` 모두:

```bash
echo 'acp-host-agent ALL=(ALL) NOPASSWD: ALL' | sudo tee /etc/sudoers.d/acp-host-agent
sudo chmod 440 /etc/sudoers.d/acp-host-agent
sudo visudo -cf /etc/sudoers.d/acp-host-agent
sudo -u acp-host-agent sudo -n true && echo sudo-ok
```

`gpadmin` home directory가 `/dbaashome/gpadmin`처럼 기본 `/home/gpadmin`이 아닌 경우 `acp-host-agent`가 파일을 읽지 못할 수 있습니다.

예시:

```text
/dbaashome/gpadmin = drwxr-x---
```

이 경우 `acp-host-agent`를 `gpadmin` 그룹에 추가합니다.

```bash
sudo usermod -aG gpadmin acp-host-agent
sudo systemctl restart acp-host-agent
id acp-host-agent
```

config 파일 읽기 확인:

```bash
sudo -u acp-host-agent test -r /dbaashome/gpadmin/backup_nfs2.yaml && echo readable || echo not-readable
sudo -u acp-host-agent test -r /home/gpadmin/restore_nfs2.yaml && echo readable || echo not-readable
```

`.whpg-dr` 작업 디렉토리는 `gpadmin`과 `acp-host-agent`가 모두 읽고 쓸 수 있어야 합니다.

```bash
sudo setfacl -R -m u:gpadmin:rwx,u:acp-host-agent:rwx /dbaashome/gpadmin/.whpg-dr
sudo setfacl -R -d -m u:gpadmin:rwx,u:acp-host-agent:rwx /dbaashome/gpadmin/.whpg-dr
```

DR 노드가 `/home/gpadmin`을 사용하면:

```bash
sudo setfacl -R -m u:gpadmin:rwx,u:acp-host-agent:rwx /home/gpadmin/.whpg-dr
sudo setfacl -R -d -m u:gpadmin:rwx,u:acp-host-agent:rwx /home/gpadmin/.whpg-dr
```

### 9.5 백업 YAML

Primary coordinator인 `cdw`에 backup config YAML을 둡니다.

예시: `/dbaashome/gpadmin/backup_nfs2.yaml`

```yaml
cluster_name: whpg_cluster2

storage:
  type: posix
  prefix: whpg_cluster2
  path: /backup/whpg-dr

barman_options:
  compression: zstd
  compression_level: medium
  retention_policy: RECOVERY WINDOW OF 4 WEEKS
  jobs: 1
  max_archive_size: 10Gi
```

UI에서 YAML을 사용하지 않고 수동 입력하는 경우에는 Safeguard > Configuration의 `Global Settings` 영역에 다음 값을 입력합니다.

```text
General
  Cluster Name              : whpg_cluster2
  Checkpoint Interval (min) : 60

Storage
  Type                      : Filesystem / NFS
  Path                      : /backup/whpg-dr

Barman & Retention
  Compression               : zstd
  Compression Level         : 3
  Network Compression       : On
  WAL Prefetch              : -
  Retention Policy          : Keep All
```

> UI 수동 입력 화면에 `storage.prefix` 또는 `max_archive_size` 입력 항목이 없는 경우에는 YAML 방식에서만 해당 값을 명시합니다. 수동 입력 후에는 Deploy Primary 결과로 생성된 `backup/config.yaml`에서 실제 생성 값을 확인합니다.

Primary topology는 Discover Primary Cluster로 자동 조회한 뒤, coordinator data directory가 실제 경로인지 확인합니다.

```text
Coordinator Host       : cdw
Coordinator Data Dir   : /data1/coordinator/gpseg-1
Segment Hosts          : sdw1,sdw2,sdw3,sdw4
```

`Coordinator Data Dir`는 반드시 `gpseg-1`까지 포함한 full path를 입력합니다.

### 9.6 Restore YAML

DR coordinator인 `cdw-dr`에 restore config YAML을 둡니다.

예시: `/home/gpadmin/restore_nfs2.yaml`

```yaml
source_cluster_name: whpg_cluster2

storage:
  type: posix
  path: /backup/whpg-dr
  prefix: whpg_cluster2

coordinator_host: cdw-dr
coordinator_data_directory: /data/coordinator

segment_hosts:
- sdw1-dr
- sdw2-dr

data_directory: /data/primary
data_directory_prefix: gpseg

barman_options:
  compression: zstd
  compression_level: medium
  jobs: 1
  wal_prefetch: 1
```

UI에서 DR topology를 수동 입력하는 경우:

```text
DR Coordinator Host       : cdw-dr
Coordinator Data Dir      : /data/coordinator/gpseg-1
Segment Hosts             : sdw1-dr,sdw2-dr
DR Data Directory         : /data/primary
DR Data Directory Prefix  : gpseg
```

### 9.7 WEM UI에서 Safeguard 배포

WEM UI에서 Safeguard > Configuration으로 이동합니다.

Pre-created YAML mode를 사용하는 경우:

```text
Cluster Name                 : whpg_cluster2
Backup Config on Primary     : /dbaashome/gpadmin/backup_nfs2.yaml
Restore Config on DR         : /home/gpadmin/restore_nfs2.yaml
```

UI에서 값을 직접 입력하는 경우:

```text
Global Settings
  Cluster Name              : whpg_cluster2
  Checkpoint Interval (min) : 60
  Type                      : Filesystem / NFS
  Path                      : /backup/whpg-dr
  Compression               : zstd
  Compression Level         : 3
  Network Compression       : On
  Retention Policy          : Keep All

Primary
  Discover Primary Cluster  : Click
  Coordinator Host          : cdw
  Coordinator Data Dir      : /data1/coordinator/gpseg-1
  Segment Hosts             : sdw4,sdw1,sdw2,sdw3

DR Standby
  DR Coordinator Host       : cdw-dr
  Mirror Primary Topology   : No - Enter manually
  Coordinator Data Dir      : /data/coordinator/gpseg-1
  Segment Hosts             : sdw1-dr,sdw2-dr
  DR Data Directory         : /data/primary
  DR Data Directory Prefix  : gpseg
```

Primary topology는 반드시 실제 coordinator data directory full path를 사용합니다.

```text
/data1/coordinator/gpseg-1
```

`/data1/coordinator`처럼 상위 디렉토리만 입력하면 `gpstop -ar` 실행 시 다음 오류가 발생할 수 있습니다.

```text
No such file or directory: /data1/coordinator/postgresql.conf
```

Deploy Primary 후에는 다음 파일이 생성되어야 합니다.

```text
/dbaashome/gpadmin/.whpg-dr/whpg_cluster2/backup/config.yaml
```

Deploy DR 후에는 다음 파일이 생성되어야 합니다.

```text
/home/gpadmin/.whpg-dr/whpg_cluster2/restore/config.yaml
```

파일이 생성되지 않으면 먼저 UI 입력값의 오타를 확인합니다. 특히 `Cluster Name`, config file path, DR coordinator hostname이 실제 값과 일치해야 합니다.

CLI로도 동일하게 확인할 수 있습니다.

```bash
ssh gpadmin@cdw "test -f /dbaashome/gpadmin/.whpg-dr/whpg_cluster2/backup/config.yaml && echo backup-config-ok"
ssh gpadmin@cdw-dr "test -f /home/gpadmin/.whpg-dr/whpg_cluster2/restore/config.yaml && echo restore-config-ok"
```

## 10. Safeguard 검증

### 10.1 Backup 생성

WEM UI에서 Safeguard > Backups > Create Backup을 실행합니다.

CLI 확인:

```bash
/usr/edb/whpg-dr/bin/barman \
  -c /dbaashome/gpadmin/.whpg-dr/whpg_cluster2/backup/barman_conf/whpg_cluster2-global.conf \
  list-backup whpg_cluster2-seg-1
```

WAL archive 상태:

```bash
psql -h cdw -p 5432 -U gpadmin -d postgres -c "select * from pg_stat_archiver;"
```

### 10.2 Restore point 생성

WEM UI에서 Create Restore Point를 실행합니다.

RC2에서는 WEM API timeout으로 UI에 실패가 표시되더라도 실제 restore point 파일이 생성될 수 있습니다.

확인:

```bash
find /backup/whpg-dr/whpg_cluster2/restore-points -type f | tail -50
cat /backup/whpg-dr/whpg_cluster2/restore-points/latest.txt
```

예시:

```text
/backup/whpg-dr/whpg_cluster2/restore-points/test1/20260806-110359R_wem_2026-08-06T02-03-56Z
```


## 11. 로그 확인

WEM 로그:

```bash
sudo journalctl -u wem -n 300 --no-pager -l
sudo tail -f /var/log/wem/wem.log
```

Primary agent 로그:

```bash
sudo tail -f /var/log/edb/acp-host-agent/acp-host-agent.log
```

DR agent 로그:

```bash
sudo tail -f /var/log/edb/acp-host-agent/acp-host-agent.log
```

whpg-dr 로그:

```bash
tail -f /dbaashome/gpadmin/gpAdminLogs/whpg_dr-$(date +%Y-%m-%d).log
tail -f /home/gpadmin/gpAdminLogs/whpg_dr-$(date +%Y-%m-%d).log
```

WHPG coordinator 로그:

```bash
tail -f /data1/coordinator/gpseg-1/log/*.csv
```

## 12. WEM 1.3 주의사항

### 12.1 WEM DB는 WHPG와 분리 권장

WEM application state DB는 모니터링 대상 WHPG와 분리된 PostgreSQL에 저장하는 것을 권장합니다.

`scdw`가 WHPG standby coordinator로 5432를 사용 중이면 WEM DB용 PostgreSQL은 5433 등 별도 포트를 사용합니다.

### 12.2 gpadmin home directory

`gpadmin` home이 `/dbaashome/gpadmin`처럼 기본 `/home/gpadmin`이 아닌 경우, `acp-host-agent`가 파일을 읽지 못할 수 있습니다.

필요 조치:

```text
acp-host-agent를 gpadmin 그룹에 추가
.whpg-dr 디렉토리에 ACL 설정
config YAML 읽기 가능 여부 확인
```

### 12.3 Deploy Primary가 cluster restart 수행

Safeguard Deploy Primary는 archive 설정 적용을 위해 `gpconfig`와 `gpstop -ar`를 실행할 수 있습니다. cluster 상태가 정상인지 먼저 확인합니다.

```bash
gpstate
psql -h cdw -p 5432 -U gpadmin -d postgres -c "select now();"
```

### 12.4 Deploy 결과와 config 파일 확인

Deploy Primary / Deploy DR 후에는 다음 config 파일이 생성되었는지 확인합니다.

```bash
ssh gpadmin@cdw "ls -l /dbaashome/gpadmin/.whpg-dr/whpg_cluster2/backup/config.yaml"
ssh gpadmin@cdw-dr "ls -l /home/gpadmin/.whpg-dr/whpg_cluster2/restore/config.yaml"
```

파일이 없으면 UI 입력값의 오타, cluster name 불일치, agent의 파일 접근 권한을 먼저 확인합니다.

### 12.5 logscan 기본 DB

`acp-host-agent.conf`에서 `WEM_DATABASE`를 지정하지 않으면 기본값 `wem`으로 psql 접속을 시도할 수 있습니다. WHPG에 `wem` DB가 없으면 다음 오류가 발생합니다.

```text
FATAL: database "wem" does not exist
```

이 경우 `cdw`, `cdw-dr` agent 설정에 다음을 추가합니다.

```bash
WEM_DATABASE=postgres
```
