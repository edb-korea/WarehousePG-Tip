# edb-whpg7-pg_partman 설치 및 사용 예시

`pg_partman`은 WarehousePG의 Native Declarative Partition 기능을 기반으로 파티션 생성, 사전 생성(premake), 유지보수, 보관 주기(retention)를 자동화하는 확장입니다.

WHPG 7용 패키지는 `edb-whpg7-pg_partman`으로 제공됩니다. 이 문서는 WarehousePG에서 `edb-whpg7-pg_partman`을 설치하고 사용하는 절차를 기준으로 정리합니다.

참고 문서

- EDB WarehousePG whpg-partman: https://www.enterprisedb.com/docs/warehousepg/latest/ref_guide/modules/extensions/whpg-partman/
- EDB WarehousePG Extensions: https://www.enterprisedb.com/docs/warehousepg/latest/ref_guide/modules/extensions/
- WarehousePG CREATE EXTENSION: https://warehouse-pg.io/docs/7x/ref_guide/sql_commands/CREATE_EXTENSION.html

---

## 1. 개요

### 주요 기능

| 기능 | 설명 |
|---|---|
| 시간 기반 파티션 | timestamp/date 컬럼 기준으로 일/월/년/사용자 정의 interval 파티션 관리 |
| 숫자 기반 파티션 | bigint/int 컬럼 기준으로 range 파티션 관리 |
| 사전 파티션 생성 | 현재 시점 또는 현재 ID 기준으로 미래 파티션을 미리 생성 |
| 유지보수 자동화 | `run_maintenance()` 또는 `run_maintenance_proc()`로 신규 파티션 생성 및 retention 수행 |
| retention | 오래된 child partition detach/drop/move 처리 |
| 기존 데이터 이관 | default partition 또는 source table의 데이터를 child partition으로 배치 이관 |

### 주의 사항

- WHPG 7 Native Partition 테이블을 대상으로 사용합니다.
- `p_type` 값은 `native`만 지원합니다.
- `create_parent()` 호출 전에 parent table은 미리 `PARTITION BY RANGE`로 생성되어 있어야 합니다.
- parent table 이름은 반드시 schema-qualified 형식으로 지정합니다. 예: `public.sales`
- `p_interval`은 text로 입력합니다. 예: `'1 month'`, `'1 day'`, `'1000000'`
- extension은 사용할 데이터베이스마다 `CREATE EXTENSION`으로 등록해야 합니다.
- extension 패키지 파일은 모든 WHPG host에 동일하게 설치되어 있어야 합니다.

### 제한 사항

- WHPG의 `whpg-partman`은 native partitioning만 지원합니다. `create_parent()` 호출 시 `p_type := 'native'`를 사용합니다.
- legacy trigger-based mode인 `p_type := 'partman'`은 WarehousePG에서 지원하지 않습니다. segment(QE) slice에서 non-SELECT 문을 실행할 수 없어 오류가 발생할 수 있습니다.
- native mode에서는 parent table을 먼저 `PARTITION BY RANGE`로 선언해야 합니다. `create_parent()`가 parent partition 구조를 대신 만들어주지 않습니다.

---

## 2. 패키지 설치

### 2.1 설치 원칙

WHPG extension은 coordinator, standby coordinator, segment, mirror host가 같은 경로의 extension 파일을 볼 수 있어야 합니다. 따라서 `edb-whpg7-pg_partman` 패키지는 WHPG cluster의 모든 host에 설치합니다.

공식 문서 기준 흐름:

1. coordinator host에서 EDB repository 설정
2. coordinator에서 RPM 다운로드
3. `all_hosts` 파일에 전체 WHPG host 목록 작성
4. `scp`로 RPM을 모든 host에 복사
5. `gpssh`로 모든 host에 패키지 설치
6. DB별로 `CREATE EXTENSION` 실행

### 2.2 EDB repository에서 패키지 다운로드

coordinator에서 수행합니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh

export EDB_SUBSCRIPTION_TOKEN=<your-token>
export EDB_REPO=gpsupp

curl -1sSLf "https://downloads.enterprisedb.com/$EDB_SUBSCRIPTION_TOKEN/$EDB_REPO/setup.rpm.sh" | sudo -E bash
sudo dnf download edb-whpg7-pg_partman
```

변수 설명:

| 변수 | 설명 |
|---|---|
| `EDB_SUBSCRIPTION_TOKEN` | EDB subscription 등록 후 받은 token |
| `EDB_REPO` | WHPG extension 패키지를 받을 repository. 일반적으로 `gpsupp` 사용 |
| `edb-whpg7-pg_partman` | WHPG 7용 pg_partman 패키지명 |

RHEL/Rocky 8 환경에서 `dnf download`가 준비되어 있지 않거나 yum 계열 명령을 사용하는 경우:

```bash
sudo yumdownloader edb-whpg7-pg_partman
```

> `dnf download` 명령이 없으면 `dnf-plugins-core` 패키지가 필요할 수 있습니다.

### 2.3 all_hosts 파일 작성

coordinator, standby coordinator, 모든 segment host를 포함합니다.

```bash
cat > all_hosts <<'EOF'
cdw
scdw
sdw1
sdw2
sdw3
sdw4
EOF
```

현재 환경의 hostfile이 이미 있다면 그 파일을 그대로 사용해도 됩니다.

```bash
cat hostfile_exkeys
```

### 2.4 전체 host에 RPM 복사 및 설치

다운로드된 실제 RPM 파일명을 확인합니다.

```bash
ls -1 edb-whpg7-pg_partman*.rpm
```

모든 host로 복사:

```bash
for h in $(cat all_hosts); do
    scp edb-whpg7-pg_partman*.rpm ${h}:/tmp/
done
```

모든 host에서 설치:

```bash
gpssh -f all_hosts -e 'sudo dnf install -y /tmp/edb-whpg7-pg_partman*.rpm'
```

yum 기반:

```bash
gpssh -f all_hosts -e 'sudo yum install -y /tmp/edb-whpg7-pg_partman*.rpm'
```

`gpscp`가 제공되는 환경에서는 아래처럼 복사할 수도 있습니다.

```bash
gpscp -f all_hosts edb-whpg7-pg_partman*.rpm =:/tmp/
```

복사 확인:

```bash
gpssh -f all_hosts -e 'ls -1 /tmp/edb-whpg7-pg_partman*.rpm'
```

### 2.5 repository가 모든 host에 설정된 경우

모든 host에서 repository 접근이 가능하면 직접 설치해도 됩니다.

```bash
gpssh -f all_hosts -e 'sudo dnf install -y edb-whpg7-pg_partman'
```

단일 노드 테스트 환경:

```bash
sudo dnf install -y edb-whpg7-pg_partman
```

### 2.6 설치 확인

```bash
source /usr/edb/whpg7/greenplum_path.sh

gpssh -f all_hosts -e 'rpm -q edb-whpg7-pg_partman'

pg_config --sharedir
ls "$(pg_config --sharedir)"/extension/pg_partman*
```

SQL에서 확인:

```sql
SELECT name, default_version, installed_version, comment
FROM pg_available_extensions
WHERE name = 'pg_partman';
```

---

## 3. Extension 등록

패키지 설치는 OS/cluster 단위 작업이고, `CREATE EXTENSION`은 database 단위 작업입니다. `pg_partman`을 사용할 database마다 한 번씩 등록합니다.


```sql
CREATE SCHEMA IF NOT EXISTS partman;
CREATE EXTENSION IF NOT EXISTS pg_partman SCHEMA partman;
```

간단히 기본 schema에 등록할 수도 있지만, 운영에서는 `partman` 같은 전용 schema 사용을 권장합니다.

```sql
CREATE EXTENSION IF NOT EXISTS pg_partman;
```

설치 확인:

```sql
SELECT extname, extversion, nspname AS schema_name
FROM pg_extension e
JOIN pg_namespace n ON n.oid = e.extnamespace
WHERE extname = 'pg_partman';
```

사용 편의를 위해 `search_path`에 `partman`을 추가할 수 있습니다.

```sql
SET search_path = partman, public;
```

또는 함수 호출 시 schema를 명시합니다.

```sql
SELECT partman.run_maintenance();
```

psql에서 확인:

```sql
\dx pg_partman
\df partman.*
```

삭제는 database별로 수행합니다.

```sql
DROP EXTENSION pg_partman;
```

---

## 4. 기본 사용 흐름

1. Native Partition parent table 생성
2. `partman.create_parent()`로 pg_partman 관리 대상 등록
3. `partman.part_config`에서 premake, retention 등 옵션 조정
4. `partman.run_maintenance()` 수행
5. child partition 생성 상태 확인

### 4.1 자주 쓰는 함수

| 구분 | 함수/프로시저 | 용도 |
|---|---|---|
| 파티션 등록 | `create_parent()` | parent partition table을 pg_partman 관리 대상으로 등록 |
| 유지보수 | `run_maintenance()` | 신규 child partition 생성, retention 처리 |
| 유지보수 | `run_maintenance_proc()` | 여러 partition set 유지보수를 commit 단위로 나누어 수행 |
| 파티션 목록 | `show_partitions()` | pg_partman 관리 대상의 child partition 목록 확인 |
| 파티션명 예측 | `show_partition_name()` | 특정 값이 들어갈 child partition 이름/범위 확인 |
| default 확인 | `check_default()` | 관리 대상 default partition 데이터 유입 여부 확인 |
| 권한 재적용 | `reapply_privileges()` | parent의 grant/owner 기준으로 child 권한 재적용 |
| 파티션 해제 | `undo_partition()` | partition 구조를 해제하고 데이터를 일반 테이블로 이관 |

> 함수명은 WHPG 패키지 버전에 따라 일부 차이가 있을 수 있습니다. 운영 전 `\df partman.*` 또는 `pg_proc` 조회로 현재 설치된 패키지의 함수명을 확인하십시오.

### 4.2 create_parent 주요 파라미터

`create_parent()`는 이미 생성된 native partition parent table을 pg_partman 관리 대상으로 등록하고, 초기 child partition을 생성하는 함수입니다.

기본 형식:

```sql
SELECT partman.create_parent(
    p_parent_table := 'public.sales_daily',
    p_control      := 'sale_time',
    p_type         := 'native',
    p_interval     := '1 day',
    p_premake      := 7
);
```

| 파라미터 | 필수 | 설명 | 예시 |
|---|---|---|---|
| `p_parent_table` | 필수 | pg_partman이 관리할 parent table. schema-qualified 형식 권장 | `'public.sales_daily'` |
| `p_control` | 필수 | 파티션 기준 컬럼. parent table의 `PARTITION BY RANGE` 컬럼과 일치해야 함 | `'sale_time'`, `'event_id'` |
| `p_type` | 필수 | WHPG에서는 native partitioning만 지원 | `'native'` |
| `p_interval` | 필수 | child partition 하나의 범위. 시간 기반은 interval 문자열, ID 기반은 숫자 문자열 | `'1 day'`, `'1 month'`, `'1000000'` |
| `p_premake` | 선택 | maintenance 시 미리 만들어둘 partition 개수 | `7`, `14` |
| `p_start_partition` | 선택 | 초기 partition 생성 기준값을 명시하고 싶을 때 사용 | `'2026-08-01'` |
| `p_epoch` | 선택 | bigint/integer control 컬럼이 epoch time인 경우 단위 지정 | `'seconds'`, `'milliseconds'` |
| `p_template_table` | 선택 | 새 child partition에 적용할 template table을 직접 지정 | `'partman.template_public_sales_daily'` |

`p_premake` 동작 예시:

- `p_interval := '1 day'`, `p_premake := 7`이면 현재 날짜 기준으로 미래 partition이 부족하지 않도록 7개를 미리 유지합니다.
- 최초 `create_parent()` 실행 시에는 현재 기준 앞/뒤 partition이 함께 생성될 수 있습니다. 예를 들어 기준일이 `2026-08-11`이면 `2026-08-04`부터 `2026-08-18`까지 생성될 수 있습니다.
- 이후 운영 중에는 `run_maintenance()`가 실행될 때 미래 partition이 `premake` 개수만큼 유지되도록 보충합니다.

주의:

- parent table은 `create_parent()` 전에 반드시 `PARTITION BY RANGE`로 생성되어 있어야 합니다.
- WHPG에서는 `p_type := 'partman'` legacy trigger-based mode를 사용하지 않습니다.
- child partition 생성 시 `table doesn't have 'DISTRIBUTED BY' clause...` NOTICE가 출력될 수 있습니다. child partition 생성 과정에서 parent/LIKE table의 distribution 정보를 따르는 안내성 NOTICE이며, `create_parent`가 `t`를 반환하고 child partition이 정상 생성되면 성공으로 봅니다.

### 4.3 등록 직후 필수 확인

기본 확인 쿼리:

```sql
SELECT parent_table, control, partition_type, partition_interval, premake, retention
FROM partman.part_config
ORDER BY parent_table;

    parent_table    |  control  | partition_type | partition_interval | premake | retention 
--------------------+-----------+----------------+--------------------+---------+-----------
 public.sales_daily | sale_time | native         | 1 day              |       7 | 
```

child partition 확인:

```sql
SELECT
    child_ns.nspname || '.' || child.relname AS child_table,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent      ON parent.oid = i.inhparent
JOIN pg_class child       ON child.oid = i.inhrelid
JOIN pg_namespace p_ns    ON p_ns.oid = parent.relnamespace
JOIN pg_namespace child_ns ON child_ns.oid = child.relnamespace
WHERE p_ns.nspname || '.' || parent.relname = 'public.sales_daily'
ORDER BY child_table;

          child_table           |                         partition_boundary                         
--------------------------------+--------------------------------------------------------------------
 public.sales_daily_default     | DEFAULT
 public.sales_daily_p2026_08_04 | FOR VALUES FROM ('2026-08-04 00:00:00') TO ('2026-08-05 00:00:00')
 public.sales_daily_p2026_08_05 | FOR VALUES FROM ('2026-08-05 00:00:00') TO ('2026-08-06 00:00:00')
 public.sales_daily_p2026_08_06 | FOR VALUES FROM ('2026-08-06 00:00:00') TO ('2026-08-07 00:00:00')
 public.sales_daily_p2026_08_07 | FOR VALUES FROM ('2026-08-07 00:00:00') TO ('2026-08-08 00:00:00')
 public.sales_daily_p2026_08_08 | FOR VALUES FROM ('2026-08-08 00:00:00') TO ('2026-08-09 00:00:00')
 public.sales_daily_p2026_08_09 | FOR VALUES FROM ('2026-08-09 00:00:00') TO ('2026-08-10 00:00:00')
 public.sales_daily_p2026_08_10 | FOR VALUES FROM ('2026-08-10 00:00:00') TO ('2026-08-11 00:00:00')
 public.sales_daily_p2026_08_11 | FOR VALUES FROM ('2026-08-11 00:00:00') TO ('2026-08-12 00:00:00')
 public.sales_daily_p2026_08_12 | FOR VALUES FROM ('2026-08-12 00:00:00') TO ('2026-08-13 00:00:00')
 public.sales_daily_p2026_08_13 | FOR VALUES FROM ('2026-08-13 00:00:00') TO ('2026-08-14 00:00:00')
 public.sales_daily_p2026_08_14 | FOR VALUES FROM ('2026-08-14 00:00:00') TO ('2026-08-15 00:00:00')
 public.sales_daily_p2026_08_15 | FOR VALUES FROM ('2026-08-15 00:00:00') TO ('2026-08-16 00:00:00')
 public.sales_daily_p2026_08_16 | FOR VALUES FROM ('2026-08-16 00:00:00') TO ('2026-08-17 00:00:00')
 public.sales_daily_p2026_08_17 | FOR VALUES FROM ('2026-08-17 00:00:00') TO ('2026-08-18 00:00:00')
 public.sales_daily_p2026_08_18 | FOR VALUES FROM ('2026-08-18 00:00:00') TO ('2026-08-19 00:00:00')
```

pg_partman helper 함수로 확인:

```sql
SELECT * FROM partman.show_partitions('public.sales_daily');
```

특정 값이 어느 partition에 들어갈지 확인:

```sql
SELECT *
FROM partman.show_partition_name(
    p_parent_table := 'public.sales_daily',
    p_value        := now()::text
);

 partition_schema |     partition_table     |    suffix_timestamp    | suffix_id | table_exists 
------------------+-------------------------+------------------------+-----------+--------------
 public           | sales_daily_p2026_08_11 | 2026-08-11 00:00:00+09 |           | t
```

### 4.4 part_config 주요 옵션

`create_parent()` 후에는 `partman.part_config`에 관리 설정이 저장됩니다. 운영 중 변경은 대부분 이 테이블을 `UPDATE`한 뒤 `run_maintenance()`를 실행하는 방식입니다.

| 컬럼 | 설명 | 예시 |
|---|---|---|
| `premake` | 미래 partition을 몇 개 미리 만들지 지정 | `7`, `14` |
| `retention` | 오래된 partition 보관 기간 | `'3 months'`, `'30 days'`, `'10000000'` |
| `retention_keep_table` | retention 대상 partition table을 실제 drop하지 않고 남길지 여부 | `true`/`false` |
| `retention_schema` | retention 대상 partition을 삭제하지 않고 다른 schema로 이동 | `'archive'` |
| `automatic_maintenance` | 전체 `run_maintenance()` 호출 대상 포함 여부 | `'on'`/`'off'` |
| `ignore_default_data` | default partition 데이터가 있어도 maintenance 진행할지 여부 | `true`/`false` |
| `infinite_time_partitions` | 현재 데이터가 없어도 시간 기준 미래 partition 생성 | `true`/`false` |

설정 변경 예시:

```sql
UPDATE partman.part_config
SET premake = 14,
    automatic_maintenance = 'on'
WHERE parent_table = 'public.sales_daily';

SELECT partman.run_maintenance('public.sales_daily');

SELECT parent_table, control, partition_type, partition_interval, premake, retention
FROM partman.part_config
ORDER BY parent_table;
    parent_table    |  control  | partition_type | partition_interval | premake | retention 
--------------------+-----------+----------------+--------------------+---------+-----------
 public.sales_daily | sale_time | native         | 1 day              |      14 | 
```

---

## 5. 예시 1: 일 단위 시간 파티션

### 5.1 Parent table 생성

```sql
DROP TABLE IF EXISTS public.sales_daily CASCADE;

CREATE TABLE public.sales_daily (
    sale_id     bigint NOT NULL,
    sale_time   timestamp NOT NULL,
    customer_id bigint,
    amount      numeric(12,2),
    created_at  timestamp DEFAULT now()
)
DISTRIBUTED BY (sale_id)
PARTITION BY RANGE (sale_time);

DELETE FROM partman.part_config
WHERE parent_table = 'public.sales_daily';
```

### 5.2 pg_partman 등록

```sql
SELECT partman.create_parent(
    p_parent_table := 'public.sales_daily',
    p_control      := 'sale_time',
    p_type         := 'native',
    p_interval     := '1 day',
    p_premake      := 7
);
```

### 5.3 파티션 생성/확인

```sql
SELECT partman.run_maintenance('public.sales_daily');

SELECT
    child.relname AS partition_name,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent ON parent.oid = i.inhparent
JOIN pg_class child  ON child.oid = i.inhrelid
JOIN pg_namespace n  ON n.oid = parent.relnamespace
WHERE n.nspname = 'public'
  AND parent.relname = 'sales_daily'
ORDER BY child.relname;
```

### 5.4 데이터 입력

```sql
INSERT INTO public.sales_daily (sale_id, sale_time, customer_id, amount)
SELECT
    gs,
    now() - (gs % 10) * interval '1 day',
    10000 + gs,
    round((random() * 100000)::numeric, 2)
FROM generate_series(1, 10000) AS gs;
```

조회:

```sql
EXPLAIN
SELECT count(*), sum(amount)
FROM public.sales_daily
WHERE sale_time >= current_date - interval '1 day'
  AND sale_time <  current_date;

                                                              QUERY PLAN                                                              
--------------------------------------------------------------------------------------------------------------------------------------
 Finalize Aggregate  (cost=0.00..431.01 rows=1 width=16)
   ->  Gather Motion 16:1  (slice1; segments: 16)  (cost=0.00..431.01 rows=1 width=16)
         ->  Partial Aggregate  (cost=0.00..431.01 rows=1 width=16)
               ->  Dynamic Seq Scan on sales_daily  (cost=0.00..431.01 rows=25 width=16)
                     Number of partitions to scan: 1 (out of 16) <<< 16개의 파티션중 하나의 파티션만 확인
                     Filter: ((sale_time >= '2026-08-11 00:00:00'::timestamp without time zone) AND (sale_time < '2026-08-12'::date))
```

---

## 6. 예시 2: 월 단위 시간 파티션 + retention

### 6.1 Parent table 생성

```sql
DROP TABLE IF EXISTS public.order_monthly CASCADE;

CREATE TABLE public.order_monthly (
    order_id    bigint NOT NULL,
    order_date  date NOT NULL,
    region      text,
    amount      numeric(12,2)
)
DISTRIBUTED BY (order_id)
PARTITION BY RANGE (order_date);
```

### 6.2 pg_partman 등록

```sql
SELECT partman.create_parent(
    p_parent_table := 'public.order_monthly',
    p_control      := 'order_date',
    p_type         := 'native',
    p_interval     := '1 month',
    p_premake      := 3
);
```

### 6.3 retention 설정

최근 12개월만 유지하도록 설정합니다.

```sql
UPDATE partman.part_config
SET retention = '12 months',
    retention_keep_table = false
WHERE parent_table = 'public.order_monthly';
```

설정 확인:

```sql
SELECT parent_table, retention, retention_keep_table, retention_keep_index
FROM partman.part_config
WHERE parent_table = 'public.order_monthly';
     parent_table     | retention | retention_keep_table | retention_keep_index 
----------------------+-----------+----------------------+----------------------
 public.order_monthly | 12 months | f                    | t
```

유지보수 실행:

```sql
SELECT partman.run_maintenance('public.order_monthly');
```

> `retention_keep_table = true`이면 오래된 partition을 실제 drop하지 않고 detach 상태로 남기는 방식입니다. 운영에서 즉시 삭제가 부담스러우면 먼저 `true`로 검증한 뒤 `false`로 변경하는 것을 권장합니다.

---

## 7. 예시 3: ID range 파티션

ID 또는 sequence 값을 기준으로 일정 범위마다 파티션을 생성합니다.

### 7.1 Parent table 생성

```sql
DROP TABLE IF EXISTS public.event_by_id CASCADE;

CREATE TABLE public.event_by_id (
    event_id   bigint NOT NULL,
    event_time timestamp NOT NULL DEFAULT now(),
    payload    text
)
DISTRIBUTED BY (event_id)
PARTITION BY RANGE (event_id);
```

### 7.2 pg_partman 등록

100만 건 단위 파티션을 생성합니다.

```sql
SELECT partman.create_parent(
    p_parent_table := 'public.event_by_id',
    p_control      := 'event_id',
    p_type         := 'native',
    p_interval     := '1000000',
    p_premake      := 4
);
```

### 7.3 데이터 입력 및 유지보수

```sql
SELECT                                                        
    child.relname AS partition_name,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent ON parent.oid = i.inhparent
JOIN pg_class child  ON child.oid = i.inhrelid
JOIN pg_namespace n  ON n.oid = parent.relnamespace
WHERE n.nspname = 'public'
  AND parent.relname = 'event_by_id'
ORDER BY child.relname;
    partition_name    |             partition_boundary             
----------------------+--------------------------------------------
 event_by_id_default  | DEFAULT
 event_by_id_p0       | FOR VALUES FROM ('0') TO ('1000000')
 event_by_id_p1000000 | FOR VALUES FROM ('1000000') TO ('2000000')
 event_by_id_p2000000 | FOR VALUES FROM ('2000000') TO ('3000000')
 event_by_id_p3000000 | FOR VALUES FROM ('3000000') TO ('4000000')
 event_by_id_p4000000 | FOR VALUES FROM ('4000000') TO ('5000000')

INSERT INTO public.event_by_id (event_id, event_time, payload)
SELECT gs, now(), 'payload-' || gs
FROM generate_series(1, 2500000) AS gs;

SELECT partman.run_maintenance('public.event_by_id');

SELECT                                                        
    child.relname AS partition_name,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent ON parent.oid = i.inhparent
JOIN pg_class child  ON child.oid = i.inhrelid
JOIN pg_namespace n  ON n.oid = parent.relnamespace
WHERE n.nspname = 'public'
  AND parent.relname = 'event_by_id'
ORDER BY child.relname;
    partition_name    |             partition_boundary             
----------------------+--------------------------------------------
 event_by_id_default  | DEFAULT
 event_by_id_p0       | FOR VALUES FROM ('0') TO ('1000000')
 event_by_id_p1000000 | FOR VALUES FROM ('1000000') TO ('2000000')
 event_by_id_p2000000 | FOR VALUES FROM ('2000000') TO ('3000000')
 event_by_id_p3000000 | FOR VALUES FROM ('3000000') TO ('4000000')
 event_by_id_p4000000 | FOR VALUES FROM ('4000000') TO ('5000000')
 event_by_id_p5000000 | FOR VALUES FROM ('5000000') TO ('6000000')
 event_by_id_p6000000 | FOR VALUES FROM ('6000000') TO ('7000000')
```

확인:

```sql
SELECT
    child.relname AS partition_name,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent ON parent.oid = i.inhparent
JOIN pg_class child  ON child.oid = i.inhrelid
JOIN pg_namespace n  ON n.oid = parent.relnamespace
WHERE n.nspname = 'public'
  AND parent.relname = 'event_by_id'
ORDER BY child.relname;
```

---

## 8. 예시 4: Epoch 컬럼 기반 시간 파티션

control 컬럼은 bigint이지만 값이 epoch time인 경우 `p_epoch`을 사용합니다.

### 8.1 Parent table 생성

```sql
DROP TABLE IF EXISTS public.access_log_epoch CASCADE;

CREATE TABLE public.access_log_epoch (
    log_id       bigint NOT NULL,
    log_epoch_ms bigint NOT NULL,
    url          text,
    status_code  int
)
DISTRIBUTED BY (log_id)
PARTITION BY RANGE (log_epoch_ms);
```

### 8.2 pg_partman 등록

```sql
SELECT partman.create_parent(
    p_parent_table := 'public.access_log_epoch',
    p_control      := 'log_epoch_ms',
    p_type         := 'native',
    p_interval     := '1 day',
    p_premake      := 7,
    p_epoch        := 'milliseconds'
);
```

### 8.3 데이터 입력

```sql
INSERT INTO public.access_log_epoch (log_id, log_epoch_ms, url, status_code)
SELECT
    gs,
    floor(extract(epoch FROM now()) * 1000)::bigint,
    '/api/v1/orders/' || gs,
    200
FROM generate_series(1, 1000) AS gs;

SELECT partman.run_maintenance('public.access_log_epoch');

SELECT                                                        
    child.relname AS partition_name,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent ON parent.oid = i.inhparent
JOIN pg_class child  ON child.oid = i.inhrelid
JOIN pg_namespace n  ON n.oid = parent.relnamespace
WHERE n.nspname = 'public'
  AND parent.relname = 'access_log_epoch'
ORDER BY child.relname;
        partition_name        |                   partition_boundary                   
------------------------------+--------------------------------------------------------
 access_log_epoch_default     | DEFAULT
 access_log_epoch_p2026_08_05 | FOR VALUES FROM ('1785855600000') TO ('1785942000000')
 access_log_epoch_p2026_08_06 | FOR VALUES FROM ('1785942000000') TO ('1786028400000')
 access_log_epoch_p2026_08_07 | FOR VALUES FROM ('1786028400000') TO ('1786114800000')
 access_log_epoch_p2026_08_08 | FOR VALUES FROM ('1786114800000') TO ('1786201200000')
 access_log_epoch_p2026_08_09 | FOR VALUES FROM ('1786201200000') TO ('1786287600000')
 access_log_epoch_p2026_08_10 | FOR VALUES FROM ('1786287600000') TO ('1786374000000')
 access_log_epoch_p2026_08_11 | FOR VALUES FROM ('1786374000000') TO ('1786460400000')
 access_log_epoch_p2026_08_12 | FOR VALUES FROM ('1786460400000') TO ('1786546800000')
 access_log_epoch_p2026_08_13 | FOR VALUES FROM ('1786546800000') TO ('1786633200000')
 access_log_epoch_p2026_08_14 | FOR VALUES FROM ('1786633200000') TO ('1786719600000')
 access_log_epoch_p2026_08_15 | FOR VALUES FROM ('1786719600000') TO ('1786806000000')
 access_log_epoch_p2026_08_16 | FOR VALUES FROM ('1786806000000') TO ('1786892400000')
 access_log_epoch_p2026_08_17 | FOR VALUES FROM ('1786892400000') TO ('1786978800000')
 access_log_epoch_p2026_08_18 | FOR VALUES FROM ('1786978800000') TO ('1787065200000')
 access_log_epoch_p2026_08_19 | FOR VALUES FROM ('1787065200000') TO ('1787151600000')
```

---


## 9. Template table 사용

template table은 pg_partman이 새 child partition을 만들 때 참고하는 설계도 테이블입니다.

```text
parent table      = 사용자가 조회/적재하는 원본 partition table
child partition   = 실제 데이터가 날짜/범위별로 들어가는 하위 partition
template table    = 앞으로 만들 child partition의 구조를 참고하기 위한 설계도
```

예를 들어 pg_partman이 아래와 같은 새 partition을 자동 생성한다고 가정합니다.

```text
sales_daily_p2026_08_12
sales_daily_p2026_08_13
sales_daily_p2026_08_14
```

이때 새 partition에 어떤 index나 일부 속성을 적용할지 참고하는 기준이 template table입니다. 업무 데이터를 넣거나 조회하는 테이블이 아니라, 새 partition을 만들 때 참고하는 내부 기준 테이블로 이해하면 됩니다.

pg_partman은 parent에서 자동 상속되지 않는 일부 속성을 child partition에 적용하기 위해 template table을 사용합니다. `create_parent()` 호출 시 `p_template_table`을 지정하지 않으면 pg_partman이 자동으로 template table을 생성합니다.

### 9.1 template table 확인

```sql
SELECT parent_table, template_table
FROM partman.part_config
WHERE parent_table = 'public.sales_daily';
```

### 9.2 template table에 index 추가

새로 생성되는 child partition에 index를 자동 적용하고 싶을 때 template table에 index를 추가합니다.

```sql
-- 실제 template_table 이름으로 변경
CREATE INDEX ON partman.template_public_sales_daily (customer_id);
```

### 9.3 parent 권한을 child에 다시 적용

parent table의 권한을 변경한 뒤 기존 child partition에도 동일하게 맞추고 싶을 때 사용합니다.

```sql
GRANT SELECT ON public.sales_daily TO readonly_user;

SELECT partman.reapply_privileges('public.sales_daily');
```

---

## 10. 기존 테이블 데이터 파티션 이관

이미 데이터가 있는 일반 테이블을 바로 parent partition table로 변경하는 것은 운영 영향이 큽니다. 일반적으로 신규 partition table을 만들고 데이터를 배치 이관합니다.

### 10.1 기존 테이블 예시

```sql
DROP TABLE IF EXISTS public.sales_old;

CREATE TABLE public.sales_old (
    sale_id     bigint NOT NULL,
    sale_time   timestamp NOT NULL,
    customer_id bigint,
    amount      numeric(12,2)
)
DISTRIBUTED BY (sale_id);

INSERT INTO public.sales_old
SELECT
    gs,
    now() - (gs % 90) * interval '1 day',
    10000 + gs,
    round((random() * 100000)::numeric, 2)
FROM generate_series(1, 100000) AS gs;
```

### 10.2 신규 partition table 생성

```sql
DROP TABLE IF EXISTS public.sales_new CASCADE;

CREATE TABLE public.sales_new (
    sale_id     bigint NOT NULL,
    sale_time   timestamp NOT NULL,
    customer_id bigint,
    amount      numeric(12,2)
)
DISTRIBUTED BY (sale_id)
PARTITION BY RANGE (sale_time);

SELECT partman.create_parent(
    p_parent_table := 'public.sales_new',
    p_control      := 'sale_time',
    p_type         := 'native',
    p_interval     := '1 day',
    p_premake      := 7
);
```

### 10.3 데이터 이관

```sql
INSERT INTO public.sales_new
SELECT *
FROM public.sales_old;

ANALYZE public.sales_new;
```

검증:

```sql
SELECT count(*) FROM public.sales_old;
SELECT count(*) FROM public.sales_new;

SELECT min(sale_time), max(sale_time), count(*)
FROM public.sales_new;
```

### 10.4 온라인 이관 패턴

운영 중 테이블을 한 번에 오래 잠그기 어렵다면 새 partition table을 만든 뒤 작은 구간 단위로 반복 이관합니다.

```sql
-- 예: 하루 단위로 반복 수행
INSERT INTO public.sales_new
SELECT *
FROM public.sales_old
WHERE sale_time >= timestamp '2026-08-01'
  AND sale_time <  timestamp '2026-08-02';

ANALYZE public.sales_new;
```

이관 후 검증:

```sql
SELECT date_trunc('day', sale_time) AS sale_day, count(*)
FROM public.sales_old
GROUP BY 1
EXCEPT
SELECT date_trunc('day', sale_time) AS sale_day, count(*)
FROM public.sales_new
GROUP BY 1;
```

검증이 끝나면 애플리케이션 write 전환, rename, view 교체 등 운영 방식에 맞춰 마무리합니다.

---

## 11. Default partition 데이터 정리

범위를 벗어난 데이터가 default partition에 들어간 경우 child partition을 만든 뒤 데이터를 옮길 수 있습니다.

pg_partman은 partition set 생성 시 default partition을 자동으로 만들 수 있습니다. default partition은 데이터 유실을 막는 안전망이지만, 많이 쌓이면 maintenance와 조회 성능에 영향을 줄 수 있으므로 주기적으로 확인해야 합니다.

### 11.1 default partition 생성 예시

```sql
CREATE TABLE IF NOT EXISTS public.sales_daily_default
PARTITION OF public.sales_daily DEFAULT;
```

### 11.2 default partition 데이터 확인

```sql
SELECT count(*) FROM public.sales_daily_default;
```

pg_partman helper 함수로 여러 관리 대상을 전체 확인:

```sql
SELECT *
FROM partman.check_default();
```

현재 설치된 WHPG 패키지의 `check_default()` 인자 구조 확인:

```sql
SELECT p.proname, pg_get_function_arguments(p.oid) AS arguments
FROM pg_proc p
JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'partman'
  AND p.proname = 'check_default';
```

### 11.3 default에 쌓이는 원인

| 원인 | 조치 |
|---|---|
| 미래 timestamp 데이터가 premake 범위를 초과 | `premake` 증가 후 `run_maintenance()` |
| 과거 데이터 이관 중 해당 범위 child가 없음 | 필요한 partition 생성 후 범위별 직접 이관 |
| timezone 차이 | DB/client timezone 통일 권장 |
| partition key NULL | control column `NOT NULL` 검토 |
| interval 설계가 적재 패턴과 맞지 않음 | 일/월/시간 interval 재검토 |

### 11.4 default partition 데이터 직접 이관

WHPG/Greenplum에서는 `partition_data_time()`, `partition_data_id()`, `partition_data_proc()` 같은 pg_partman 데이터 재분배 함수가 내부 writable CTE 제약으로 실패할 수 있습니다.

따라서 default partition에 들어간 데이터는 날짜 또는 ID 범위별로 직접 `INSERT` 후 `DELETE` 방식으로 이관하는 것을 권장합니다.

먼저 대상 범위의 child partition이 존재하는지 확인합니다.

```sql
SELECT
    child.relname AS partition_name,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent ON parent.oid = i.inhparent
JOIN pg_class child  ON child.oid = i.inhrelid
JOIN pg_namespace n  ON n.oid = parent.relnamespace
WHERE n.nspname = 'public'
  AND parent.relname = 'sales_daily'
ORDER BY child.relname;
```

필요한 child partition이 없고, 해당 범위 데이터가 이미 default partition에 들어가 있으면 바로 partition을 생성할 수 없습니다.

PostgreSQL/WHPG는 새 child partition을 attach할 때 default partition의 constraint도 함께 조정합니다. 이때 default partition 안에 새 child partition 범위와 겹치는 row가 있으면 다음과 같은 오류가 발생합니다.

```text
ERROR: updated partition constraint for default partition would be violated by some row
```

이 경우에는 해당 범위 데이터를 임시 테이블로 먼저 빼낸 뒤, child partition을 생성하고, 다시 parent table로 넣어야 합니다.

예: `2026-08-04` 하루 partition이 없는데 해당 날짜 데이터가 default partition에 들어간 경우

```sql
BEGIN;

CREATE TEMP TABLE tmp_sales_daily_20260804
AS
SELECT *
FROM public.sales_daily_default
WHERE sale_time >= timestamp '2026-08-04 00:00:00'
  AND sale_time <  timestamp '2026-08-05 00:00:00';

DELETE FROM public.sales_daily_default
WHERE sale_time >= timestamp '2026-08-04 00:00:00'
  AND sale_time <  timestamp '2026-08-05 00:00:00';

SELECT partman.create_partition_time(
    p_parent_table := 'public.sales_daily',
    p_partition_times := ARRAY[
        timestamp '2026-08-04 00:00:00'
    ]
);

INSERT INTO public.sales_daily
SELECT *
FROM tmp_sales_daily_20260804;

DROP TABLE tmp_sales_daily_20260804;

COMMIT;
```

이미 child partition이 존재하는 범위라면 partition 생성 단계는 생략하고, default partition에서 parent table로 직접 `INSERT` 후 `DELETE` 합니다.

마무리:

```sql
SELECT count(*) FROM public.sales_daily_default;
ANALYZE ROOTPARTITION public.sales_daily;
```

---

## 12. 운영 스케줄 예시

### 12.1 cron 방식

```bash
crontab -e
```

```cron
# 매시간 pg_partman 유지보수
0 * * * * source /usr/edb/whpg7/greenplum_path.sh && psql -d whpg -c "SELECT partman.run_maintenance();"
```

특정 partition set만 관리:

```cron
*/10 * * * * source /usr/edb/whpg7/greenplum_path.sh && psql -d whpg -c "SELECT partman.run_maintenance('public.sales_daily');"
```

### 12.2 수동 운영 명령

```sql
SELECT partman.run_maintenance();
SELECT partman.run_maintenance('public.sales_daily');
```

commit 단위를 나눠 contention을 줄이고 싶으면 procedure 방식을 사용합니다.

```sql
CALL partman.run_maintenance_proc();
```

### 12.3 partition set별 유지보수 분리

특정 partition set은 자동 유지보수에서 제외하고, 별도 시간대에만 수행할 수 있습니다.

```sql
UPDATE partman.part_config
SET automatic_maintenance = 'off'
WHERE parent_table = 'public.sales_daily';

-- 전체 maintenance에서는 제외됨
SELECT partman.run_maintenance();

-- 필요할 때 명시적으로만 수행
SELECT partman.run_maintenance('public.sales_daily');
```

---

## 13. 자주 사용하는 관리 쿼리

### 13.1 pg_partman 설정 확인

```sql
SELECT *
FROM partman.part_config
ORDER BY parent_table;
```

### 13.2 특정 parent의 파티션 목록

```sql
SELECT
    child_ns.nspname AS child_schema,
    child.relname AS child_table,
    pg_get_expr(child.relpartbound, child.oid) AS partition_boundary
FROM pg_inherits i
JOIN pg_class parent       ON parent.oid = i.inhparent
JOIN pg_class child        ON child.oid = i.inhrelid
JOIN pg_namespace parent_ns ON parent_ns.oid = parent.relnamespace
JOIN pg_namespace child_ns  ON child_ns.oid = child.relnamespace
WHERE parent_ns.nspname = 'public'
  AND parent.relname = 'sales_daily'
ORDER BY child.relname;
```

### 13.3 현재 생성된 파티션 개수

```sql
SELECT parent_table, count(*) AS child_count
FROM (
    SELECT
        parent_ns.nspname || '.' || parent.relname AS parent_table,
        child.oid AS child_oid
    FROM pg_inherits i
    JOIN pg_class parent        ON parent.oid = i.inhparent
    JOIN pg_class child         ON child.oid = i.inhrelid
    JOIN pg_namespace parent_ns ON parent_ns.oid = parent.relnamespace
) s
GROUP BY parent_table
ORDER BY parent_table;
```

### 13.4 extension 함수 목록

```sql
SELECT p.proname, pg_get_function_arguments(p.oid) AS arguments
FROM pg_proc p
JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'partman'
ORDER BY p.proname, arguments;
```

### 13.5 partition boundary 상세 확인

child partition의 시작/종료 boundary를 pg_partman 함수로 확인합니다.

```sql
SELECT *
FROM partman.show_partition_info(
    p_child_table := 'public.sales_daily_p2026_08_11'
);
```

### 13.6 누락 partition gap 보정

운영 중 수동 작업이나 장애로 child partition 사이에 빈 구간이 생긴 경우 gap을 채웁니다.

```sql
SELECT partman.partition_gap_fill('public.sales_daily');
```

### 13.7 특정 partition 강제 생성

일반적으로는 `run_maintenance()`가 자동 생성하지만, 특정 날짜 또는 ID 구간을 미리 열어야 할 때 사용할 수 있습니다.

`p_partition_times`에는 생성하려는 partition의 시작 시각만 넣습니다. 이미 존재하는 partition 시작 시각은 넣지 않습니다.

또한 default partition 안에 생성하려는 범위의 데이터가 이미 들어가 있으면 `updated partition constraint for default partition would be violated by some row` 오류가 발생할 수 있습니다. 이 경우에는 11.4 절처럼 해당 범위 데이터를 임시 테이블로 먼저 빼고, partition을 생성한 뒤 parent table로 다시 입력합니다.

시간 기반:

```sql
SELECT partman.create_partition_time(
    p_parent_table := 'public.sales_daily',
    p_partition_times := ARRAY[
        timestamp '2026-08-11 00:00:00',
        timestamp '2026-08-12 00:00:00'
    ]
);
```

ID 기반:

```sql
SELECT partman.create_partition_id(
    p_parent_table := 'public.event_by_id',
    p_partition_ids := ARRAY[3000000, 4000000]
);
```

---

## 14. 권장 운영 기준

### 파티션 interval 선정

| 데이터 유형 | 권장 interval | 설명 |
|---|---|---|
| 초대용량 로그 | `1 day` 또는 `1 hour` | 삭제/백업 단위와 쿼리 조건을 함께 고려 |
| 주문/거래 fact | `1 day` 또는 `1 month` | 월 마감/일별 조회 패턴 기준 |
| IoT/metric | `1 hour` 또는 `1 day` | ingest 양이 많으면 더 짧게 |
| sequence성 이벤트 | `1000000` 등 ID range | 시간 컬럼보다 ID 범위 관리가 쉬운 경우 |

### premake

- `p_premake`는 미래 파티션을 몇 개 미리 만들지 결정합니다.
- 일 단위면 `7` 또는 `14`, 월 단위면 `3` 또는 `6` 정도로 시작합니다.
- 적재 지연/미래 timestamp 데이터가 들어올 수 있으면 더 크게 잡습니다.

### 시작 partition 지정

초기 생성 범위를 명시하고 싶으면 `p_start_partition`을 사용합니다. 주 단위 partition은 실행 요일에 따라 boundary가 달라질 수 있으므로 월요일 기준으로 맞추는 식의 명시가 유용합니다.

```sql
SELECT partman.create_parent(
    p_parent_table    := 'public.weekly_event',
    p_control         := 'event_time',
    p_type         := 'native',
    p_interval        := '1 week',
    p_premake         := 4,
    p_start_partition := to_char(date_trunc('week', current_timestamp), 'YYYY-MM-DD HH24:MI:SS')
);
```

### retention

- 실제 삭제 전에는 `retention_keep_table = true`로 detach 동작을 먼저 검증합니다.
- 검증 후 drop까지 자동화하려면 `retention_keep_table = false`로 변경합니다.
- retention은 `run_maintenance()` 실행 시 적용됩니다.

archive schema로 이동:

```sql
CREATE SCHEMA IF NOT EXISTS archive;

UPDATE partman.part_config
SET retention = '12 months',
    retention_schema = 'archive'
WHERE parent_table = 'public.order_monthly';
```

ID 기반 retention:

```sql
UPDATE partman.part_config
SET retention = '10000000',
    retention_keep_table = false
WHERE parent_table = 'public.event_by_id';
```

### ANALYZE

대량 적재 또는 파티션 이관 후에는 parent table 기준으로 analyze를 수행합니다.

```sql
ANALYZE public.sales_daily;
```

---

## 15. Troubleshooting

### `extension "pg_partman" is not available`

패키지가 OS에 설치되지 않았거나 WHPG가 참조하는 `SHAREDIR/extension` 경로에 control 파일이 없습니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh
pg_config --sharedir
ls "$(pg_config --sharedir)"/extension/pg_partman*
```

모든 노드에 패키지를 설치했는지 확인합니다.

```bash
sudo dnf install -y edb-whpg7-pg_partman
```

### parent table 이름 오류

`p_parent_table`은 schema를 포함해야 합니다.

```sql
-- 오류 가능
SELECT partman.create_parent('sales_daily', 'sale_time', 'native', '1 day');

-- 권장
SELECT partman.create_parent(
    p_parent_table := 'public.sales_daily',
    p_control      := 'sale_time',
    p_type         := 'native',
    p_interval     := '1 day'
);
```

### 파티션이 자동으로 안 생김

WHPG에서는 cron 또는 운영 배치에서 maintenance를 실행하는 방식을 기본으로 사용합니다. 먼저 수동으로 maintenance를 실행해 봅니다.

```sql
SELECT partman.run_maintenance('public.sales_daily');
```

설정값을 확인합니다.

```sql
SELECT parent_table, control, partition_type, partition_interval, premake, automatic_maintenance
FROM partman.part_config
WHERE parent_table = 'public.sales_daily';
```

수동 실행은 되지만 주기적으로 생성되지 않는다면 cron 또는 운영 스케줄러 등록 상태를 확인합니다.

### 데이터가 default partition에 쌓임

미래 파티션이 부족하거나 파티션 범위를 벗어난 데이터가 들어간 경우입니다.

```sql
SELECT partman.run_maintenance('public.sales_daily');
SELECT count(*) FROM public.sales_daily_default;
```

필요한 범위의 child partition이 생성되어 있는지 확인한 뒤, default partition에서 parent table로 범위별 직접 `INSERT` 후 `DELETE` 방식으로 이관합니다.

WHPG/Greenplum에서는 `partition_data_time()`, `partition_data_id()`, `partition_data_proc()` 같은 pg_partman 데이터 재분배 함수가 내부 writable CTE 제약으로 실패할 수 있으므로 문서 예시에서는 사용하지 않습니다.

---

## 16. 삭제 및 partition 해제 주의

운영 환경에서 pg_partman 관리 대상이나 partition 구조를 제거하는 작업은 영향 범위가 큽니다.

사용 가능한 함수 확인:

```sql
SELECT p.proname, pg_get_function_arguments(p.oid) AS arguments
FROM pg_proc p
JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'partman'
  AND (p.proname ILIKE '%undo%' OR p.proname ILIKE '%drop%')
ORDER BY p.proname;
```

### 16.1 `undo_partition()` 설명

`undo_partition()`은 partition table을 다시 일반 테이블 형태로 풀어야 할 때 사용하는 함수입니다.

이 함수는 단순히 pg_partman 관리 설정만 제거하지 않습니다. child partition 데이터를 target table로 옮기고, parent table의 partition 구조에서 child partition을 제거합니다.

주요 용도:

| 용도 | 설명 |
|---|---|
| partition 구조 해제 | parent/child partition 구조를 일반 테이블 데이터로 되돌릴 때 사용 |
| 데이터 통합 | 여러 child partition의 데이터를 하나의 target table로 모을 때 사용 |
| partition 전략 변경 전 정리 | 기존 partition 구조를 해제한 뒤 새 구조로 재설계할 때 검토 |

주요 파라미터:

| 파라미터 | 설명 |
|---|---|
| `p_parent_table` | 해제할 partition parent table |
| `p_target_table` | child partition 데이터를 옮길 대상 테이블 |
| `p_batch_count` | 한 번에 처리할 partition 개수 |
| `p_batch_interval` | 한 번에 처리할 데이터 범위 |
| `p_keep_table` | child table을 drop하지 않고 일반 테이블로 남길지 여부 |

실행 후에는 `pg_inherits` 기준 partition 목록에서 제거된 child partition이 보이지 않을 수 있습니다.

예를 들어 다음과 같은 결과가 나오면 10개 partition의 데이터가 target table로 이동했고, 해당 10개 partition은 parent partition 구조에서 제거된 것입니다.

```sql
SELECT *
FROM partman.undo_partition(
    p_parent_table    := 'public.sales_daily',
    p_target_table    := 'public.sales_daily_undo',
    p_batch_count     := 10,
    p_batch_interval  := '1 day',
    p_keep_table      := true
);
```

```text
NOTICE: Moved 10000 row(s) to the target table. Removed 10 partitions.

 partitions_undone | rows_undone
-------------------+-------------
                10 |       10000
```

일반적인 운영 점검이나 default partition 데이터 정리 목적으로는 `undo_partition()`을 사용하지 않습니다.

### 16.2 extension 제거

extension 자체를 제거하면 pg_partman 함수/설정 테이블이 제거됩니다.

```sql
DROP EXTENSION pg_partman;
DROP SCHEMA partman;
```

> 운영 주의: `DROP EXTENSION`은 pg_partman 자체 객체를 제거합니다. 관리 중인 partition table 및 업무 데이터에 대한 영향은 반드시 테스트 환경에서 확인한 뒤 수행하십시오.
