# pg-airman-mcp Air-Gap 설치 준비 가이드 (RHEL 9.2)

## 왜 이렇게 준비하나

1. **RHEL 9.2에는 Python 3.12가 없습니다.** Red Hat 공식 문서에 따르면 Python 3.11은
   RHEL 9.2부터, Python 3.12는 RHEL 9.4부터 AppStream에 패키지로 제공됩니다.
   pg-airman-mcp는 Python 3.12+가 필수이므로, `dnf install python3.12`가 통하지 않습니다.
   → `python-build-standalone`의 사전 컴파일된 relocatable 런타임을 사용합니다.

2. **psycopg가 최근 `psycopg[binary]` → `psycopg[c]`로 전환되었습니다.**
   GitHub README에 명시된 대로, Python(pipx/uv) 설치 시 C 컴파일러 + libpq 헤더 +
   Python 개발 헤더 + `pg_config`가 필요합니다.
   → 컴파일은 인터넷이 되는 **스테이징 서버**에서 한 번만 수행하고, 그 결과물(wheel)을
     에어갭 서버로 옮깁니다. 에어갭 서버는 컴파일러가 전혀 필요 없습니다.

## 아키텍처 요약

```
[스테이징 서버]                          [에어갭 RHEL 9.2 서버]
 인터넷 O                                 인터넷 X
 동일 OS/Arch (RHEL 9.2 x86_64)     ==>    (동일 OS/Arch 여야 함)
 - Python 3.12 런타임 다운로드
 - gcc/libpq-devel 설치 (빌드 전용)
 - pip wheel 로 전체 의존성 사전 컴파일
 - 결과물을 하나의 tar.gz 로 압축
                                          - 번들 압축 해제
                                          - Python 3.12 런타임 배치 (컴파일 불필요)
                                          - venv 생성 + wheel --no-index 설치
                                          - systemd 등록
```

## 파일 구성

| 파일 | 실행 위치 | 역할 |
|---|---|---|
| `01_staging_prepare.sh` | 인터넷 연결 스테이징 서버 | Python 런타임 다운로드 + wheel 사전 컴파일 + 번들 생성 |
| `02_airgap_install.sh`  | 에어갭 RHEL 9.2 서버 | 번들 압축 해제 + venv 설치 + systemd 등록 |
| `pg-airman-mcp.service` | (자동 포함) | systemd 유닛 템플릿 |

## 사전 확인 사항 (중요)

- [ ] 스테이징 서버와 에어갭 서버가 **동일한 아키텍처(x86_64)** 인지 확인
- [ ] 두 서버의 glibc 버전이 최대한 일치하는지 확인 (`rpm -q glibc`).
      다르면 wheel이 로드되지 않을 수 있습니다 (특히 psycopg-c의 C 확장 모듈).
- [ ] 에어갭 서버에 **libpq.so 런타임 라이브러리**가 이미 설치되어 있는지 확인
      (`ldconfig -p | grep libpq`). psycopg-c는 이를 동적 링크로 참조합니다.
      DB 관련 작업을 계속해오셨다면 이미 있을 가능성이 높습니다.
- [ ] `01_staging_prepare.sh` 상단의 `PBS_TAG`, `PBS_PYVER`를
      [python-build-standalone 릴리스 페이지](https://github.com/astral-sh/python-build-standalone/releases)
      최신값으로 교체
- [ ] gcc/libpq-devel 설치 시 CodeReady Builder(CRB) 리포 활성화가 필요할 수 있음
      (`subscription-manager repos --enable codeready-builder-for-rhel-9-x86_64-rpms`)

## 전송해야 할 파일 (에어갭 서버로)

- `pg-airman-mcp-airgap-bundle.tar.gz` (01번 스크립트 결과물 — Python 런타임 + wheelhouse + systemd 유닛 포함)
- `02_airgap_install.sh`

`rpms/`, `build-venv/`, `01_staging_prepare.sh` 자체는 스테이징 서버 전용이며 옮길 필요 없습니다.

## 사용법

**스테이징 서버:**
```bash
chmod +x 01_staging_prepare.sh
PKG_VERSION=1.1.1 ./01_staging_prepare.sh   # 버전 고정하려면 PKG_VERSION 지정, 아니면 생략
```

**에어갭 서버 (번들 + 02번 스크립트 전송 후):**
```bash
chmod +x 02_airgap_install.sh
./02_airgap_install.sh pg-airman-mcp-airgap-bundle.tar.gz
```

## 설치 후

- `/etc/systemd/system/pg-airman-mcp.service`에서 `AIRMAN_MCP_DATABASE_URL` 등 실제 값으로 수정
- `sudo systemctl enable --now pg-airman-mcp`
- 문제가 있으면 SELinux AVC 차단부터 확인:
  `sudo ausearch -m avc -ts recent` / `sudo semanage port -l`
