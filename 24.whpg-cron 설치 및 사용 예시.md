# edb-whpg7-whpg-cron 설치 및 기능

`edb-whpg7-whpg-cron`은 WarehousePG 7에서 database 내부 작업을 cron 표현식으로 예약 실행하기 위한 pg_cron 기반 패키지입니다. pg_cron은 background worker가 `cron.job` 메타데이터를 읽어 SQL 또는 stored procedure를 정해진 시각에 실행하는 방식입니다.

> Source:
> - EDB `gpsupp` repository의 `edb-whpg7-whpg-cron` 패키지 메타데이터
> - EDB WarehousePG whpg-cron: https://www.enterprisedb.com/docs/warehousepg/latest/ref_guide/modules/extensions/whpg-cron/

>
> Version:
> - WHPG: 7.5.0 (확인 환경)
> - OS: Rocky Linux 9.5 (확인 환경)
> - Package: `edb-whpg7-whpg-cron-1.6.8-1.el9.x86_64` (확인 환경)

---

## 1. 개요

### 1.1 설치 범위

WHPG extension 패키지는 coordinator, standby coordinator, primary segment, mirror segment를 포함한 **모든 WHPG host**에 동일하게 설치합니다. OS 패키지 설치 후 coordinator에서 대상 database에 접속하여 `CREATE EXTENSION pg_cron`을 수행합니다.

| 구분 | 작업 위치 | 수행 내용 |
|---|---|---|
| OS 패키지 설치 | 모든 WHPG host | RPM 설치 및 extension 파일 배치 |
| extension 등록 | coordinator에서 대상 DB 접속 | `CREATE EXTENSION` 실행 |
| 기능 검증 | coordinator에서 대상 DB 접속 | 제공 함수, 설정, 실행 결과 확인 |

### 1.2 사전 조건

- WarehousePG 7이 정상 기동 중이어야 합니다.
- 모든 WHPG host에서 EDB `gpsupp` repository를 사용할 수 있어야 합니다.
- 패키지 설치 권한이 있는 OS 계정(`root` 또는 `sudo` 권한 계정)이 필요합니다.
- cluster 관리와 SQL 실행에는 `gpadmin` 계정을 사용합니다.
- 설치 대상 host 목록에는 coordinator, standby coordinator, 모든 segment/mirror host를 포함해야 합니다.

### 1.3 주의 사항

- WHPG에서는 모든 host에 RPM을 설치한 뒤 coordinator에서 extension을 등록합니다. 일반 PostgreSQL용 `postgresql.conf` 편집 절차는 적용하지 않습니다.
- 운영 database에서 스케줄 작업을 등록하기 전, 작업 SQL의 실행 주체·권한·실행 위치(coordinator 또는 segment)를 검증합니다.
- 스케줄 작업이 DDL, 대량 DML, VACUUM, 백업/삭제 등을 실행한다면 lock, resource group, 동시 실행 및 실패 재시도 정책을 사전에 검토합니다.
- repository token, repository URL에 포함된 token, OS/DB 비밀번호는 문서·스크립트·로그에 저장하지 않습니다.

---

## 2. 설치 전 확인

### 2.1 coordinator에서 WHPG 및 OS 버전 확인

coordinator에서 `gpadmin`으로 수행합니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh
gpstate -f
```

OS와 설치된 패키지를 확인합니다.

```bash
cat /etc/os-release
rpm -q edb-whpg7-whpg-cron
```

### 2.2 repository 및 제공 패키지 확인

`root` 또는 `sudo` 권한 계정으로 coordinator에서 수행합니다.

```bash
dnf repolist --enabled | grep -i gpsupp
dnf repoquery --available edb-whpg7-whpg-cron \
  --qf '%{name}-%{version}-%{release}.%{arch}'
```

확인 환경에서 조회된 패키지 예시는 아래와 같습니다.

```text
edb-whpg7-whpg-cron-1.6.8-1.el9.x86_64
```

`repoquery` 명령을 사용할 수 없으면 다음 명령으로 대체합니다.

```bash
dnf list --available 'edb-whpg7-whpg-cron*'
```

### 2.3 전체 host 목록 준비

현재 cluster 구성에 맞게 host 목록을 작성합니다. 아래 host명은 예시입니다.

```bash
cat > /tmp/whpg_cron_hosts <<'EOF'
cdw
scdw
sdw1
sdw2
sdw3
sdw4
EOF
```

기존 `hostfile_exkeys`가 전체 WHPG host를 포함한다면 해당 파일을 사용할 수 있습니다.

```bash
cat hostfile_exkeys
```

> `hostfile_exkeys`에 mirror 또는 standby host가 빠져 있지 않은지 반드시 확인합니다.

---

## 3. 패키지 설치

### 3.1 단일 host 설치

단일 노드 테스트 환경 또는 개별 host 설치 시 다음 명령을 사용합니다.

```bash
sudo dnf install -y edb-whpg7-whpg-cron
```

특정 버전으로 고정해야 하는 경우 repository에 해당 버전이 존재하는지 먼저 확인한 뒤 설치합니다.

```bash
dnf repoquery --available 'edb-whpg7-whpg-cron*' \
  --qf '%{name}-%{version}-%{release}.%{arch}'

sudo dnf install -y edb-whpg7-whpg-cron-<VERSION>-<RELEASE>.<ARCH>
```

### 3.2 모든 WHPG host에 설치

coordinator에서 `gpadmin`으로 수행합니다. 각 host에서 `gpadmin`이 passwordless `sudo` 권한을 갖는 구성일 때 사용할 수 있습니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh

gpssh -f /tmp/whpg_cron_hosts -e \
  'sudo dnf install -y edb-whpg7-whpg-cron'
```

각 host의 repository 설정이 동일하지 않거나 `sudo` 정책상 위 명령을 사용할 수 없다면, 각 host의 `root` 또는 권한 있는 계정으로 동일한 `dnf install` 명령을 실행합니다.

### 3.3 설치 확인

coordinator에서 `gpadmin`으로 모든 host의 RPM 설치 상태를 확인합니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh

gpssh -f /tmp/whpg_cron_hosts -e \
  'rpm -q edb-whpg7-whpg-cron'
```

모든 host에서 같은 버전이 출력되어야 합니다.

```text
edb-whpg7-whpg-cron-<VERSION>-<RELEASE>.<ARCH>
```

---

## 4. Extension 등록

pg_cron은 background worker를 사용하므로 WHPG cluster의 preload library 설정이 필요합니다. 일반 PostgreSQL처럼 각 instance의 `postgresql.conf`를 직접 수정하지 않고, coordinator에서 `gpconfig`으로 coordinator, standby coordinator, segment, mirror에 일괄 반영합니다.

> pg_cron은 WHPG cluster에서 **하나의 database에만** 등록합니다. `cron.database_name`을 지정하지 않으면 기본 database는 `postgres`입니다.

### 4.1 pg_cron 메타데이터 database 설정

이 구성에서는 전용 database인 `pg_cron`을 사용합니다. `gpadmin`으로 다음을 수행합니다. `cron.database_name`은 `gpconfig` 기본 검증 목록에 없을 수 있으므로 `--skipvalidation`을 사용합니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh

gpconfig -c cron.database_name -v 'pg_cron' --skipvalidation
```

기본값인 `postgres`를 사용할 경우에만 이 단계를 건너뜁니다.

### 4.2 `shared_preload_libraries`에 pg_cron 추가

현재 preload library 값을 먼저 확인합니다.

```bash
gpconfig -s shared_preload_libraries
```

기존 값이 비어 있으면 다음과 같이 설정합니다.

```bash
gpconfig -c shared_preload_libraries -v 'pg_cron'
```

기존 값이 있다면 **기존 library를 유지**한 채 `pg_cron`을 추가합니다. 예를 들어 기존 값이 `auto_explain`이면 다음과 같습니다.

```bash
gpconfig -c shared_preload_libraries -v 'auto_explain,pg_cron'
```

> `gpconfig -c`는 지정한 값으로 기존 설정을 대체합니다. `gpconfig -s` 출력의 coordinator/segment 값을 확인하고, 기존 library를 빠뜨리지 않도록 완성된 목록 전체를 `-v`에 지정합니다.

### 4.3 timezone 설정

pg_cron의 기본 timezone은 GMT입니다. KST 기준으로 job을 등록하려면 다음과 같이 설정합니다.

```bash
gpconfig -c cron.timezone -v 'Asia/Seoul' --skipvalidation
```

GMT 기준으로 사용할 경우 이 단계는 건너뜁니다.

### 4.4 Job 실행 연결 방식

pg_cron은 coordinator에서 job 실행을 시작합니다. 아래 방식 중 하나를 선택하여 job 실행 계정이 대상 database에 연결할 수 있게 설정합니다.

#### 권장: Background worker 사용

Background worker 방식은 별도 libpq 연결과 `pg_hba.conf` 인증 설정이 필요하지 않습니다. WHPG 7에서는 다음과 같이 설정할 수 있습니다.

```bash
gpconfig -c cron.use_background_workers -v 'on'
gpconfig -c max_worker_processes -v '20'
```

`max_worker_processes`는 동시에 실행할 수 있는 background worker 수를 제한합니다. 기존 worker 사용량과 예정된 cron job 수를 검토하여 값을 정합니다.

#### 대안 1: Unix domain socket 사용

Unix socket으로 local connection을 사용하려면 socket directory를 지정하고, coordinator의 `pg_hba.conf`에 해당 local connection을 허용합니다.

```bash
gpconfig -c cron.host -v '/tmp'
```

`pg_hba.conf` 예시:

```text
local   all   <JOB_USER>   trust
```

`trust`는 지정 계정이 OS local connection으로 password 없이 접속하도록 허용합니다. `all all trust`처럼 범위를 넓게 설정하지 않고 job 실행 계정으로 제한합니다.

#### 대안 2: TCP loopback 사용

기본값인 `cron.host = 'localhost'`를 유지하는 경우 coordinator의 `pg_hba.conf`에 loopback 접속을 허용합니다.

```text
host    all   <JOB_USER>   127.0.0.1/32   trust
```

또는 `trust` 대신 job 실행 계정의 `.pgpass`를 사용합니다. 비밀번호를 문서, SQL, 또는 `gpconfig` 값에 기록하지 않습니다.

### 4.5 주요 운영 파라미터

| 파라미터 | 기본값 | 운영 용도 |
|---|---:|---|
| `cron.max_running_jobs` | `32` | 동시 실행 job 수 상한 |
| `cron.launch_active_jobs` | `on` | `off`로 설정하면 active job 전체 실행을 일시 중지 |
| `cron.log_run` | `on` | `cron.job_run_details` 실행 이력 기록 여부 |
| `cron.log_statement` | `on` | job 실행 SQL 로그 기록 여부 |
| `cron.enable_superuser_jobs` | `on` | superuser 권한 job 허용 여부 |

현재 값을 확인합니다.

```sql
SELECT name, setting, context
FROM pg_settings
WHERE name LIKE 'cron.%'
ORDER BY name;
```

일반 파라미터 변경은 `gpconfig`으로 수행한 뒤 cluster를 재기동합니다.

```bash
gpconfig -c cron.max_running_jobs -v '10'
```

`cron.log_min_messages`와 `cron.launch_active_jobs`는 `pg_reload_conf()`로 반영할 수 있고, 나머지 `cron.*` 파라미터는 재기동이 필요합니다.

```sql
SELECT pg_reload_conf();
```

### 4.6 WHPG cluster 재기동

`shared_preload_libraries`, `cron.database_name`, `cron.timezone`, 연결 방식 또는 worker 관련 설정을 적용하려면 WHPG cluster 재기동이 필요합니다.

```bash
gpstop -ar
```

재기동 후 설정이 모든 instance에 일관되게 반영됐는지 확인합니다.

```bash
gpconfig -s shared_preload_libraries
gpconfig -s cron.database_name
gpconfig -s cron.timezone
```

### 4.7 Extension 생성 및 권한 부여

재기동이 완료된 뒤, 4.1에서 지정했거나 기본값으로 사용하는 database에 접속하여 superuser로 실행합니다.

```sql
CREATE EXTENSION IF NOT EXISTS pg_cron;
```

등록 결과를 확인합니다.

```sql
SELECT e.extname,
       e.extversion,
       n.nspname AS schema_name
FROM pg_extension AS e
JOIN pg_namespace AS n ON n.oid = e.extnamespace
WHERE e.extname = 'pg_cron';
```

일반 사용자에게 작업 등록 권한을 부여하려면 schema 사용 권한을 추가합니다.

```sql
GRANT USAGE ON SCHEMA cron TO <USER_NAME>;
```

> pg_cron의 job 조회·변경은 생성 사용자 기준으로 제한됩니다. superuser 또는 `BYPASSRLS` 권한이 있는 사용자는 다른 사용자의 작업까지 관리할 수 있습니다.

---

## 5. 작업 등록 및 관리

### 5.1 cron 표현식

```text
┌───────────── minute (0-59)
│ ┌───────────── hour (0-23)
│ │ ┌───────────── day of month (1-31, $는 월말)
│ │ │ ┌───────────── month (1-12)
│ │ │ │ ┌───────────── day of week (0-6, 0=일요일)
│ │ │ │ │
* * * * *
```

| 표현식 | 의미 |
|---|---|
| `*/5 * * * *` | 5분마다 |
| `0 * * * *` | 매시 정각 |
| `0 0 * * *` | 매일 00:00 |
| `0 0 * * 1-5` | 평일 00:00 |
| `0 12 $ * *` | 매월 마지막 날 12:00 |
| `30 seconds` | 30초마다. 다른 시간 단위와 혼용 불가 |

### 5.2 테스트 작업 등록

먼저 읽기 전용 SQL을 사용해 실행 경로를 검증합니다.

```sql
SELECT cron.schedule(
    'whpg-cron-smoke-test',
    '*/5 * * * *',
    $$SELECT current_database(), current_user, clock_timestamp()$$
);
```

반환되는 `bigint`가 `jobid`입니다. 이름을 사용하는 작업은 같은 이름으로 다시 등록하면 기존 작업을 갱신하는 방식으로 관리할 수 있습니다.

### 5.3 운영 작업 예시

운영에서는 대상 SQL을 먼저 수동 실행하고 실행 시간·lock·resource group 영향을 확인합니다.

```sql
SELECT cron.schedule(
    'purge-old-job-history',
    '0 2 * * *',
    $$DELETE FROM cron.job_run_details
      WHERE end_time < now() - interval '14 days'$$
);
```

다른 database에서 작업을 실행해야 한다면 `cron.schedule_in_database()`를 사용합니다.

```sql
SELECT cron.schedule_in_database(
    'refresh-summary-in-appdb',
    '0 1 * * *',
    $$CALL public.refresh_summary()$$,
    '<TARGET_DB_NAME>',
    '<RUN_AS_USER>'
);
```

### 5.4 작업 조회, 변경 및 해제

```sql
-- 등록 작업 조회
SELECT jobid, jobname, schedule, command, database, username, active
FROM cron.job
ORDER BY jobid;

-- 최근 실행 이력 조회
SELECT jobid, runid, status, return_message, start_time, end_time
FROM cron.job_run_details
ORDER BY start_time DESC
LIMIT 20;

-- jobid 42의 schedule 변경
SELECT cron.alter_job(42, schedule := '0 3 * * *');

-- 작업 일시 중지
SELECT cron.alter_job(42, active := false);

-- 이름으로 작업 해제
SELECT cron.unschedule('whpg-cron-smoke-test');
```

`cron.job`은 직접 `INSERT`, `UPDATE`, `DELETE`하지 않고 위 `cron` schema 함수를 사용해 관리합니다. WHPG에서는 `TRIGGER` statement가 지원되지 않아 직접 수정한 내용이 pg_cron cache에 자동 반영되지 않을 수 있습니다.

부득이하게 `cron.job`을 직접 수정했다면 다음을 실행합니다.

```sql
SELECT cron.reload_job();
```

동일 작업은 동시에 두 번 실행되지 않습니다. 이전 실행이 끝나지 않은 상태에서 다음 주기가 오면 해당 작업은 대기했다가 기존 실행 완료 후 시작됩니다. 다만 서로 다른 작업은 병렬 실행될 수 있으므로 `cron.max_running_jobs`, resource group 및 대상 table lock을 함께 고려합니다.

---

## 6. pg_partman 유지보수 자동화

`pg_partman`의 `partman.run_maintenance()`는 미래 partition을 생성하고, `partman.part_config`에 설정한 retention 정책을 적용합니다. pg_cron을 사용하면 OS crontab 없이 이 유지보수 작업을 database 내부에서 정기 실행할 수 있습니다.

전용 `pg_cron` database에 pg_cron extension을 한 번만 등록하고, `cron.schedule_in_database()`로 각 업무 database의 pg_partman 유지보수를 중앙에서 관리합니다.

```text
pg_cron (pg_cron metadata / job 관리)
    └─ cron.schedule_in_database()
         └─ 업무 DB: partman.run_maintenance()
              ├─ premake 기준 미래 partition 생성
              └─ retention 기준 오래된 partition 정리
```

### 6.1 사전 조건

- `pg_cron` database는 `cron.database_name`으로 설정하고 pg_cron extension을 등록합니다.
- 대상 업무 database에는 `pg_partman`을 등록하고, 관리 대상은 `partman.part_config`에 등록합니다.
- 실행 계정은 대상 업무 database 접속 권한과 `partman.run_maintenance()` 실행 권한을 가져야 합니다.
- 4.4에서 선택한 연결 방식에 따라 job 실행 계정의 coordinator 인증 또는 background worker 설정을 완료합니다.
- retention을 설정하면 자동 유지보수 실행 시 보관 기간이 지난 partition과 그 데이터가 삭제될 수 있으므로, 삭제할 기간과 보관 방법을 먼저 확인합니다.

### 6.2 전체 관리 대상 유지보수 등록

`pg_cron` database에서 실행합니다. 아래 예시는 업무 database인 `appdb`의 모든 pg_partman 관리 대상을 매시 정각에 유지보수합니다.

```sql
SELECT cron.schedule_in_database(
    'appdb-partman-hourly-maintenance',
    '0 * * * *',
    $$SELECT partman.run_maintenance()$$,
    'appdb',
    'gpadmin'
);
```

### 6.3 특정 partition set 유지보수 등록

특정 partition set만 더 짧은 주기로 유지보수하려면 대상 parent table을 인자로 지정합니다.

```sql
SELECT cron.schedule_in_database(
    'sales-daily-partman-maintenance',
    '*/10 * * * *',
    $$SELECT partman.run_maintenance('public.sales_daily')$$,
    'appdb',
    'gpadmin'
);
```

### 6.4 실행 결과 확인

등록 작업과 실행 이력은 `pg_cron` database에서 확인합니다.

```sql
SELECT jobid, jobname, schedule, command, database, username, active
FROM cron.job
ORDER BY jobid;

SELECT jobid, status, return_message, start_time, end_time
FROM cron.job_run_details
ORDER BY start_time DESC
LIMIT 20;
```

> `gpadmin`은 예시 실행 계정입니다. 운영에서는 대상 database에 필요한 최소 권한만 가진 전용 batch 계정을 사용합니다.
