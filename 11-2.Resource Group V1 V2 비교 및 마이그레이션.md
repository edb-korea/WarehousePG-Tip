# Resource Group V1 → V2 비교 및 마이그레이션 가이드

## 1. Resource Group V1 vs V2 핵심 비교

| 항목 | V1 (WHPG 6) | V2 (WHPG 7) |
|---|---|---|
| 동시성(Concurrency) | 트랜잭션 레벨 관리 | 동일 |
| CPU | 퍼센트/코어 수 지정(`CPU_RATE_LIMIT`), cgroup 사용 | `CPU_MAX_PERCENT`(상한) + `CPU_WEIGHT`(우선순위)로 이원화 |
| 메모리 | 트랜잭션 레벨 관리, **오버서브스크립션 불가** (승인 시 메모리 가용 여부까지 체크) | 트랜잭션 레벨 관리, **오버서브스크립션 허용** (승인은 CONCURRENCY만으로 결정), 설정이 훨씬 단순함 |
| Disk I/O | ❌ 미지원 | ✅ `IO_LIMIT`으로 읽기/쓰기 최대 처리량 및 IOPS 제한 (cgroup v2 전용) |
| 사용자 | SUPERUSER/일반 사용자, 기본 그룹 2개(`admin_group`, `default_group`) | SUPERUSER/일반 사용자/**시스템 프로세스**까지 포함, 기본 그룹 3개(`admin_group`, `default_group`, `system_group`) |
| 큐잉(Queueing) | 슬롯 없거나 메모리 부족 시 대기 | **슬롯 없을 때만** 대기 (메모리는 승인 조건에서 제외) |
| 쿼리 실패 조건 | 고정 메모리 한도 초과 + 공유 메모리 없을 때 | 할당 메모리가 `gp_vmem_protect_limit` 및 스필 한도를 초과할 때 |
| 제한 우회 | `SET`/`RESET`/`SHOW`는 제한 미적용 | 위와 동일 + **`MIN_COST` 이하 저비용 쿼리는 그룹 배정 자체를 우회** |
| 외부 컴포넌트 | PL/Container CPU+메모리 관리 | PL/Container **CPU만** 관리 |
| cgroup 버전 | v1 전용 | v1/v2 모두 지원 (`IO_LIMIT`은 v2 전용) |

---

## 2. 기본(default) 리소스 그룹 비교

### V1 (WarehousePG/Greenplum 6)

| Limit Type | admin_group | default_group |
|---|---|---|
| CONCURRENCY | 10 | 20 |
| CPU_RATE_LIMIT | 10 | 30 |
| CPUSET | -1 | -1 |
| MEMORY_LIMIT | 10 | 0 |
| MEMORY_SHARED_QUOTA | 80 | 80 |
| MEMORY_SPILL_RATIO | 0 | 0 |
| MEMORY_AUDITOR | vmtracker | vmtracker |

### V2 (WarehousePG/Greenplum 7)

| Limit Type | admin_group | default_group | system_group |
|---|---|---|---|
| CONCURRENCY | 10 | 5 | 0 |
| CPU_MAX_PERCENT | 10 | 20 | 10 |
| CPU_WEIGHT | 100 | 100 | 100 |
| CPUSET | -1 | -1 | -1 |
| IO_LIMIT | -1 | -1 | -1 |
| MEMORY_LIMIT(MEMORY_QUOTA) | -1 | -1 | -1 |
| MIN_COST | 0 | 0 | 0 |

> `system_group`의 CONCURRENCY가 0인 이유는 사용자 쿼리가 이 그룹에서 실행되지 않기 때문입니다. autovacuum worker, syslogger, bgwriter, checkpointer, WAL writer/receiver/archiver, stats collector 등 **핵심 시스템 프로세스 전용**입니다.

---

## 3. 리소스 그룹 속성(Attribute) — 전체 매핑표

> ⚠️ 이전 버전 문서에서 V2 메모리 파라미터를 `MEMORY_LIMIT`으로 잘못 표기했던 오류를 정정했습니다. **V2의 정확한 이름은 `MEMORY_QUOTA`(MB 절대값)** 입니다.

| V1 (GP6) | V2 (GP7) | 상태 / 비고 |
|---|---|---|
| `CONCURRENCY` | `CONCURRENCY` | **변경 없음** — 그대로 사용 가능 |
| `CPUSET` | `CPUSET` | **변경 없음** — 그대로 사용 가능 |
| `CPU_RATE_LIMIT` | `CPU_MAX_PERCENT` + `CPU_WEIGHT` | 이름·의미 변경, 재계산 필요 (§5 참조) |
| `MEMORY_LIMIT` (퍼센트) | `MEMORY_QUOTA` (MB 절대값) | 이름·단위 변경, 재계산 필요 (§6 참조) |
| `MEMORY_SHARED_QUOTA` | ❌ 제거 | 대응 파라미터 없음, 무시 가능 |
| `MEMORY_SPILL_RATIO` | ❌ 제거 | 대응 파라미터 없음 (§7 상세 설명) |
| `MEMORY_AUDITOR` | ❌ 제거 | vmtracker로 통일, 지정 불필요 |
| (없음) | `MIN_COST` (신규) | 저비용 쿼리는 그룹 배정 자체를 우회 (기본값 0 = 비활성) |
| (없음) | `IO_LIMIT` (신규) | cgroup v2 환경 전용, 읽기/쓰기 처리량·IOPS 제한 |

---

## 4. 서버 설정 파라미터(GUC) 변경

### `gp_resource_manager` 값

| 값 | 설명 |
|---|---|
| `none` | 리소스 관리자 사용 안 함 (기본값) |
| `group` | 리소스 그룹 사용, cgroup **v1** 기반 |
| `group-v2` (신규) | 리소스 그룹 사용, cgroup **v2** 기반 |
| `queue` | 리소스 큐 사용 |

### 신규 파라미터

| 파라미터 | 설명 |
|---|---|
| `gp_resgroup_memory_query_fixed_mem` | 세션 레벨에서 고정 메모리량 오버라이드 |
| `gp_resource_group_move_timeout` | `pg_resgroup_move_query()` 함수의 대기 타임아웃(ms) |
| `gp_resource_group_bypass_direct_dispatch` | 다이렉트 디스패치 쿼리의 리소스 그룹 제한 우회 |
| `gp_resource_group_cpu_limit` (V1에도 존재, 유지) | 호스트 전체에서 Greenplum 프로세스가 사용할 수 있는 최대 CPU 비율 (기본 0.9 = 90%), 항상 강제됨 |
| `gp_resource_group_cpu_priority` (V1에도 존재, 유지) | 경합 시 Greenplum 프로세스 vs 외부 프로세스의 상대 우선순위 (기본 10 = 10배) |

### 제거된 파라미터

- `gp_resource_group_cpu_ceiling_enforcement`
- `gp_resource_group_enable_recalculate_query_mem`
- `gp_resource_group_memory_limit`

---

## 5. CPU 관리 상세 — `CPU_RATE_LIMIT` → `CPU_MAX_PERCENT`/`CPU_WEIGHT`

### V1 동작 방식
`gp_resource_group_cpu_ceiling_enforcement` 설정에 따라 두 가지로 동작:
- **`false`(기본값, 탄력적/elastic)**: 다른 그룹이 유휴 상태면 그 여유 CPU를 빌려 사용 가능. 전체 그룹의 `CPU_RATE_LIMIT` 합은 반드시 100 이하.
- **`true`(엄격한 상한/strict ceiling)**: 여유가 있어도 `CPU_RATE_LIMIT`을 절대 초과 못 함.

### V2 동작 방식 — 두 파라미터로 분리
- **`CPU_MAX_PERCENT`** (1~100, 기본 -1=미설정): gpdb cgroup 할당량(`gp_resource_group_cpu_limit`) 대비 퍼센트. **항상 강제되는 하드 리밋.**
  - 전체 시스템 CPU = `gp_resource_group_cpu_limit × (CPU_MAX_PERCENT / 100)`
  - 예: `gp_resource_group_cpu_limit=0.9`, `CPU_MAX_PERCENT=50` → 전체 시스템의 45%
- **`CPU_WEIGHT`** (1~500, 기본 100): 그룹 간 경합 시에만 작동하는 상대 우선순위. 경합이 없으면 영향 없음.
- **CPU 오버커밋 가능**: 모든 그룹의 `CPU_MAX_PERCENT` 합이 100%를 넘어도 됩니다(V1과 가장 큰 차이). 각 그룹은 개별 상한만 지키면 되고, 워크로드 피크 시간이 겹치지 않으면 문제 없습니다.

### 마이그레이션 변환 — 3가지 옵션

**예시: V1 설정**
```sql
CREATE RESOURCE GROUP executive WITH (CONCURRENCY=1, CPU_RATE_LIMIT=30);
CREATE RESOURCE GROUP reporting WITH (CONCURRENCY=8, CPU_RATE_LIMIT=20);
CREATE RESOURCE GROUP etl WITH (CONCURRENCY=3, CPU_RATE_LIMIT=20);
```

**옵션 1 — Weight만 사용 (탄력적 동작 재현, 가장 단순)**
```sql
-- CPU_WEIGHT = CPU_RATE_LIMIT × 10
CREATE RESOURCE GROUP executive WITH (CONCURRENCY=1, CPU_WEIGHT=300);
CREATE RESOURCE GROUP reporting WITH (CONCURRENCY=8, CPU_WEIGHT=200);
CREATE RESOURCE GROUP etl WITH (CONCURRENCY=3, CPU_WEIGHT=200);
```

**옵션 2 — CPU_MAX_PERCENT 직접 변환 (V1에서 엄격한 상한을 썼거나 강한 격리가 필요할 때)**
```sql
CREATE RESOURCE GROUP executive WITH (CONCURRENCY=1, CPU_MAX_PERCENT=30, CPU_WEIGHT=300);
CREATE RESOURCE GROUP reporting WITH (CONCURRENCY=8, CPU_MAX_PERCENT=20, CPU_WEIGHT=200);
CREATE RESOURCE GROUP etl WITH (CONCURRENCY=3, CPU_MAX_PERCENT=20, CPU_WEIGHT=200);
```

**옵션 3 (권장) — 여유 있는 CPU_MAX_PERCENT + Weight (유연성과 안전장치의 균형)**
```sql
CREATE RESOURCE GROUP executive WITH (CONCURRENCY=1, CPU_MAX_PERCENT=60, CPU_WEIGHT=300);
CREATE RESOURCE GROUP reporting WITH (CONCURRENCY=8, CPU_MAX_PERCENT=40, CPU_WEIGHT=200);
CREATE RESOURCE GROUP etl WITH (CONCURRENCY=3, CPU_MAX_PERCENT=40, CPU_WEIGHT=200);
```

| 접근 방식 | CPU_MAX_PERCENT | CPU_WEIGHT | 동작 |
|---|---|---|---|
| 옵션 1: Weight만 | 미설정(-1) | 300, 200, 200 | 탄력적, GP6 기본값과 유사 |
| 옵션 2: 엄격 | 30, 20, 20 | 300, 200, 200 | 엄격한 격리 |
| 옵션 3: 균형(권장) | 60, 40, 40 | 300, 200, 200 | 유연성 + 안전 상한 |

> `CPUSET`으로 코어를 직접 지정해서 쓰던 그룹은 **변경 없이 그대로 마이그레이션** 가능합니다.

### 마이그레이션 사전 점검
```sql
-- V1의 ceiling enforcement 설정 확인
gpconfig -s gp_resource_group_cpu_ceiling_enforcement
```
- `false`(기본값)였다면 → 그룹들이 탄력적으로 버스트하던 상태였다는 뜻
- `true`였다면 → 이미 하드 상한으로 운영 중이었다는 뜻 (옵션 2/3이 더 적합)

Greenplum Command Center의 **Resource Groups > Monitor** 화면에서 그룹별 실제 CPU 사용 패턴(평균/최대 사용률)을 확인한 뒤 값을 정하는 것을 권장합니다.

---

## 6. 메모리 관리 상세 — `MEMORY_LIMIT` → `MEMORY_QUOTA`

### V1 메모리 모델 (복잡함)
```
rg_perseg_mem = ((RAM × (vm.overcommit_ratio/100) + SWAP) × gp_resource_group_memory_limit) / num_active_primary_segments
```
- `MEMORY_LIMIT`(0~100%): 세그먼트 메모리 중 이 그룹에 예약된 비율. 전체 그룹 합이 100% 이하여야 함.
- `MEMORY_SHARED_QUOTA`(기본 80%): 예약된 그룹 메모리 중 트랜잭션 간 공유 가능한 비율.
- `MEMORY_SPILL_RATIO`(기본 0%): 할당 메모리의 몇 %를 쓰면 디스크로 스필할지의 임계값.
- 메모리 소스 3단계: **고정 슬롯 → 그룹 공유 → 전역 공유** 순서로 소비.
- 문제점: 파라미터 3개의 상호작용이 복잡해서 실제 할당량 예측이 어려움.

### V2 메모리 모델 (단순화)
- `gp_resource_group_memory_limit` 파라미터 자체가 **제거됨**.
- 대신 시스템 전역 파라미터 `gp_vmem_protect_limit`(MB)이 세그먼트 인스턴스의 전체 postgres 프로세스가 쓸 수 있는 절대 상한을 결정.
- `runaway_detector_activation_percent`가 이 한도의 몇 %에서 쿼리를 강제 종료할지 결정.
- **`MEMORY_QUOTA`**(정수 MB, 기본 -1=미설정): 쿼리 하나당 메모리 = `MEMORY_QUOTA / CONCURRENCY` 와 `statement_mem` 중 **더 큰 값**.
  - `-1`이면 그냥 `statement_mem` 사용.
  - `max_statement_mem`을 초과할 수 없음.
- **핵심 동작 변화**: 트랜잭션이 그룹에 승인되는지는 **오직 CONCURRENCY(슬롯 여부)로만 결정**되며, 메모리 여유는 승인 조건이 아닙니다. 즉 `MEMORY_QUOTA`는 그룹 전체에 대한 하드 상한이 아니라 **"쿼리당 얼마씩 나눠줄지"의 배분 공식**일 뿐입니다.

### 변환 예시 (`gp_vmem_protect_limit=32768 MB` 가정)

| V1 MEMORY_LIMIT | V2 MEMORY_QUOTA | 계산 |
|---|---|---|
| 20% | 6554 MB | 32768 × 0.20 |
| 30% | 9830 MB | 32768 × 0.30 |
| 40% | 13107 MB | 32768 × 0.40 |
| 0% | -1 | statement_mem 사용 |

```sql
-- V1
CREATE RESOURCE GROUP executive WITH (CONCURRENCY=1, CPU_RATE_LIMIT=30, MEMORY_LIMIT=20);
CREATE RESOURCE GROUP reporting WITH (CONCURRENCY=8, CPU_RATE_LIMIT=20, MEMORY_LIMIT=30);
CREATE RESOURCE GROUP etl WITH (CONCURRENCY=3, CPU_RATE_LIMIT=20, MEMORY_LIMIT=40);

-- V2
CREATE RESOURCE GROUP executive WITH (CONCURRENCY=1, CPU_WEIGHT=300, MEMORY_QUOTA=6554);
CREATE RESOURCE GROUP reporting WITH (CONCURRENCY=8, CPU_WEIGHT=200, MEMORY_QUOTA=9830);
CREATE RESOURCE GROUP etl WITH (CONCURRENCY=3, CPU_WEIGHT=200, MEMORY_QUOTA=13107);
```

**쿼리당 실제 할당 메모리** (`statement_mem='3GB'`=3072MB 가정):

| 그룹 | MEMORY_QUOTA | CONCURRENCY | QUOTA/CONCURRENCY | 실제 할당 | 적용 규칙 |
|---|---|---|---|---|---|
| executive | 6554 MB | 1 | 6554 MB | **6554 MB** | MEMORY_QUOTA/CONCURRENCY |
| reporting | 9830 MB | 8 | 1228 MB | **3072 MB** | statement_mem (더 큰 값) |
| etl | 13107 MB | 3 | 4369 MB | **4369 MB** | MEMORY_QUOTA/CONCURRENCY |
| admin_group | -1 | 10 | -1 | **3072 MB** | statement_mem |

> `MEMORY_SHARED_QUOTA`와 `MEMORY_SPILL_RATIO`는 대응 파라미터가 없으므로 마이그레이션 시 그냥 무시하면 됩니다.

---

## 7. `MEMORY_SPILL_RATIO`는 V2에서 어떻게 동작하는가

**핵심: V2에는 이 기능에 대응하는 "설정 가능한 스필 임계값" 자체가 없습니다.**

- **V1**: `MEMORY_SPILL_RATIO`(기본 0)로 "할당 메모리의 몇 %를 쓰면 디스크로 스필할지" 조절. 0이면 `statement_mem`을 초기 오퍼레이터 메모리로 사용.
- **V2**: 세 단계 메모리 풀(고정/그룹 공유/전역 공유) 모델 자체가 사라지고, 앞서 설명한 단순 쿼터 나눗셈(`MEMORY_QUOTA / CONCURRENCY` vs `statement_mem` 중 큰 값)으로 **쿼리당 고정 메모리**가 정해집니다. **이 고정 메모리를 초과하는 순간 자동으로 디스크 스필이 발생**하며, "몇 %에서 스필할지" 조절하는 다이얼 자체가 없습니다.
- 실시간 스필 모니터링은 `gp_toolkit.gp_workfile_usage_per_query`, `gp_toolkit.gp_workfile_entries`, `gp_toolkit.gp_workfile_usage_per_segment` 뷰로 확인합니다.

**결론**: `MEMORY_SPILL_RATIO` 값을 그대로 옮길 대상이 없으므로, V2에서는 **`MEMORY_QUOTA` 값 자체를 넉넉하게 설정**해 불필요한 스필을 줄이는 방향으로 튜닝 전략을 바꿔야 합니다.

---

## 8. V2의 오버서브스크립션(Overcommit) 정책

### CPU 오버서브스크립션
- V1: 모든 그룹의 `CPU_RATE_LIMIT` 합이 **100을 넘을 수 없음** (기본 그룹 포함, 강제 제약).
- V2: 모든 그룹의 `CPU_MAX_PERCENT` 합이 **100%를 넘어도 됨**. 각 그룹은 자신의 `CPU_MAX_PERCENT`로만 개별 제한되며, 실제 수요 합이 100%를 넘을 때만 `CPU_WEIGHT` 비율대로 나눠 가짐.
- 워크로드별 피크 시간이 겹치지 않는다면(예: 낮 리포팅 / 밤 ETL), 각 그룹에 넉넉한 상한을 줘도 실제 동시 사용률은 낮을 수 있다는 전제.

### 메모리 오버서브스크립션 (V1과 근본적으로 다른 부분)
- **V1**: 트랜잭션이 그룹에 admit(승인)될 때 `CONCURRENCY`와 **그룹의 메모리 가용 여부를 모두** 체크. 메모리가 없으면 큐잉(대기).
- **V2**: 트랜잭션 승인은 **오직 `CONCURRENCY`(슬롯 여부)로만** 결정됩니다. 즉 `MEMORY_QUOTA`는 그룹 전체 메모리에 대한 하드 상한(차단 장치)이 아니라, "슬롯이 있으면 쿼리당 이만큼 배분한다"는 **계산 공식**일 뿐입니다.
- 여러 그룹의 `MEMORY_QUOTA` 합이 시스템 물리 메모리(`gp_vmem_protect_limit`)를 초과하도록 설정해도 **V2는 이를 사전에 막지 않습니다** — 이것이 메모리 오버서브스크립션을 허용한다는 의미입니다.

**대신 실제 물리적 한계는 아래 두 안전장치가 담당**:

1. **`gp_vmem_protect_limit`(MB)**: 세그먼트 인스턴스 전체가 쓸 수 있는 절대 상한. 초과 시 즉시 실패:
   ```
   ERROR:  Out of memory  (seg0 slice6 ...)
   DETAIL:  Vmem limit reached, failed to allocate ... from tracker
   ```
2. **`runaway_detector_activation_percent`**: `gp_vmem_protect_limit`의 몇 %에서 "런어웨이 쿼리"로 판단해 강제 종료할지 결정:
   ```
   ERROR: Canceling query because of high VMEM usage. Used: 9100MB, available 900MB, red zone: 9000MB
   ```

**정책 요약**: V2의 메모리 오버서브스크립션은 "여러 그룹에 넉넉하게 쿼터를 나눠주되, 실제로 동시에 몰아 쓰면 물리적 한계에서 걸러낸다"는 **낙관적(optimistic) 자원 배분 철학**입니다. V1처럼 그룹 단위로 메모리를 사전에 안전하게 예약해두는 방식이 아닙니다.

**마이그레이션 실무 함의**:
- V1에서 `MEMORY_LIMIT` 합을 항상 100% 이하로 엄격히 관리했더라도, V2에서는 `MEMORY_QUOTA` 합이 시스템 메모리를 넘겨도 당장 에러가 나지 않으므로 **안일하게 설정하기 쉽습니다.**
- 반드시 `gp_toolkit.gp_resgroup_status_per_host` / `gp_resgroup_status_per_segment` 뷰로 실사용량을 모니터링하고, OOM/runaway 종료 로그 발생 여부를 확인해야 합니다.
- 문제가 발생하면 `MEMORY_QUOTA`를 낮추거나 `gp_vmem_protect_limit`을 늘리는 방향으로 조정합니다.

---

## 9. 시스템 뷰 변경

| 뷰 | 변경 내용 |
|---|---|
| `gp_resgroups_config` | `cpu_rate_limit`, `memory_shared_quota`, `memory_spill_ratio`, `memory_auditor` → `cpu_max_percent`, `cpu_weight`, `cpuset`, `min_cost`, `io_limit`로 교체 |
| `gp_resgroup_status` | `rsgname` → `groupname`으로 이름 변경, `cpu_usage`/`memory_usage`는 `gp_resgroup_status_per_host`로 이동 |
| `gp_resgroup_status_per_host` | 호스트명/메모리 관련 필드 제거, `segment_id`/`cpu_usage`/`memory_usage` 필드 추가 |
| `gp_resgroup_status_per_segment` | `rsgname`, `hostname`, 메모리 관련 필드 제거, `groupname`/`vmem_usage` 필드 추가 |
| `gp_resgroup_iostats_per_host` (신규) | I/O 통계 전용 뷰 신규 추가 |
| `gp_workfile_usage_per_query` 등 (스필 모니터링) | V2에서 스필 임계값 파라미터가 사라진 대신, 실시간 스필 모니터링 용도로 중요도 상승 |

---

## 10. 전체 마이그레이션 절차 요약

### Step 1. 기존 V1 설정값 조회 및 백업
```sql
SELECT groupid, groupname, concurrency, cpu_rate_limit, memory_limit,
       memory_shared_quota, memory_spill_ratio, memory_auditor
FROM gp_toolkit.gp_resgroup_config;
```

### Step 2. gp_resource_group_cpu_ceiling_enforcement 확인 및 CPU 사용 패턴 리뷰
```sql
gpconfig -s gp_resource_group_cpu_ceiling_enforcement
```
Greenplum Command Center의 Resource Groups > Monitor 화면에서 그룹별 실제 CPU/메모리 사용률(평균/최대) 확인.

### Step 3. `gp_resource_manager` 전환 (재시작 필요)
```bash
gpconfig -c gp_resource_manager -v "group-v2"   # cgroup v2 사용 시
# 또는
gpconfig -c gp_resource_manager -v "group"      # cgroup v1 유지 시
gpstop -ra
```

### Step 4. 리소스 그룹 재생성 (V2 속성 기준, §5/§6 변환 공식 적용)
```sql
CREATE RESOURCE GROUP etl_group
WITH (
    CPU_MAX_PERCENT=40,
    CPU_WEIGHT=200,
    CONCURRENCY=3,
    MEMORY_QUOTA=13107,
    MIN_COST=0
);

-- Disk I/O 제한이 필요하면 (cgroup v2 환경만)
ALTER RESOURCE GROUP etl_group SET IO_LIMIT '*:1000mbps';
```

### Step 5. 역할(Role)에 재할당
```sql
ALTER ROLE etl_user RESOURCE GROUP etl_group;
```

### Step 6. 검증 및 모니터링
```sql
-- 리소스 그룹 설정값 조회
SELECT groupname, cpu_max_percent, cpu_weight, cpuset, min_cost, io_limit
FROM gp_toolkit.gp_resgroup_config;

-- Segment 별 메모리 사용량 확인
SELECT * FROM gp_toolkit.gp_resgroup_status_per_segment;

-- 리소스 그룹별 수행되는 job 현황 확인
select * FROM gp_toolkit.gp_resgroup_status;

-- 스필/메모리 모니터링
SELECT * FROM gp_toolkit.gp_workfile_usage_per_query;
SELECT * FROM gp_toolkit.gp_resgroup_status_per_host;

-- 그룹별 실행/대기 현황
SELECT groupid, groupname, num_running, num_bypass, num_earlyunassign,
       num_queueing, num_queued, num_executed, total_queue_duration
FROM gp_toolkit.gp_resgroup_status;

-- 호스트별 디스크 I/O 사용량
SELECT * FROM gp_toolkit.gp_resgroup_iostats_per_host;
```

## Greenplum 7 / WarehousePG V2 리소스 그룹 모니터링 뷰 참고표

목적별로 어떤 `gp_toolkit` 뷰를 사용해야 하는지 정리한 참고표입니다.

| 확인하고 싶은 것 | 사용할 뷰 |
|---|---|
| 그룹별 실행 중/대기 중 쿼리 수, 우회(bypass) 건수 | `gp_toolkit.gp_resgroup_status` |
| 세그먼트별 실시간 메모리(`vmem_usage`) 사용량 | `gp_toolkit.gp_resgroup_status_per_segment` |
| 호스트별 CPU/메모리 사용량 | `gp_toolkit.gp_resgroup_status_per_host` |
| 그룹 설정값(`CPU_MAX_PERCENT` 등) | `gp_toolkit.gp_resgroup_config` |
| 호스트별 디스크 I/O 사용량 | `gp_toolkit.gp_resgroup_iostats_per_host` |
| 역할(Role)별 할당된 그룹 | `gp_toolkit.gp_resgroup_role` |

> 모든 뷰는 `gp_toolkit` 스키마에 속해 있으므로, `search_path`에 `gp_toolkit`이 포함되어 있지 않다면 반드시 스키마를 명시(`gp_toolkit.뷰이름`)해서 조회해야 합니다.

---

## 11. 마이그레이션 시 특히 주의할 점

1. **`MEMORY_SHARED_QUOTA`/`MEMORY_SPILL_RATIO`/`MEMORY_AUDITOR`는 단순 값 이전이 불가능**합니다. V2의 메모리 모델(오버서브스크립션 허용 + 다른 실패 조건)이 근본적으로 다르므로, 워크로드를 재테스트하며 `MEMORY_QUOTA`를 새로 튜닝해야 합니다.
2. **cgroup v1 → v2 전환 시 OS 레벨 준비**가 선행되어야 합니다 (마운트된 cgroup 파일시스템 확인, `gpadmin`의 `/sys/fs/cgroup/cgroup.procs` 쓰기 권한 등).
3. `system_group`이라는 새 기본 그룹이 추가되었으므로, 기존에 시스템 프로세스에 별도 제한을 두지 않았다면 이 신규 그룹의 기본 설정값이 운영에 영향을 주는지 확인해야 합니다. **`system_group`의 CPU 할당을 기본값보다 낮추는 것은 권장하지 않습니다** (체크포인트/WAL/오토바큠 지연으로 안정성 저하 위험).
4. `rsgname → groupname` 같은 **시스템 뷰 컬럼명 변경**으로 인해, 기존 모니터링 스크립트/대시보드 쿼리는 반드시 수정이 필요합니다.
5. **메모리 오버서브스크립션 특성상, V1 대비 "설정은 자유롭지만 실제 부하 시 개별 쿼리가 실패할 위험"이 커졌습니다.** 전환 후 반드시 OOM/runaway 종료 로그를 모니터링하십시오.
6. 실제 전환 전, **스테이징 환경에서 워크로드 재현 테스트**를 강력히 권장합니다.

