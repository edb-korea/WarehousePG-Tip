# WarehousePG Tiered Partition 구성 가이드 (네이티브 `file://` Protocol External Table 사용)

이번엔 `s3://`나 `pxf://`가 아니라 WarehousePG의 가장 기본적인 **`file://` external table protocol**로 동일한 패턴(오래된 파티션 → 오프로드 → 부모 테이블에 재연결)을 구성하는 방법입니다.

> **먼저 알아야 할 중요한 차이점:** `file://` 프로토콜은 **S3 같은 오브젝트 스토리지에 직접 연결하는 프로토콜이 아닙니다.** 세그먼트 호스트(`gpadmin`이 접근 가능한)의 **로컬/마운트된 파일시스템 경로**만 읽을 수 있고, **읽기 전용(read-only)**만 지원합니다. S3를 "파일처럼" 다루려면 각 세그먼트 호스트와 코디네이터에 S3 버킷을 파일시스템으로 마운트(`mountpoint-s3`, `s3fs-fuse`, `goofys` 등 FUSE 마운트)해 두고, 그 마운트 경로를 `file://` URI로 지정해야 합니다. 순수하게 S3 API로 직접 쓰고 읽으려면 `s3://` 프로토콜이나 `pxf://` 프로토콜을 쓰는 것이 정석입니다.

## 0. 사전 조건

- `file://`는 **읽기 전용**입니다. 따라서 오래된 파티션 데이터를 "내보내는(쓰는)" 단계는 `file://`로 할 수 없고, 병렬로 파일을 만들어주는 `gpfdist://` writable external table(또는 `COPY`)을 함께 사용해야 합니다.
- URI에 지정하는 호스트명은 `gp_segment_configuration` 카탈로그에 등록된 세그먼트 호스트명과 정확히 일치해야 합니다.
- `LOCATION` 절에 지정하는 URI 개수만큼 세그먼트가 병렬로 파일을 나눠 읽습니다. 세그먼트 호스트당 파일 개수는 그 호스트의 primary 세그먼트 수를 넘을 수 없습니다(`pg_max_external_files` 뷰로 확인 가능).
- S3를 쓰려면: 모든 세그먼트 호스트 + 코디네이터에 동일한 마운트 경로(예: `/mnt/s3-archive`)로 대상 버킷을 FUSE 마운트해 둡니다.

  ```bash
  # 예: mountpoint-s3 사용 (각 호스트에서 동일하게 실행)
  mount-s3 my-warehouse-archive /mnt/s3-archive
  ```

## 1. 파티션 테이블 생성 (일반 range partition)

```sql
CREATE TABLE sales (
  sale_id     bigint,
  sale_date   date NOT NULL,
  amount      numeric
) DISTRIBUTED BY (sale_id)
PARTITION BY RANGE (sale_date);

CREATE TABLE sales_2024_01 PARTITION OF sales
  FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');
CREATE TABLE sales_2024_02 PARTITION OF sales
  FOR VALUES FROM ('2024-02-01') TO ('2024-03-01');
CREATE TABLE sales_2024_03 PARTITION OF sales
  FOR VALUES FROM ('2024-03-01') TO ('2024-04-01');
```
## 1-1. Sample data
```sql
INSERT INTO sales (sale_id, sale_date, amount)
SELECT
  i AS sale_id,
  ('2024-01-01'::date + (i % 31) * interval '1 day')::date AS sale_date,
  round((random() * 1000 + 10)::numeric, 2) AS amount
FROM generate_series(1, 10000) AS i;

INSERT INTO sales (sale_id, sale_date, amount)
SELECT
  i AS sale_id,
  ('2024-02-01'::date + (i % 28) * interval '1 day')::date AS sale_date,
  round((random() * 1000 + 10)::numeric, 2) AS amount
FROM generate_series(1, 10000) AS i;

INSERT INTO sales (sale_id, sale_date, amount)
SELECT
  i AS sale_id,
  ('2024-03-01'::date + (i % 31) * interval '1 day')::date AS sale_date,
  round((random() * 1000 + 10)::numeric, 2) AS amount
FROM generate_series(1, 10000) AS i;
```

## 2. 오래된 파티션 내보내기 (병렬 unload → 마운트된 S3 경로)

각 세그먼트 호스트에서 `gpfdist`를 마운트 경로 위에 띄우고, writable external table로 파티션 데이터를 세그먼트별 파일로 풀어냅니다.

```bash
# 각 세그먼트 호스트에서 (마운트된 S3 경로를 서빙)
gpfdist -d /mnt/s3-archive/sales/2024_01 -p 8801 &
```

```sql
-- ① 부모에서 분리
ALTER TABLE sales DETACH PARTITION sales_2024_01;

-- ② 세그먼트 호스트별 gpfdist를 통해 병렬로 unload
CREATE WRITABLE EXTERNAL TABLE sales_2024_01_wex (LIKE sales_2024_01)
  LOCATION (
    'gpfdist://seghost1:8801/sales_2024_01_seg.csv',
    'gpfdist://seghost2:8801/sales_2024_01_seg.csv'
  )
  FORMAT 'CSV';

INSERT INTO sales_2024_01_wex SELECT * FROM sales_2024_01;

-- ③ 검증 후 로컬 파티션 드롭
DROP TABLE sales_2024_01;
DROP EXTERNAL TABLE sales_2024_01_wex;
```

이제 `/mnt/s3-archive/sales/2024_01/`(= S3 버킷 내부)에 세그먼트 수만큼 CSV 파일이 생성되어 있고, 이는 곧 S3에 실제로 업로드된 상태입니다.

## 3. `file://` 프로토콜로 다시 부모 테이블에 파티션 연결하기

이제 각 세그먼트 호스트에 마운트된 동일 경로를 `file://` URI로 지정해 읽기 전용 external table을 만들고, DETACH 자리에 ATTACH 합니다.

```sql
-- ① S3(마운트된 파일시스템)에 있는 파일을 가리키는 file:// external table
CREATE EXTERNAL TABLE sales_2024_01_cold (LIKE sales)
  LOCATION (
    'file://seghost1:40000/mnt/s3-archive/sales/2024_01/sales_2024_01_seg.csv',
    'file://seghost2:40000/mnt/s3-archive/sales/2024_01/sales_2024_01_seg.csv'
  )
  FORMAT 'CSV';

-- ② 부모 파티션 테이블에 다시 연결
ALTER TABLE sales ATTACH PARTITION sales_2024_01_cold
  FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');
```

> `file://` URI의 포트 번호는 실제 서비스 포트가 아니라 WarehousePG가 세그먼트를 식별하기 위한 값이며, 관례적으로 세그먼트 포트 대역(`gp_segment_configuration`의 포트)을 사용합니다. 호스트명은 `gp_segment_configuration.hostname`과 정확히 일치해야 합니다.

이제 `sales`를 조회하면 로컬 heap 파티션(핫 데이터)과 `file://`로 연결된 파티션(콜드 데이터, 실제로는 S3에 저장)이 하나의 쿼리로 함께 조회됩니다.

## 주의할 제약사항

- **`file://` 외부 테이블은 항상 읽기 전용**입니다. 쓰기가 필요하면 `gpfdist://`나 `s3://` 같은 다른 프로토콜을 병행해야 합니다.
- URI에 지정한 호스트명이 `gp_segment_configuration`에 등록된 세그먼트 호스트명과 다르면 오류가 발생합니다.
- 세그먼트 호스트당 파일 개수는 해당 호스트의 primary 세그먼트 수를 초과할 수 없습니다. `SELECT * FROM pg_max_external_files;`로 사용 가능한 파일 슬롯을 확인하세요.
- FUSE 기반 S3 마운트(`s3fs`, `goofys`, `mountpoint-s3` 등)는 **POSIX 파일시스템과 완전히 동일하지 않습니다** — 강한 일관성, atomic rename, 임의 위치 쓰기 등이 제한적일 수 있고, 운영 환경에서는 마운트 프로세스를 모든 세그먼트/코디네이터 호스트에서 안정적으로 유지·모니터링해야 하는 추가 운영 부담이 있습니다.
- external/foreign leaf 파티션 공통 제약(WarehousePG 파티셔닝 문서 기준): 쓰기 불가한 파티션에 대한 `INSERT/DELETE/UPDATE/TRUNCATE`는 오류, `VACUUM`은 스킵, `COPY`로 복사하려면 `IGNORE EXTERNAL PARTITIONS` 절이 필요합니다.
- `gpbackup`은 leaf 파티션이 external/foreign 테이블이면 그 데이터를 백업하지 않으므로, 마운트된 S3 원본에 대한 별도 백업/보존 정책이 필요합니다.

## 정리: 언제 `file://`를 쓰나

`file://`는 원래 **세그먼트 호스트에 이미 존재하는 로컬 디스크나 NFS 공유 파일**을 외부 테이블로 노출하기 위한 프로토콜입니다. S3를 이 방식으로 다루려면 FUSE 마운트라는 우회 계층이 필요하고, 이는 네이티브 `s3://` 프로토콜이나 `pxf://` 프로토콜(S3 API를 직접 사용, 마운트 불필요, 더 안정적)에 비해 운영 복잡도와 리스크가 높습니다. 온프레미스 NFS/공유 스토리지로 콜드 티어를 구성하거나, 이미 마운트 기반 아키텍처를 운영 중인 경우가 아니라면 `s3://` 또는 `pxf://` 프로토콜을 권장합니다.

## 참고 자료

- [file:// Protocol (WarehousePG)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/external/external-tables/file-protocol/)
- [Using the WarehousePG Parallel File Server (gpfdist)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/external/using-gpfdist/)
- [Working with External Data (WarehousePG)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/external/)
- [Partitioning Large Tables (WarehousePG)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/ddl/ddl-partition)
