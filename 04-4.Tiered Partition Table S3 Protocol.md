# WarehousePG Tiered Partition 구성 가이드 (네이티브 `s3://` Protocol External Table 사용)

PGAA 대신 WarehousePG에 내장된 **`s3://` external table protocol**(`gps3ext`/`gpcheckcloud` 기반, Greenplum 시절부터 있던 네이티브 프로토콜)을 사용해 동일한 패턴(오래된 파티션 → S3 오프로드 → 부모 테이블에 재연결)을 구성하는 방법입니다. PGAA/Iceberg·Delta 같은 오픈 테이블 포맷이 아니라 **일반 텍스트/CSV 파일**을 S3에 그대로 쓰고 읽는 방식이라는 점이 가장 큰 차이입니다.

## 0. 사전 준비 — s3 protocol 등록 및 설정 파일

### 0-1. 프로토콜 등록 (DB마다 1회)

```sql
CREATE OR REPLACE FUNCTION write_to_s3() RETURNS integer AS
  '$libdir/gps3ext.so', 's3_export' LANGUAGE C STABLE;

CREATE OR REPLACE FUNCTION read_from_s3() RETURNS integer AS
  '$libdir/gps3ext.so', 's3_import' LANGUAGE C STABLE;

-- superuser 전용이면:
CREATE PROTOCOL s3 (writefunc = write_to_s3, readfunc = read_from_s3);

-- 일반 사용자도 쓰게 하려면 TRUSTED로 만들고 권한 부여:
-- CREATE TRUSTED PROTOCOL s3 (writefunc = write_to_s3, readfunc = read_from_s3);
-- GRANT ALL ON PROTOCOL s3 TO analytics_user;
```

### 0-2. `s3.conf` 설정 파일 준비 (모든 세그먼트 호스트에 동일 경로로 배치, 또는 http(s) 서버로 서빙)

```bash
# 템플릿 생성
gpcheckcloud -t > /home/gpadmin/aws_s3/s3.conf
```

```ini
[default]
accessid = "AKIA..."
secret = "..."
threadnum = 3
chunksize = 67108864
```

### 0-3. 연결 확인

```bash
gpcheckcloud -c "s3://s3-us-west-2.amazonaws.com/my-warehouse-archive/sales/ config=/home/gpadmin/aws_s3/s3.conf"
# Your configuration works well.
```

## 1. 파티션 테이블 생성 (일반 range partition)

```sql
CREATE TABLE sales (
  sale_id     bigint,
  sale_date   date NOT NULL,
  amount      numeric
) DISTRIBUTED BY (sale_id)
PARTITION BY RANGE (sale_date);

CREATE TABLE sales_2024_01 PARTITION OF sales
  FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');
CREATE TABLE sales_2024_02 PARTITION OF sales
  FOR VALUES FROM ('2024-02-01') TO ('2024-03-01');
```

## 2. 오래된 파티션을 S3로 내보내기

`s3` 프로토콜은 **쓰기(writable)와 읽기(readable) 외부 테이블을 별개 객체로 정의**해야 합니다(하나의 external table이 read/write를 겸하지 않음). 절차는 다음과 같습니다.

```sql
-- ① 부모에서 분리 (테이블 자체는 그대로 유지)
ALTER TABLE sales DETACH PARTITION sales_2024_01;

-- ② S3에 쓰기 위한 writable external table 정의
CREATE WRITABLE EXTERNAL TABLE sales_2024_01_wex (LIKE sales_2024_01)
  LOCATION('s3://s3-us-west-2.amazonaws.com/my-warehouse-archive/sales/2024_01/ config=/home/gpadmin/aws_s3/s3.conf')
  FORMAT 'csv';

-- ③ 각 세그먼트가 병렬로 S3에 파일 업로드 (세그먼트당 1개 파일 생성)
INSERT INTO sales_2024_01_wex SELECT * FROM sales_2024_01;

-- ④ 검증 후 로컬 파티션 드롭 (디스크 회수)
DROP TABLE sales_2024_01;
DROP EXTERNAL TABLE sales_2024_01_wex;  -- 쓰기 완료 후에는 정의 삭제(파일은 S3에 남음)
```

> `INSERT`만 지원됩니다(`UPDATE`/`DELETE`/`TRUNCATE` 불가). 같은 prefix에 재실행하면 파일이 추가로 쌓이므로, 파티션당 **prefix를 고유하게** 유지하세요.

## 3. S3에 있는 데이터를 다시 부모 테이블에 파티션으로 연결하기

이번엔 **readable** external table을 만들어 부모에 ATTACH 합니다 (DETACH → ATTACH 교체 패턴은 동일).

```sql
-- ① S3에 쌓인 파일을 가리키는 읽기 전용 external table
CREATE READABLE EXTERNAL TABLE sales_2024_01_cold (LIKE sales)
  LOCATION('s3://s3-us-west-2.amazonaws.com/my-warehouse-archive/sales/2024_01/ config=/home/gpadmin/aws_s3/s3.conf')
  FORMAT 'csv';

-- ② 부모 파티션 테이블에 다시 연결
ALTER TABLE sales ATTACH PARTITION sales_2024_01_cold
  FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');
```

이제 `sales`를 조회하면 로컬 heap 파티션(핫 데이터)과 S3 external table 파티션(콜드 데이터)이 하나의 쿼리로 함께 조회됩니다. 각 세그먼트가 S3의 해당 prefix 파일들을 병렬로 읽습니다.

## 병렬성 팁

읽기 성능을 위해 **prefix당 세그먼트 수만큼 파일을 나눠서** 저장하는 것이 좋습니다. 세그먼트가 16개인데 파일이 1~2개뿐이면 1~2개 세그먼트만 데이터를 읽어오게 됩니다. 위 예시처럼 `INSERT INTO ... SELECT * FROM <local_partition>`을 사용하면 세그먼트별로 자동으로 파일 하나씩 생성되어 병렬 업로드/다운로드가 자연히 맞춰집니다.

## 주의할 제약사항

- **writable s3 external table은 `INSERT`만 지원**합니다. `UPDATE`/`DELETE`/`TRUNCATE`는 불가능합니다.
- **하나의 external table 정의는 read 또는 write 중 하나의 방향으로만** 사용됩니다. 내보낼 때와 다시 붙일 때 별도의 CREATE EXTERNAL TABLE 문이 필요합니다.
- s3 prefix는 와일드카드를 지원하지 않지만, prefix 매칭 방식으로 동작하므로 **파티션마다 겹치지 않는 고유 prefix**를 사용해야 합니다 (예: `abc`, `abcdef`처럼 앞부분이 겹치는 prefix는 의도치 않게 여러 파티션 파일을 함께 선택할 수 있음).
- 외부 테이블은 **path-style S3 URL만 지원**하며 virtual-hosted style URL은 지원하지 않습니다.
- external/foreign leaf 파티션 공통 제약(WarehousePG 파티셔닝 문서 기준): 쓰기 불가한 파티션에 대한 `INSERT/DELETE/UPDATE/TRUNCATE`는 오류, `VACUUM`은 스킵, `COPY`로 데이터를 복사하려면 `IGNORE EXTERNAL PARTITIONS` 절이 필요합니다.
- `gpbackup`은 leaf 파티션이 external/foreign 테이블이면 그 데이터를 백업하지 않으므로, S3 원본에 대한 별도 백업/보존 정책이 필요합니다.
- Amazon S3 호환 스토리지(Dell ECS 등)를 쓰려면 설정 파일에서 `version = 2`로 지정하고 `LOCATION`에 `region` 파라미터를 추가해야 합니다.

## 참고: PXF `s3://` protocol과의 차이

WarehousePG는 네이티브 `s3://` 프로토콜 외에 **`pxf://` 프로토콜**로도 S3(및 GCS, Azure, MinIO 등)에 접근할 수 있습니다. `pxf`는 Parquet/ORC/Avro 등 다양한 포맷과 filter pushdown을 지원하고 Amazon 외 오브젝트 스토어도 지원하는 반면, 네이티브 `s3` 프로토콜은 텍스트/CSV 위주이고 설정이 더 단순합니다. 이미 PXF 인프라를 운영 중이거나 Parquet 등 컬럼형 포맷이 필요하면 `pxf://`를, 별도 서비스 설치 없이 가볍게 텍스트/CSV만 다루면 되는 경우엔 네이티브 `s3://`를 선택하는 것이 일반적입니다.

## 참고 자료

- [s3:// Protocol (WarehousePG)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/external/external-tables/s3-protocol/)
- [Working with External Data (WarehousePG)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/external/)
- [Partitioning Large Tables (WarehousePG)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/ddl/ddl-partition)
- [pxf:// Protocol overview](https://www.enterprisedb.com/docs/pxf/latest/overview/)
