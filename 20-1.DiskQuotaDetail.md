# WHPG 7 diskquota 2.4 Cluster / Database Quota 사용 가이드

## 1. 개요

`diskquota`는 WarehousePG에서 디스크 사용량을 제한하기 위한 extension이다. 기존 schema, role, tablespace 단위 quota에 더해 diskquota 2.4에서는 다음 범위의 quota를 설정할 수 있다.

| 구분 | 설명 |
|---|---|
| Cluster quota | `diskquota` extension이 설치된 database들의 전체 사용량 합계를 제한 |
| Database quota | 특정 database 하나의 전체 사용량을 제한 |

quota를 초과하면 새로운 쓰기 작업이 차단된다. 필요 시 hard limit을 활성화하여 실행 중인 대량 작업도 quota 초과 시 중단할 수 있다.

## 2. 사전 조건

아래 조건을 만족해야 한다.

| 항목 | 내용 |
|---|---|
| DB | WarehousePG 7 |
| Extension | diskquota 2.4 |
| 권한 | WHPG superuser 또는 동등 권한 |
| 설치 범위 | Coordinator, Standby Coordinator, 모든 Segment 노드 |

> 주의:
> `shared_preload_libraries` 변경과 `diskquota.hard_limit` 변경은 클러스터 재시작이 필요하다. 운영 환경에서는 작업 시간을 확보한 뒤 수행한다.

## 3. 패키지 설치

모든 노드에서 OS 버전에 맞는 RPM을 설치한다.

EL9 예시:

```bash
dnf install -y ./edb-whpg7-diskquota-2.4.0-0.0snapshot33710800552.42.1.2a06b56.1.el9.x86_64.rpm
```

EL8 예시:

```bash
dnf install -y ./edb-whpg7-diskquota-2.4.0-0.0snapshot33710800552.42.1.2a06b56.1.el8.x86_64.rpm
```

설치 확인:

```bash
rpm -qa | grep -i diskquota
```

## 4. 클러스터 설정

### 4.1 기존 shared_preload_libraries 확인

Coordinator에서 현재 설정을 확인한다.

```bash
gpconfig -s shared_preload_libraries
```

기존에 설정된 library가 있으면 삭제하지 말고 `diskquota-2.4.so`를 함께 추가한다.

예시:

```bash
gpconfig -c shared_preload_libraries -v 'pg_stat_statements,pgaa,pgfs,diskquota-2.4.so'
```

`diskquota`만 설정하는 신규 환경 예시:

```bash
gpconfig -c shared_preload_libraries -v 'diskquota-2.4.so'
```

### 4.2 diskquota database 생성

diskquota launcher가 사용할 `diskquota` database를 생성한다.

```bash
create database diskquota;
```

이미 생성되어 있는지 먼저 확인하려면 다음 SQL을 사용한다.

```bash
psql -d postgres -Atc "SELECT datname FROM pg_database WHERE datname = 'diskquota';"
```

> 주의:
> `diskquota` database가 없으면 업무 database에서 `CREATE EXTENSION diskquota;` 실행 시 `no response from diskquota launcher` 오류가 발생할 수 있다. `diskquota` database를 생성한 뒤 클러스터를 재시작해야 한다.

### 4.3 naptime 설정

`diskquota.naptime`은 background worker가 사용량을 다시 계산하는 주기이다. 단위는 초이다.

```bash
gpconfig -c diskquota.naptime -v 2
```

운영 환경에서는 너무 짧게 설정하면 사용량 집계 빈도가 증가한다. 빠른 반응이 필요한 환경은 낮게, 부하를 줄이고 싶은 환경은 높게 설정한다.

### 4.4 hard limit 설정

기본 운영은 hard limit을 끈 상태에서 시작하는 것을 권장한다.

```bash
gpconfig -c diskquota.hard_limit -v off
```

실행 중인 대량 쿼리까지 quota 초과 시 중단해야 하는 경우에는 hard limit을 켠다.

```bash
gpconfig -c diskquota.hard_limit -v on
```

### 4.5 클러스터 재시작

설정 적용을 위해 클러스터를 재시작한다.

```bash
gpstop -ra
```

재시작 후 설정을 확인한다.

```bash
gpconfig -s shared_preload_libraries
gpconfig -s diskquota.naptime
gpconfig -s diskquota.hard_limit
```

## 5. Database별 Extension 생성

quota를 적용하거나 cluster quota 집계에 포함할 database마다 `diskquota` extension을 생성한다.

예시:

```bash
psql -d <DB_NAME> -c 'CREATE EXTENSION IF NOT EXISTS diskquota;'
```

여러 database에 적용하는 예시:

```bash
psql -d salesdb -c 'CREATE EXTENSION IF NOT EXISTS diskquota;'
psql -d martdb -c 'CREATE EXTENSION IF NOT EXISTS diskquota;'
```

설치 확인:

```bash
psql -d <DB_NAME> -c '\dx diskquota'
```

상태 확인:

```bash
psql -d <DB_NAME> -c 'SELECT diskquota.status();'
```

실행 결과:

```text
("soft limits",on)
("hard limits",off)
("current binary version",2.4.0)
("current schema version",2.4)
```

> 참고:
> `diskquota` extension을 생성하지 않은 database는 cluster quota 사용량 집계에 포함되지 않는다. 또한 해당 database에서는 `diskquota.show_database_quota_view` 같은 view/function을 조회할 수 없다.

## 6. Cluster Quota 설정

cluster quota는 `diskquota` extension이 설치된 database들의 전체 사용량 합계를 제한한다.

```sql
SELECT diskquota.set_cluster_quota('500 GB');
```

설정 확인:

```sql
SELECT * FROM diskquota.show_cluster_quota_view;
```

예시 결과:

```text
 quota_in_mb | total_size_in_bytes
-------------+---------------------
      512000 |           109248512
```

quota를 변경하려면 같은 함수를 새 값으로 다시 실행한다.

```sql
SELECT diskquota.set_cluster_quota('1 TB');
```

quota를 제거하려면 `'-1 MB'`를 설정한다.

```sql
SELECT diskquota.set_cluster_quota('-1 MB');
```

cluster quota 설정 시 extension이 설치되지 않은 database가 있으면 다음과 같은 WARNING이 출력될 수 있다.

```text
WARNING:  cluster quota counts monitored databases only; not monitored: gpadmin, quota_unmonitored
HINT:     run CREATE EXTENSION diskquota in those databases to include them
```

이 메시지는 오류가 아니다. 표시된 database를 cluster quota 집계에 포함하려면 해당 database에 접속해서 `CREATE EXTENSION diskquota;`를 실행한다.

## 7. Database Quota 설정

database quota는 특정 database 하나의 전체 사용량을 제한한다.

```sql
SELECT diskquota.set_database_quota('<DB_NAME>', '100 GB');
```

예시:

```sql
SELECT diskquota.set_database_quota('salesdb', '100 GB');
```

설정 확인:

```sql
SELECT * FROM diskquota.show_database_quota_view
ORDER BY database_name;
```

quota를 변경하려면 같은 함수를 새 값으로 다시 실행한다.

```sql
SELECT diskquota.set_database_quota('salesdb', '200 GB');
```

quota를 제거하려면 `'-1 MB'`를 설정한다.

```sql
SELECT diskquota.set_database_quota('salesdb', '-1 MB');
```

> 중요:
> `diskquota.set_database_quota()`는 quota를 설정할 대상 database에 접속한 상태에서 실행해야 한다. 예를 들어 `martdb` quota를 설정하려면 `martdb`에 접속해서 실행한다.

```bash
psql -d martdb
```

```sql
SELECT diskquota.set_database_quota('martdb', '300 GB');
```

다른 database에 접속한 상태에서 실행하면 다음 오류가 발생한다.

```text
ERROR:  set_database_quota() must be called from the target database
HINT:   connect to "martdb" and run it there
```

## 8. Soft Limit과 Hard Limit

### 8.1 Soft limit

soft limit은 기본으로 켜져 있다. diskquota worker가 database 또는 cluster 사용량이 quota를 초과했다고 판단하면, 이후 새로 시작되는 `INSERT`, `UPDATE`, `COPY` 같은 쓰기 작업을 차단한다.

예시:

```text
ERROR:  database's disk space quota exceeded with name: salesdb
HINT:   database usage 152 MB exceeds database quota 50 MB; free disk space or raise diskquota.set_database_quota()
```

cluster quota 초과 예시:

```text
ERROR:  cluster's disk space quota exceeded
HINT:   cluster usage 104 MB exceeds cluster quota 50 MB; free disk space or raise diskquota.set_cluster_quota()
```

### 8.2 Hard limit

hard limit은 실행 중인 쿼리가 quota를 초과하는 경우 해당 쿼리를 중단하기 위한 설정이다. 대량 `INSERT`, `COPY`, `CREATE TABLE AS SELECT`처럼 한 번에 많은 데이터를 생성하는 작업을 더 강하게 제한할 때 사용한다.

hard limit 활성화:

```bash
gpconfig -c diskquota.hard_limit -v on
gpstop -ra
```

hard limit 비활성화:

```bash
gpconfig -c diskquota.hard_limit -v off
gpstop -ra
```

| 구분 | Soft limit | Hard limit |
|---|---|---|
| 기본값 | on | off |
| 차단 시점 | quota 초과 감지 후 새 쓰기 작업 시작 전 | 실행 중 quota 초과 시 |
| 주요 대상 | 일반 DML, COPY 시작 차단 | 대량 적재, CTAS 등 실행 중 차단 |
| 재시작 필요 | 별도 설정 없음 | 설정 변경 후 재시작 필요 |

## 9. 사용 예시

### 9.1 테스트 테이블 생성

```sql
CREATE TABLE quota_fill (
  id bigint,
  payload text
)
DISTRIBUTED BY (id);
```

### 9.2 database quota 설정

```sql
SELECT diskquota.set_database_quota('salesdb', '50 MB');
```

### 9.3 데이터 입력

```sql
INSERT INTO quota_fill
SELECT gs, repeat('x', 1024)
FROM generate_series(1, 60000) AS gs;
```

### 9.4 worker 집계 대기

```sql
SELECT diskquota.wait_for_worker_new_epoch();
```

### 9.5 사용량 확인

```sql
SELECT *
FROM diskquota.show_database_quota_view;
```

### 9.6 quota 초과 확인

quota 초과 상태에서 추가 쓰기를 실행한다.

```sql
INSERT INTO quota_fill
SELECT gs, repeat('y', 1024)
FROM generate_series(60001, 61000) AS gs;
```

예상 결과:

```text
ERROR:  database's disk space quota exceeded with name: salesdb
```

### 9.7 공간 해제 후 복구

공간을 해제한 뒤 worker가 다시 집계하면 쓰기 작업이 허용된다.

```sql
TRUNCATE TABLE quota_fill;
SELECT diskquota.wait_for_worker_new_epoch();
```

또는 quota 값을 상향한다.

```sql
SELECT diskquota.set_database_quota('salesdb', '200 MB');
SELECT diskquota.wait_for_worker_new_epoch();
```

## 10. 주요 함수와 View

### 10.1 관리 함수

| 함수 | 설명 | 실행 위치 |
|---|---|---|
| `diskquota.set_cluster_quota('size')` | cluster quota를 설정한다. `diskquota` extension이 설치된 DB들의 합산 사용량을 제한한다. | extension이 설치된 임의의 DB |
| `diskquota.set_database_quota('db', 'size')` | 특정 database quota를 설정한다. | 대상 DB |
| `diskquota.init_table_size_table()` | extension 생성 전에 이미 존재하던 테이블 크기를 diskquota 내부 집계 테이블에 초기 반영한다. | 대상 DB |
| `diskquota.wait_for_worker_new_epoch()` | background worker가 다음 집계 주기를 완료할 때까지 기다린다. | 대상 DB |
| `diskquota.pause()` | quota enforcement를 일시 중지한다. | 대상 DB |
| `diskquota.resume()` | 일시 중지된 quota enforcement를 다시 활성화한다. | 대상 DB |
| `diskquota.status()` | soft/hard limit 상태, binary/schema version을 확인한다. | 대상 DB |

### 10.2 조회 View

| View | 설명 |
|---|---|
| `diskquota.show_cluster_quota_view` | cluster quota 설정값과 현재 집계된 전체 사용량을 보여준다. |
| `diskquota.show_database_quota_view` | 현재 database의 database quota 설정값과 집계된 사용량을 보여준다. |

### 10.3 GUC 설정

| 설정 | 설명 | 적용 |
|---|---|---|
| `shared_preload_libraries` | `diskquota-2.4.so` library를 preload한다. | 재시작 필요 |
| `diskquota.naptime` | diskquota worker의 사용량 재계산 주기이다. 단위는 초이다. | 재시작 필요 |
| `diskquota.hard_limit` | 실행 중 quota 초과 쿼리 중단 여부를 설정한다. | 재시작 필요 |

## 11. 함수별 설명

### `diskquota.wait_for_worker_new_epoch()`

diskquota는 쓰기 작업이 발생할 때마다 즉시 전체 테이블 크기를 다시 계산하지 않는다. background worker가 `diskquota.naptime` 주기마다 사용량을 계산하고 quota view를 갱신한다.

`diskquota.wait_for_worker_new_epoch()`는 worker가 다음 집계 주기를 완료할 때까지 기다리는 함수이다. 대량 `INSERT`, `COPY`, `TRUNCATE`, `DROP`, quota 변경 직후 정확한 사용량을 확인하거나 quota 초과 여부를 테스트할 때 사용한다.

일반적인 확인 흐름:

```sql
INSERT INTO quota_fill
SELECT gs, repeat('x', 1024)
FROM generate_series(1, 60000) AS gs;

SELECT diskquota.wait_for_worker_new_epoch();

SELECT * FROM diskquota.show_database_quota_view;
```

### `diskquota.init_table_size_table()`

`CREATE EXTENSION diskquota;` 전에 이미 데이터가 있던 database에서는 기존 테이블 크기를 초기 집계에 반영해야 한다. 이때 `diskquota.init_table_size_table()`을 실행한다.

```sql
CREATE EXTENSION IF NOT EXISTS diskquota;
SELECT diskquota.init_table_size_table();
SELECT diskquota.wait_for_worker_new_epoch();
```

신규 database처럼 extension 생성 전 데이터가 없는 경우에는 생략할 수 있다.

### `diskquota.pause()` / `diskquota.resume()`

일시적인 대량 적재나 관리 작업 중 quota enforcement를 잠시 멈출 때 사용한다.

```sql
SELECT diskquota.pause();
```

작업 완료 후 다시 활성화한다.

```sql
SELECT diskquota.resume();
SELECT diskquota.wait_for_worker_new_epoch();
```

> 주의:
> `pause()` 상태에서는 quota 초과 상태에서도 쓰기가 허용될 수 있다. 작업이 끝나면 반드시 `resume()`을 실행한다.

### `diskquota.status()`

현재 diskquota 동작 상태와 version을 확인한다.

```sql
SELECT diskquota.status();
```

예시:

```text
("soft limits",on)
("hard limits",off)
("current binary version",2.4.0)
("current schema version",2.4)
```

## 12. Quota 크기 단위

quota 크기는 문자열로 지정한다.

```sql
SELECT diskquota.set_database_quota('salesdb', '500 MB');
SELECT diskquota.set_database_quota('salesdb', '100 GB');
SELECT diskquota.set_cluster_quota('2 TB');
```

quota 제거는 `'-1 MB'`를 사용한다.

```sql
SELECT diskquota.set_database_quota('salesdb', '-1 MB');
SELECT diskquota.set_cluster_quota('-1 MB');
```

## 13. 운영 시 확인 포인트

quota 설정 후에는 다음 순서로 확인한다.

```sql
SELECT diskquota.wait_for_worker_new_epoch();
SELECT diskquota.status();
SELECT * FROM diskquota.show_database_quota_view ORDER BY database_name;
SELECT * FROM diskquota.show_cluster_quota_view;
```

주요 확인 항목:

| 항목 | 확인 내용 |
|---|---|
| `quota_in_mb` | 설정한 quota가 MB 단위로 반영되었는지 확인 |
| `total_size_in_bytes` | 현재 집계된 사용량 확인 |
| `status()` | soft limit, hard limit 상태 확인 |
| WARNING | cluster quota 집계에서 제외된 database가 있는지 확인 |

`quota_in_mb`가 `0`으로 보이면 명시적인 quota가 설정되지 않은 상태로 볼 수 있다. 필요한 경우 `set_cluster_quota()` 또는 `set_database_quota()`를 실행한다.

## 14. 비활성화 및 정리

database quota 제거:

```sql
SELECT diskquota.set_database_quota('<DB_NAME>', '-1 MB');
```

cluster quota 제거:

```sql
SELECT diskquota.set_cluster_quota('-1 MB');
```

특정 database에서 extension을 제거하려면 quota와 의존성을 확인한 뒤 수행한다.

```sql
DROP EXTENSION diskquota;
```

클러스터에서 diskquota를 완전히 사용하지 않으려면 `shared_preload_libraries`에서 `diskquota-2.4.so`를 제거하고 재시작한다. 기존에 설정된 다른 library를 함께 제거하지 않도록 현재 값을 먼저 확인한다.

```bash
gpconfig -s shared_preload_libraries
gpconfig -c shared_preload_libraries -v '<EXISTING_LIBRARY_LIST_WITHOUT_DISKQUOTA>'
gpstop -ra
```
