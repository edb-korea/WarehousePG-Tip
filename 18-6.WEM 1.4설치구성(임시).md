# WarehousePG Enterprise Manager(WEM) 1.4 설치 및 구성 가이드

> 본 문서는 EDB 공식 문서(docs.enterprisedb.com)의 WarehousePG Enterprise Manager 섹션을 기준으로 작성되었습니다.
> WEM 최신 안정 버전은 **1.4.0**(2026-09-21 릴리스)입니다.

## 1. 개요

WarehousePG Enterprise Manager(WEM)는 WarehousePG(WHPG) 클러스터를 모니터링하고 관리하기 위한 통합 관리 플랫폼입니다. 실시간/이력 텔레메트리, AI 기반 쿼리 분석, 대화형 설정 기능을 하나의 웹 콘솔에서 제공하여 분산 데이터베이스 운영을 단순화합니다.

WEM은 다음 구성 요소로 이루어집니다.

- **WEM 서비스**: 웹 콘솔 및 내부 작업 실행 엔진(관리 작업 디스패치, 알림 평가 등)
- **ClickHouse**: 이력 메트릭·로그 데이터 저장소
- **PostgreSQL(선택)**: WEM 자체 애플리케이션 상태(대시보드, 알림 규칙, 사용자 계정) 저장소
- **호스트 에이전트(`acp-host-agent`)**: 클러스터의 모든 노드에 설치되어 PXF 제어, 백업/복구, HBA 편집 등 코디네이터 셸 접근 없이 수행해야 하는 권한 작업을 처리
- **OTel Collector(`edb-otelcol`)**: 각 노드의 메트릭/로그를 수집해 ClickHouse로 전달
- **게이트웨이 컬렉터(선택)**: 여러 시스템/클러스터가 하나의 ClickHouse 인스턴스를 공유할 때만 필요

## 2. 배포 토폴로지 결정

설치 전에 ClickHouse와 WEM을 어디에 둘지 먼저 결정해야 합니다. 이 선택에 따라 이후 설치 단계와 설정 값이 달라집니다.

| 토폴로지 | 설명 |
|---|---|
| **WHPG 클러스터 내부에 WEM/ClickHouse 배치** | 일반적으로 스탠바이 코디네이터에 설치(스탠바이가 없으면 코디네이터에 설치). 단일 클러스터에 가장 단순한 구성. 해당 노드는 반드시 RHEL 8 또는 RHEL 9여야 합니다(WEM은 RHEL 7을 지원하지 않음). |
| **전용 호스트(클러스터 외부)에 WEM/ClickHouse 배치** | 관측 구성 요소를 클러스터 노드와 분리하고 싶을 때 사용 |

- ClickHouse는 WEM과 반드시 같은 호스트에 있을 필요는 없으며, 이미 운영 중인 ClickHouse 인스턴스를 그대로 사용해도 됩니다(모든 클러스터 노드의 OTel Collector 또는 게이트웨이 컬렉터에서 접근 가능해야 함).
- 게이트웨이 컬렉터는 **여러 시스템/클러스터가 동일 ClickHouse를 공유**할 때만 설치합니다. 단일 WHPG 클러스터가 ClickHouse로 직접 전송하는 구성이면 필요 없습니다(WEM이 자동으로 처리).

## 3. 사전 요구 사항

- WHPG 6.x는 RHEL 7/8/9, WHPG 7.x는 RHEL 8/9에서 실행 중이어야 합니다.
- WEM이 실행되는 호스트는 RHEL 8 또는 RHEL 9여야 합니다(단독 호스트든 클러스터 내부 호스트든 동일). WHPG 6.x를 RHEL 7에서 운영 중이라면 WEM은 반드시 RHEL 8 이상의 별도 호스트에서 실행해야 합니다.
- WEM 호스트에 `gpadmin` OS 사용자가 있어야 합니다. WHPG가 이미 설치된 호스트에는 자동으로 존재하며, 별도 호스트라면 다음과 같이 생성합니다.

    ```bash
    sudo useradd -r -m -d /home/gpadmin -s /bin/bash gpadmin
    ```

- WEM 호스트와 각 노드의 OTel Collector/게이트웨이 컬렉터에서 접근 가능한 **ClickHouse** 인스턴스가 필요합니다.
- WHPG 클러스터에 슈퍼유저 권한으로 WEM 호스트에서 접속 가능한 데이터베이스 사용자가 필요합니다(필요 시 `pg_hba.conf`에 WEM 호스트 접근 규칙 추가).
- **백업/복원 패널**을 사용하려면 모든 클러스터 노드에 `whpg-backup` **1.34 이상**이 설치되어 있어야 합니다(1.33은 백업 생성/스케줄만 가능, 목록 조회·복원·삭제는 1.34부터 지원).
- 모든 클러스터 노드에 **호스트 에이전트(`acp-host-agent`) 1.1.0 이상**이 설치되어 있어야 합니다. 그보다 낮은 버전은 WEM 1.4에 연결할 수 없습니다.
- (선택) 쿼리 작성/최적화용 AI Assistant를 사용하려면 Anthropic 계정과 유효한 API 키가 필요합니다.

### 네트워크 요구 사항

| 출발지 | 목적지 | 용도 |
|---|---|---|
| 모든 클러스터 노드 | WEM:8081 | 호스트 에이전트의 권한 작업 요청 및 자체 등록(mTLS 인증, WEM 기본 포트+1) |
| 각 노드의 OTel Collector | 게이트웨이 컬렉터:4317 | 호스트 메트릭/로그/트레이스 전달(OTLP over gRPC) |
| 게이트웨이 컬렉터 | ClickHouse:9000 | 텔레메트리 데이터 기록(네이티브 프로토콜) |
| WEM | ClickHouse:9000 | 메트릭 수집 |
| WEM | WEM:9093 | Alertmanager로 알림 전달(루프백, 외부 네트워크 불필요) |
| WEM | WEM:7233 | Temporal 기반 내부 워크플로 오케스트레이션(루프백, 외부 네트워크 불필요) |
| 최종 사용자 | WEM:8080 | WEM 콘솔 접속 |

## 4. 설치 순서

전체 설치는 다음 순서로 진행합니다.

1. 사전 요구 사항 확인 (위 2, 3절)
2. ClickHouse, (선택) PostgreSQL 설치
3. (선택) 게이트웨이 컬렉터 설치
4. WEM 설치 및 구성
5. 모든 클러스터 노드에 호스트 에이전트 설치

## 5. ClickHouse 설치

```bash
export EDB_SUBSCRIPTION_TOKEN=<your-token>
export EDB_REPO=clickhouse
curl -1sSLf "https://downloads.enterprisedb.com/$EDB_SUBSCRIPTION_TOKEN/$EDB_REPO/setup.rpm.sh" | sudo -E bash
sudo yum install -y edb-clickhouse-server edb-clickhouse-client
```

`/etc/clickhouse-server/config.d/` 아래에 드롭인 설정 파일을 추가합니다(런타임 병합은 시작 시점에만 적용되므로 설정 후 재시작 필요).

```bash
sudo mkdir -p /etc/clickhouse-server/config.d
sudo tee /etc/clickhouse-server/config.d/observability.xml << 'EOF'
<clickhouse>
    <!-- 모든 인터페이스에서 수신, 또는 WEM 호스트 IP로 제한 -->
    <listen_host>0.0.0.0</listen_host>

    <!-- 기본값 8123. 변경이 필요할 때만 주석 해제 -->
    <!-- <http_port>8123</http_port> -->

    <!-- 기본값 9000. 변경이 필요할 때만 주석 해제 -->
    <!-- <tcp_port>9000</tcp_port> -->

    <logger>
        <level>information</level>
    </logger>
</clickhouse>
EOF
```

기본 사용자 비밀번호를 설정합니다.

```bash
sudo tee /etc/clickhouse-server/users.d/default-password.xml << 'EOF'
<clickhouse>
    <users>
        <default>
            <password><your-password></password>
        </default>
    </users>
</clickhouse>
EOF
```

ClickHouse를 시작하고 확인합니다.

```bash
sudo systemctl daemon-reload
sudo clickhouse start
sudo clickhouse status
clickhouse-client --password "<your-password>" --query "SELECT 1"

# 모든 인터페이스 리슨 확인
grep -A1 listen_host /var/lib/clickhouse/preprocessed_configs/config.xml
ss -tlnp | grep -E ':8123|:9000'

# 다른 노드에서 접근 테스트
curl "http://<clickhouse-host>:8123/?query=SELECT%201"
```

### (선택) PostgreSQL 설치

WEM 자체 상태(대시보드, 알림 규칙, 사용자 계정)를 저장할 PostgreSQL이 필요합니다. WHPG 클러스터의 Postgres 서버를 재사용할 수도 있지만, 별도 인스턴스를 두면 모니터링 대상 클러스터가 다운되어도 WEM 상태는 유지됩니다.

```sql
CREATE ROLE wem_admin WITH LOGIN PASSWORD '<your-password>' CREATEDB;
```

```
host    all    wem_admin    127.0.0.1/32    scram-sha-256
host    all    wem_admin    ::1/128         scram-sha-256
```

> PostgreSQL 13 이하는 `password_encryption` 기본값이 `md5`이므로, 위 `scram-sha-256` 인증을 쓰려면 먼저 `ALTER SYSTEM SET password_encryption = 'scram-sha-256';` 후 `pg_reload_conf()`를 실행해야 합니다.

## 6. (선택) 게이트웨이 컬렉터 설치

여러 시스템/클러스터가 동일 ClickHouse를 공유하는 경우에만 설치합니다.

```bash
export EDB_SUBSCRIPTION_TOKEN=<your-token>
export EDB_REPO=gpsupp
curl -1sSLf "https://downloads.enterprisedb.com/$EDB_SUBSCRIPTION_TOKEN/$EDB_REPO/setup.rpm.sh" | sudo -E bash
sudo yum install -y edb-otelcol
```

`/etc/edb/otelcol/config.yaml`을 아래 내용으로 교체합니다(`default`/`<password>`/`localhost`를 환경에 맞게 수정).

```yaml
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4317"
      http:
        endpoint: "0.0.0.0:4318"

exporters:
  clickhouse:
    endpoint: "clickhouse://default:<password>@localhost:9000"
    database: acp_observability
    timeout: 5s
    retry_on_failure:
      enabled: true
      initial_interval: 5s
      max_interval: 30s
      max_elapsed_time: 300s

processors:
  batch:
    timeout: 5s
    send_batch_size: 8192

service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [clickhouse]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [clickhouse]
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [clickhouse]
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable edb-otelcol
sudo systemctl start edb-otelcol
sudo systemctl status edb-otelcol

ss -tlnp | grep -E '4317|4318'
journalctl -u edb-otelcol -n 20 --no-pager
```

## 7. WEM 설치

### 7.1 패키지 설치

```bash
export EDB_SUBSCRIPTION_TOKEN=<your-token>
export EDB_REPO=gpsupp
curl -1sSLf "https://downloads.enterprisedb.com/$EDB_SUBSCRIPTION_TOKEN/$EDB_REPO/setup.rpm.sh" | sudo -E bash
sudo dnf install -y whpg-enterprise-manager
```

`wem` CLI(`setup`, `doctor` 등)를 어디서든 실행하려면 `PATH`에 추가합니다.

```bash
export PATH=/usr/local/greenplum-db/wem:$PATH
```

(WHPG 7.4/6.27.4 이상은 `/usr/local/greenplum-db`가 `/usr/edb/whpg7`로의 심볼릭 링크로 제공되므로 별도 조치 불필요)

### 7.2 `/etc/wem/wem.conf` 구성

**대상 클러스터**

```ini
WHPG_HOST=whpg-coordinator-host
WHPG_PORT=5432
WHPG_DATABASE=postgres
WHPG_USER=gpadmin
WHPG_PASSWORD=postgres
```

`WHPG_USER`는 반드시 슈퍼유저여야 합니다. WEM은 설치 시 전용 `WHPG_WEM_USER` 역할을 자동 생성해 런타임에는 이 계정을 사용합니다.

**ClickHouse**

```ini
CLICKHOUSE_URL=clickhouse://username:password@<clickhouse-host>:9000
```

> `<clickhouse-host>`에 `localhost`를 쓰지 마세요. 모든 WHPG 노드가 이 주소로 직접 접속하므로, 실제 ClickHouse 호스트명/IP를 사용해야 합니다(ClickHouse가 WEM과 같은 호스트에서 실행 중이더라도 동일).

여러 클러스터가 ClickHouse를 공유하는 경우에만 `OTEL_GATEWAY_ENDPOINT`를 설정합니다(단일 클러스터 구성이면 비워 둠).

```ini
OTEL_GATEWAY_ENDPOINT=<gateway-host>:4317
```

**애플리케이션 상태(WEM 자체 DB)**

```ini
WEM_HOST=wem-db-hostname
WEM_PORT=5432
WEM_DATABASE=wem
WEM_USER=wem_admin
WEM_PASSWORD=<your-password>
```

- `WEM_HOST`를 비워 두면 `WHPG_HOST`로 대체되지 않고 `localhost`가 기본값이 됩니다.
- `WEM_DATABASE`는 첫 시작 시 자동 생성되지만, `WEM_USER` 역할은 미리 존재해야 합니다.
- `WEM_DATABASE`를 모니터링 대상(`WHPG_DATABASE`)과 같게 두면 **Cluster Management 패널**(시작/중지/재시작/세그먼트 복구)이 숨겨집니다(해당 클러스터를 중지하면 WEM 자체 DB도 함께 내려가는 것을 막기 위한 안전장치).

**포털 포트(기본 8080)**

```ini
PORT=8082
```

**세션 보안(필수)**

```ini
WEM_COOKIE_SECRET=your-generated-secret-key
WEM_INSECURE_COOKIES=1   # HTTPS를 사용하지 않는 경우에만 설정
```

키 생성 예시: `openssl rand -base64 32`

**관리자 비밀번호 설정** — 초기 관리자 계정명은 항상 `admin`이며 아래 세 가지 방법 중 하나로 설정합니다.

1. **대화형**: `wem setup` 실행 시 프롬프트에서 직접 입력(설정 파일 수정 불필요)
2. **정적 설정**: `wem.conf`에 직접 지정

    ```ini
    WEM_ADMIN_PASSWORD=your-secure-password
    ```

3. **파일 기반**(자동화 배포에 권장)

    ```bash
    echo "your-secure-password" > /etc/wem/admin.pw
    ```
    ```ini
    WEM_ADMIN_PASSWORD_FILE=/etc/wem/admin.pw
    ```

### 7.3 WEM 초기화 및 시작

```bash
sudo systemctl enable --now wem
```

시작 실패 시 `activating (auto-restart)` 상태가 되며, 대부분 WHPG 접속 불가 또는 `WHPG_PASSWORD` 오류가 원인입니다.

```bash
sudo tail -f /var/log/wem/wem.log
```

수동 진단(설정 파일을 읽지 않고 플래그로 직접 연결 테스트):

```bash
sudo -u wem /usr/local/greenplum-db/wem/wem setup \
  --host whpg-coordinator-host --port 5432 --user gpadmin \
  --password postgres --database postgres --check --debug
```

### 7.4 설치 검증

```bash
sudo systemctl status wem
wem setup --verify
wem doctor
```

### 7.5 WEM 로그인

브라우저에서 `http://<wem-host>:8080` 접속 후 `admin` 계정과 위에서 설정한 비밀번호로 로그인합니다.

## 8. 호스트 에이전트(`acp-host-agent`) 설치

WHPG 클러스터의 **모든 노드**(코디네이터, 스탠바이 코디네이터, 전체 세그먼트 호스트)에 설치합니다. 코디네이터에서 `gpadmin`으로 실행합니다.

```bash
export EDB_SUBSCRIPTION_TOKEN=<your-token>
export EDB_REPO=gpsupp
curl -1sSLf "https://downloads.enterprisedb.com/$EDB_SUBSCRIPTION_TOKEN/$EDB_REPO/setup.rpm.sh" | sudo -E bash
sudo dnf download edb-acp-host-agent edb-otelcol
```

`all_hosts` 파일 작성 후 전체 노드에 패키지 배포·설치:

```ini
cdw
scdw
sdw1
sdw2
sdw3
```

```bash
# WHPG 7: gpsync, WHPG 6: gpscp
gpsync -f all_hosts edb-acp-host-agent-*.rpm edb-otelcol-*.rpm =:/tmp
gpssh -f all_hosts -u gpadmin -e "sudo dnf install -y /tmp/edb-acp-host-agent-*.rpm /tmp/edb-otelcol-*.rpm"
```

모든 노드에서 `WEM_CONNECT_ADDRESS`를 WEM 주소(리슨 포트+1, 기본 8081)로 설정합니다.

```bash
WEM_HOST=<wem-host-ipv4>
gpssh -f all_hosts -u gpadmin -e \
  "sudo sed -i 's/^WEM_CONNECT_ADDRESS=.*/WEM_CONNECT_ADDRESS=${WEM_HOST}:8081/' \
    /etc/edb/acp-host-agent/acp-host-agent.conf"
```

에이전트 서비스 활성화 및 시작:

```bash
gpssh -f all_hosts -u gpadmin -e "sudo systemctl enable --now acp-host-agent"
```

각 에이전트가 자체 등록(self-registration)되었는지 로그로 확인합니다.

```bash
gpssh -f all_hosts -u gpadmin -e "sudo cat /var/log/edb/acp-host-agent/acp-host-agent.log"
```

정상이면 다음과 같은 로그가 보입니다.

```
INFO self-registration succeeded task_queues=coordinator-tasks
INFO starting host-agent agent_id=... connect_address=... task_queues=coordinator-tasks
INFO awaiting TLS material — an admin must approve this agent in wem agent_id=...
```

에이전트는 관리자가 승인하기 전까지 이 대기 상태를 유지합니다(자동으로 실행 상태가 되지 않음).

WEM 콘솔에 로그인 후 **Management > Host Agents**로 이동해 대기 중인 에이전트를 선택하고 **Approve**를 실행합니다. `/etc/edb/acp-host-agent/join-token` 값과 콘솔에 표시된 Verification Token을 비교해 올바른 에이전트인지 확인할 수 있습니다.

> 기본적으로 **Admin** 프로필조차 호스트 에이전트 승인 권한이 없을 수 있습니다. 필요 시 역할 기반 접근 제어(RBAC) 설정에서 권한을 부여하세요.

승인 후에는 짧은 시간 내에 TLS 자재를 발급받아 워커가 시작됩니다.

```bash
gpssh -f all_hosts -u gpadmin -e "sudo tail -5 /var/log/edb/acp-host-agent/acp-host-agent.log"
```

```
INFO handlers registered  task_kinds=[file.pull, file.push, otel.service.start, ...]
INFO connecting to Temporal connect_address=... task_queues=coordinator-tasks
INFO worker running       task_queue=coordinator-tasks
```

이후 모든 통신은 상호 TLS(mTLS)로 인증되며, 인증서는 WEM이 자동 갱신합니다(24시간 유예 구간 지원).

## 9. 설치 후 첫 화면 둘러보기

- **Dashboard**: 클러스터 상태(Uptime, Connections, Last Sync), 세그먼트 정상/비정상 여부, 스토리지 사용률, 실행 중/대기 중/차단된 쿼리, CPU/메모리 사용률, 최근 로그 알림을 한눈에 확인
- 좌측 사이드바 **Panels**: Cluster, Monitoring, Access Management 등 기능별 모듈
- 역할(Role)은 우측 상단에서 확인(Admin / Operator / Viewer) — 역할에 따라 접근 가능한 패널과 작업이 달라짐

## 10. 참고: 자주 발생하는 설치 문제

- **`gpadmin` OS 사용자 없음 오류**: 독립형 WEM 호스트에 `gpadmin`이 없을 때 발생 → `useradd`로 생성 후 재설치
- **WEM 서비스가 `activating (auto-restart)` 상태 유지**: WHPG 접속 불가가 주 원인 → `gpstate`/`gpstart -a`로 클러스터 확인 후 `wem.log` 점검
- **호스트 에이전트가 등록되지 않음**: `WEM_CONNECT_ADDRESS` 오류 또는 포트(8081) 차단 → `curl -k https://<wem-host>:8081/`로 연결 테스트
- **PXF 탭이 "unavailable"로 표시**: 코디네이터의 호스트 에이전트가 응답하지 않거나 코디네이터에 PXF 미설치가 원인

자세한 오류 해결은 [WEM 트러블슈팅 가이드](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/troubleshooting/)를 참고하세요.

---

### 출처

- [Installing and upgrading WarehousePG Enterprise Manager](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/installing/)
- [WarehousePG Enterprise Manager prerequisites](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/installing/prerequisites/)
- [Installing ClickHouse and PostgreSQL (optional)](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/installing/observability-stack/)
- [Installing the gateway collector (optional)](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/installing/collector/)
- [Installing WEM](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/installing/wem/)
- [Installing the host agent and OTel Collector](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/installing/host-agent/)
- [Getting started with WarehousePG Enterprise Manager](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/get-started/)
- [WarehousePG Enterprise Manager release notes](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/release_notes/)
- [Troubleshooting common issues](https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/troubleshooting/)
