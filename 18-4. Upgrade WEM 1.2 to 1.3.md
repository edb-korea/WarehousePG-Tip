# WEM 1.2.1에서 WEM 1.3 업그레이드 가이드

이 문서는 WEM 1.2.1 구성이 완료된 환경에서 WEM 1.3.0 RC2로 업그레이드하는 절차입니다.

> WEM 1.3.0 RC2는 정식 릴리즈 전 버전입니다. 정식 릴리즈 RPM에서는 패키지 구성, 서비스명, 설정 항목이 달라질 수 있으므로 최종 문서 작성 전 재검증이 필요합니다.

> 참고 문서: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/wem/installing/upgrading/

## 0. 환경

| 항목 | 구성 |
| --- | --- |
| OS | Rocky Linux 9.5 |
| WEM Host | scdw-dr / 192.168.10.81 |
| WHPG Coordinator | cdw-dr / 192.168.10.80 |
| WHPG Standby Coordinator | scdw-dr / 192.168.10.81 |
| WHPG Segment Host | sdw1-dr / 192.168.10.82, sdw2-dr / 192.168.10.83 |
| WEM 1.2.1 UI | 192.168.10.81:8080 |
| Prometheus | 192.168.10.81:9090 |
| Loki | 192.168.10.81:3100 |
| WEM 1.2.1 app state DB | 192.168.10.80:5432/wem |
| WEM 1.3 Remote Write Receiver | 192.168.10.81:9091 |

현재 WEM 1.2.1 baseline은 다음 방식으로 구성되어 있다고 가정합니다.

```text
WEM 1.2.1 RPM                  : whpg-enterprise-manager-1.2.1-1.el9.x86_64
WEM 1.2.1 Collector             : edb-whpg-observability-collector-1.2.0-1.el9.x86_64
Prometheus remote write endpoint: http://192.168.10.81:9090/api/v1/write
Loki endpoint                   : http://192.168.10.81:3100/loki/api/v1/push
WHPG monitored DB               : 192.168.10.80:5432/postgres
WEM app state DB                : 192.168.10.80:5432/wem
```

업그레이드 순서:

```text
1. WEM 1.2.1 baseline 상태 확인
2. 업그레이드 전 백업
3. WEM 서비스 중지 후 WEM 1.3 RPM 업그레이드
4. WEM 1.3 설정 추가
5. WEM 1.3 기동 확인
6. Collector 1.4 RPM 업그레이드
7. Collector endpoint를 9091로 변경 후 재배포
8. 업그레이드 후 기능 확인
```

## 1. 업그레이드 파일

WEM 1.3 RC2 업그레이드에 사용하는 RPM은 다음과 같습니다.

```text
whpg-enterprise-manager-1.3.0~rc.2-1.el9.x86_64.rpm
edb-whpg-observability-collector-1.4.0.rc.1-1.el9.x86_64.rpm
```

Safeguard까지 테스트하는 경우 다음 RPM도 사용합니다.

```text
edb-acp-host-agent-1.0.0_rc.2-1.el9.x86_64.rpm
```

WEM 1.3 RC2에서는 Temporal과 Alertmanager가 `whpg-enterprise-manager` RPM에 포함되어 있습니다.

따라서 WEM 1.3 RC2 업그레이드 시 다음 RPM은 별도로 설치하지 않습니다.

```text
edb-alertmanager-0.33.1-1edb1.x86_64.rpm
edb-temporal-1.31.2-1edb1.x86_64.rpm
```

RPM 위치 예시:

```bash
ls -lh /tmp/wem13-rpms
```

## 2. WEM 1.2.1 baseline 확인

`scdw-dr`에서 WEM 1.2.1 서비스 상태를 확인합니다.

```bash
hostname
cat /etc/os-release

rpm -qa | egrep 'whpg-enterprise-manager|edb-whpg-observability|temporal|alertmanager|alloy' || true
sudo systemctl is-active wem prometheus loki
sudo systemctl status wem --no-pager
```

WEM 설정 확인:

```bash
sudo egrep -v 'PASSWORD|SECRET|TOKEN|KEY' /etc/wem/wem.conf
```

Prometheus와 Loki 확인:

```bash
curl http://127.0.0.1:9090/-/healthy
curl http://127.0.0.1:3100/ready
ss -lntp | egrep '8080|9090|3100'

Prometheus Server is Healthy.
ready
LISTEN 0      10000                   *:9090             *:*    users:(("prometheus",pid=1003139,fd=6))
LISTEN 0      10000                   *:3100             *:*    users:(("loki",pid=1003314,fd=7))      
LISTEN 0      10000                   *:8080             *:*    users:(("wem",pid=1003503,fd=12)) 
```

WEM UI 확인:

```text
http://192.168.10.81:8080
```

확인 항목:

```text
로그인 가능
Dashboard 표시
System metrics 표시
Storage metrics 표시
Query monitoring 표시
Loki 로그 조회
```

## 3. WHPG host 및 collector 상태 확인

collector 배포 대상은 수동 hostfile이 아니라 WHPG DB에서 조회되는 cluster host 기준입니다.

`cdw-dr` 또는 WHPG coordinator에서 확인합니다.

```bash
psql -h 192.168.10.80 -p 5432 -U gpadmin -d postgres -c "
select hostname, address, content, role, preferred_role, mode, status
from gp_segment_configuration
order by content, role;
"
```

host 목록을 임시 변수로 만듭니다. 실제 조회 결과에 맞춰 수정합니다.

```bash
export WHPG_HOSTS="cdw-dr scdw-dr sdw1-dr sdw2-dr"
```

`scdw-dr`은 WEM host이면서 WHPG standby coordinator이므로 collector/alloy 확인 및 업그레이드 대상에 포함합니다.

각 host의 collector RPM과 alloy 상태를 확인합니다.

```bash
for h in $WHPG_HOSTS; do
  echo "==== $h ===="
  ssh gpadmin@$h "rpm -qa | grep edb-whpg-observability-collector || true"
  ssh gpadmin@$h "systemctl is-active alloy || true"
done
```

collector 설정은 WHPG coordinator의 `/var/lib/whpg-observability-collector/collector.conf`에서 확인합니다.

```bash
ssh gpadmin@cdw-dr "grep -E 'WHPG_OBS_DSN|LOKI_ENDPOINT|PROMETHEUS_ENDPOINT|PROMETHEUS_BEARER_TOKEN' /var/lib/whpg-observability-collector/collector.conf"

WHPG_OBS_DSN="host=192.168.10.80 port=5432 dbname=postgres user=gpadmin password=gpadmin sslmode=disable"
LOKI_ENDPOINT="http://192.168.10.81:3100/loki/api/v1/push"
PROMETHEUS_ENDPOINT="http://192.168.10.81:9090/api/v1/write"
```

## 4. 업그레이드 전 백업

`scdw-dr`에서 WEM 설정과 로그를 백업합니다.

```bash
sudo mkdir -p /root/wem-upgrade-backup/$(date +%Y%m%d%H%M%S)
BACKUP_DIR=$(ls -td /root/wem-upgrade-backup/* | head -1)

sudo cp -a /etc/wem "$BACKUP_DIR/"
sudo cp -a /var/log/wem "$BACKUP_DIR/" 2>/dev/null || true
sudo systemctl cat wem > "$BACKUP_DIR/wem.service.txt"
rpm -qa | egrep 'whpg-enterprise-manager|edb-whpg-observability|temporal|alertmanager' > "$BACKUP_DIR/rpm-before.txt"
```

## 5. WEM 1.3 RPM 업그레이드

`scdw-dr`에서 WEM 서비스를 중지한 뒤 WEM 1.3 RPM을 설치합니다.

```bash
sudo systemctl stop wem
sudo systemctl status wem --no-pager || true
```

```bash
cd /tmp/wem13-rpms

sudo dnf upgrade -y \
  ./whpg-enterprise-manager-1.3.0~rc.2-1.el9.x86_64.rpm
```

설치 확인:

```bash
rpm -qa | egrep 'whpg-enterprise-manager|edb-temporal|edb-alertmanager|temporal|alertmanager'
systemctl list-unit-files | egrep -i 'wem|temporal|alert'
rpm -ql whpg-enterprise-manager | egrep -i 'temporal|alert|prometheus|wem.conf' | head -200
```

정상 기준:

```text
whpg-enterprise-manager-1.3.0~rc.2-1.el9.x86_64
wem.service만 systemd unit으로 존재
temporal-server, alertmanager는 wem.service child process로 실행
```

## 6. WEM 1.3 설정 반영

`/etc/wem/wem.conf`를 백업한 뒤 1.3 설정을 추가합니다.

```bash
sudo cp -a /etc/wem/wem.conf /etc/wem/wem.conf.before-13-upgrade.$(date +%Y%m%d%H%M%S)
sudo vi /etc/wem/wem.conf
```

기존 WEM 1.2.1 설정은 유지하고, 아래 값을 추가 또는 수정합니다.

WEM 1.3의 app state DB는 운영 방식에 따라 다음 중 하나를 선택합니다.

- 기존 WEM 1.2.1처럼 WHPG coordinator의 `wem` DB를 계속 사용하는 경우: `192.168.10.80:5432/wem`
- WEM app state를 WHPG와 분리해서 별도 PostgreSQL에 저장하는 경우: 예시 `127.0.0.1:5433/wem`

아래 예시는 기존 WEM 1.2.1 구성과 동일하게 WHPG coordinator의 `wem` DB를 계속 사용하는 방식입니다.

```bash
# SECURITY
WEM_INSECURE_COOKIES=1
WEM_COOKIE_SECRET=<existing_or_new_cookie_secret>
WEM_ADMIN_PASSWORD=<admin_password>

# Monitored WHPG Database
WHPG_HOST=192.168.10.80
WHPG_PORT=5432
WHPG_DATABASE=postgres
WHPG_USER=gpadmin
WHPG_PASSWORD=<gpadmin_db_password>

# WEM Application State Database
WEM_HOST=192.168.10.80
WEM_PORT=5432
WEM_DATABASE=wem
WEM_USER=gpadmin
WEM_PASSWORD=<gpadmin_db_password>

# Web
PORT=8080
WEM_AGENT_PORT=8081
HIGH_LOAD_MODE=true

# Observability
PROMETHEUS_URL=http://192.168.10.81:9090
LOKI_URL=http://192.168.10.81:3100
METRICS_BACKEND=prometheus

# Remote Write Receiver
WEM_REMOTE_WRITE_RECEIVER_ENABLED=true
WEM_REMOTE_WRITE_RECEIVER_PORT=9091
WEM_REMOTE_WRITE_RECEIVER_TTL=5m
WEM_REMOTE_WRITE_RECEIVER_TOKEN=<openssl rand -base64 32>

# Agent callback
WEM_AGENT_CALLBACK_BASE_URL=http://192.168.10.81:8080
```

별도 PostgreSQL을 사용하는 경우에는 위 `WEM Application State Database` 부분을 다음처럼 작성합니다.

```bash
# WEM Application State Database
WEM_HOST=127.0.0.1
WEM_PORT=5433
WEM_DATABASE=wem
WEM_USER=wem_admin
WEM_PASSWORD=<wem_admin_password>
```

별도 PostgreSQL을 선택한 경우에는 `wem` database와 `wem_admin` user가 미리 생성되어 있어야 하며, `pg_hba.conf` 인증 방식과 사용자 password encryption 설정이 일치해야 합니다.

`WEM_REMOTE_WRITE_RECEIVER_TOKEN`은 다음 명령으로 생성합니다.

```bash
openssl rand -base64 32
```

중복 설정이 없는지 확인합니다.

```bash
sudo grep -nE '^(WHPG_|WEM_|PORT=|PROMETHEUS_URL|LOKI_URL|METRICS_BACKEND|PGSSLMODE)' /etc/wem/wem.conf
```

## 7. WEM 1.3 서비스 기동

WEM 1.3은 `/etc/wem/wem.conf` 설정을 기준으로 서비스를 기동합니다.
첫 기동 시 필요한 setup은 `wem.service`에서 자동으로 수행됩니다.

```bash
sudo systemctl daemon-reload
sudo systemctl reset-failed wem
sudo systemctl enable --now wem
sudo systemctl status wem --no-pager
```

로그 확인:

```bash
sudo journalctl -u wem -n 200 --no-pager -l
sudo tail -n 300 /var/log/wem/wem.log
```

프로세스와 포트 확인:

```bash
MAINPID=$(systemctl show -p MainPID --value wem)
ps --ppid "$MAINPID" -o pid,cmd

ss -lntp | egrep '8080|8081|9091|9090|3100'
```

정상 기준:

```text
8080 : WEM UI
8081 : WEM internal job execution engine
9091 : WEM remote write receiver
```

remote write receiver 확인:

```bash
curl http://127.0.0.1:9091/-/healthy
```

정상 응답:

```text
WEM Remote Write Receiver is Healthy.
```

## 8. Prometheus alert rule 반영

WEM 1.3 RPM에 포함된 Prometheus alert rule을 복사합니다.

```bash
sudo mkdir -p /etc/wem/prometheus/alerts

sudo cp -v /usr/local/greenplum-db/wem/config/prometheus/alerts/*.rules.yml \
  /etc/wem/prometheus/alerts/ 2>/dev/null || true

sudo chown -R prometheus:prometheus /etc/wem/prometheus
```

Prometheus 설정에서 rule file을 활성화합니다.

```bash
sudo vi /etc/prometheus/prometheus.yml
```

예시:

```yaml
rule_files:
  - /etc/wem/prometheus/alerts/*.rules.yml
```

설정 검증 후 재시작합니다.

```bash
promtool check config /etc/prometheus/prometheus.yml
sudo systemctl restart prometheus
curl http://127.0.0.1:9090/-/healthy
```

## 9. Collector 1.4 RPM 업그레이드

WEM 1.3용 collector RPM을 WHPG cluster host 전체에 복사합니다.

```bash
for h in $WHPG_HOSTS; do
  echo "==== $h ===="
  scp /tmp/wem13-rpms/edb-whpg-observability-collector-1.4.0.rc.1-1.el9.x86_64.rpm gpadmin@$h:/tmp/
done
```

업그레이드합니다.

```bash
for h in $WHPG_HOSTS; do
  echo "==== $h ===="
  ssh gpadmin@$h "sudo dnf upgrade -y /tmp/edb-whpg-observability-collector-1.4.0.rc.1-1.el9.x86_64.rpm"
done
```

확인:

```bash
for h in $WHPG_HOSTS; do
  echo "==== $h ===="
  ssh gpadmin@$h "rpm -qa | grep edb-whpg-observability-collector"
done
```

## 10. Collector 설정 변경 및 재배포

collector metrics를 Prometheus 9090이 아니라 WEM remote write receiver 9091로 전송합니다.

`scdw-dr`에서 token을 확인합니다.

```bash
sudo grep '^WEM_REMOTE_WRITE_RECEIVER_TOKEN=' /etc/wem/wem.conf
```

WHPG coordinator에서 collector 설정을 수정합니다.

```bash
ssh gpadmin@cdw-dr
cd /var/lib/whpg-observability-collector
sudo cp -a collector.conf collector.conf.before-13-redeploy.$(date +%Y%m%d%H%M%S)
sudo vi collector.conf
```

예시:

```bash
WHPG_OBS_DSN="host=192.168.10.80 port=5432 dbname=postgres user=gpadmin password=<gpadmin_db_password> sslmode=disable"

LOKI_ENDPOINT="http://192.168.10.81:3100/loki/api/v1/push"
PROMETHEUS_ENDPOINT="http://192.168.10.81:9091/api/v1/write"
PROMETHEUS_BEARER_TOKEN="<WEM_REMOTE_WRITE_RECEIVER_TOKEN>"
```

재배포:

```bash
cd /var/lib/whpg-observability-collector
sudo chown -R gpadmin:gpadmin /var/lib/whpg-observability-collector
./deploy-observability
```

Alloy 상태 확인:

```bash
for h in $WHPG_HOSTS; do
  echo "==== $h ===="
  ssh gpadmin@$h "systemctl is-active alloy"
done
```

## 11. 업그레이드 후 기능 테스트

`scdw-dr`에서 서비스 상태를 확인합니다.

```bash
sudo systemctl is-active wem prometheus loki
curl http://127.0.0.1:9090/-/healthy
curl http://127.0.0.1:3100/ready
curl http://127.0.0.1:9091/-/healthy
ss -lntp | egrep '8080|8081|9091|9090|3100'
```

WEM UI:

```text
http://192.168.10.81:8080
```

확인 항목:

```text
기존 admin 계정 로그인 가능
Dashboard 정상 표시
Cluster 상태 표시
Query Monitoring 표시
System Metrics 표시
Storage Metrics 표시
Logs 조회
Canary Checks 표시
Prometheus Status 표시
```

Prometheus에서 metrics 수집을 확인합니다.

```bash
curl -s "http://127.0.0.1:9090/api/v1/label/__name__/values" | head
```

Prometheus UI:

```text
http://192.168.10.81:9090
```

확인 query:

```promql
up
node_cpu_seconds_total
node_filesystem_size_bytes
```

Alloy 로그:

```bash
for h in $WHPG_HOSTS; do
  echo "==== $h ===="
  ssh gpadmin@$h "sudo journalctl -u alloy -n 100 --no-pager"
done
```

## 12. Safeguard 추가 테스트

WEM 1.3에서 Safeguard까지 테스트하려면 업그레이드 검증 완료 후 진행합니다.

필요 RPM:

```text
edb-acp-host-agent-1.0.0_rc.2-1.el9.x86_64.rpm
edb-whpg-dr-1.0.0-1.el9.x86_64.rpm
```

host agent 설정 예시:

```bash
# Primary coordinator
WEM_CONNECT_ADDRESS=192.168.10.81:8081
WEM_TASK_QUEUES=coordinator-tasks
WEM_DATABASE=postgres

# DR coordinator
WEM_CONNECT_ADDRESS=192.168.10.90:8081
WEM_TASK_QUEUES=dr-standby-tasks
WEM_DATABASE=postgres
```

상세 절차는 `18-3. Setup WEM 1.3.md`의 Safeguard 구성 절차를 따릅니다.
