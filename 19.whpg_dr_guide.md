# WarehousePG DR 테스트 구성 가이드

이 문서는 Cluster 1의 WarehousePG를 Primary로 두고, Cluster 2를 DR 클러스터로 사용하기 위한 whpg-dr 구성 절차입니다.  
백업 저장소는 MinIO S3-compatible storage를 사용합니다.

공식 문서:

- WarehousePG Disaster Recovery: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/whpg-dr/
- DR 개념: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/whpg-dr/overview/concepts/
- Primary 설정: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/whpg-dr/setting-up-primary/
- DR 클러스터 설정: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/whpg-dr/configuring-dr/
- Warm DR: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/whpg-dr/performing-disaster-recovery/warm-dr/
- Cold DR: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/whpg-dr/performing-disaster-recovery/cold-dr/
- PITR: https://www.enterprisedb.com/docs/supported-open-source/warehousepg/whpg-dr/performing-disaster-recovery/pitr/

## 1. 구성 정보

### Cluster 1: Primary

| 역할 | Hostname | IP |
|---|---:|---:|
| Coordinator | cdw | 192.168.10.74 |
| Standby coordinator | scdw | 192.168.10.75 |
| Segment host | sdw1 | 192.168.10.76 |
| Segment host | sdw2 | 192.168.10.77 |
| Segment host | sdw3 | 192.168.10.78 |
| Segment host | sdw4 | 192.168.10.73 |

Primary segment 개수: 16

### Cluster 2: DR

| 역할 | Hostname | IP |
|---|---:|---:|
| DR coordinator | cdw 또는 cdw-dr | 192.168.10.83 |
| DR standby coordinator | scdw 또는 scdw-dr | 192.168.10.80 |
| DR segment host | sdw1 또는 sdw1-dr | 192.168.10.81 |
| DR segment host | sdw2 또는 sdw2-dr | 192.168.10.82 |

DR primary segment 개수: 16

Cluster 2는 Cluster 1과 segment host 대수가 같을 필요는 없습니다.  
중요한 것은 Cluster 1의 primary segment content 개수인 16개를 Cluster 2가 모두 담을 수 있어야 한다는 점입니다.

## 2. 작업 전 반드시 알아야 할 내용

whpg-dr restore는 DR 클러스터의 coordinator와 primary segment data directory에 백업 데이터를 복원합니다. 따라서 DR 대상 data directory는 비어 있어야 합니다.

whpg-dr가 복원하지 않는 항목:

- Standby coordinator
- Mirror segment

즉, scdw와 mirror는 DR promote 이후 필요하면 다시 구성합니다.

Cluster 2를 이미 gpinitsystem으로 초기화했다면 full restore 전에 Cluster 2를 stop하고 data directory를 정리해야 합니다.

또한 두 클러스터가 모두 cdw, sdw1, sdw2 같은 동일 hostname을 사용하면 작업 중 헷갈릴 수 있습니다. 가능하면 DR 쪽은 /etc/hosts에 별칭을 추가해서 cdw-dr, scdw-dr, sdw1-dr, sdw2-dr처럼 구분하는 것을 권장합니다.

DR host의 /etc/hosts 예시:

```text
192.168.10.83 cdw-dr
192.168.10.80 scdw-dr
192.168.10.81 sdw1-dr
192.168.10.82 sdw2-dr
```

이 문서에서는 DR hostname을 cdw-dr, scdw-dr, sdw1-dr, sdw2-dr로 표기합니다. 실제 환경에서 별칭을 쓰지 않는다면 명령어의 hostname을 현재 사용하는 이름으로 바꿔 실행하세요.


## 3. MinIO 준비

현재 MinIO 콘솔 정보:

```text
Console URL: http://192.168.10.190:9001/login
```

주의: 9001/login은 MinIO 웹 콘솔 주소입니다.  
whpg-dr 설정에 넣어야 하는 것은 S3 API endpoint입니다. 일반적으로 다음 주소를 사용합니다.

```text
http://192.168.10.190:9000
```

MinIO에서 bucket을 생성합니다.

```text
Bucket name: whpg-dr
```

테스트 환경에서는 아래 계정을 사용할 수 있습니다.

```text
Access key: minioadmin
Secret key: minioadmin123
```

운영 환경에서는 별도 access key를 만들고, 평문 파일에 secret을 저장하지 않는 방식을 검토해야 합니다.

## 4. Primary segment 개수 확인

Cluster 1 coordinator에서 gpadmin으로 실행합니다.

```bash
psql -d postgres -c "
select count(*) as primary_segment_count
from gp_segment_configuration
where content >= 0 and role = 'p';
"
```

예상 결과:

```text
16
```

상세 확인:

```bash
psql -d postgres -c "
select content, hostname, port, datadir, role, preferred_role, mode, status
from gp_segment_configuration
where content >= 0
order by content, role;
"
```

## 5. Cluster 1에 whpg-dr 설치

Cluster 1 coordinator에서 gpadmin으로 실행합니다.

작업 전 WarehousePG 환경변수를 설정합니다. 이 환경변수가 없으면 psql은 되더라도 gpconfig, gpstate, whpg-dr configure backup 같은 관리 명령이 실패할 수 있습니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh
export COORDINATOR_DATA_DIRECTORY=/data1/coordinator/gpseg-1
export MASTER_DATA_DIRECTORY=/data1/coordinator/gpseg-1
export PGPORT=5432
export PGDATABASE=postgres
```

매번 설정하지 않도록 ~/.bash_profile에도 추가합니다.

```bash
cat >> ~/.bash_profile <<'EOF'
source /usr/edb/whpg7/greenplum_path.sh
export COORDINATOR_DATA_DIRECTORY=/data1/coordinator/gpseg-1
export MASTER_DATA_DIRECTORY=/data1/coordinator/gpseg-1
export PGPORT=5432
export PGDATABASE=postgres
export PATH=$PATH:/usr/edb/whpg-dr/bin
EOF

source ~/.bash_profile
```

패키지 다운로드:

```bash
export EDB_SUBSCRIPTION_TOKEN=<your-token>
curl -1sSLf "https://downloads.enterprisedb.com/$EDB_SUBSCRIPTION_TOKEN/gpsupp/setup.rpm.sh" | sudo -E bash
sudo dnf download edb-whpg-dr
```

Cluster 1 host 목록 파일을 만듭니다.

```bash
cat > /home/gpadmin/all_hosts_cluster1 <<'EOF'
cdw
sdw1
sdw2
sdw3
sdw4
EOF
```

RPM 복사 및 설치:

```bash
gpsync -f /home/gpadmin/all_hosts_cluster1 edb-whpg-dr-<version>.el9.x86_64.rpm =:/tmp/
gpssh -f /home/gpadmin/all_hosts_cluster1 -u gpadmin -e "sudo dnf install -y /tmp/edb-whpg-dr-<version>.el9.x86_64.rpm"
```

PATH 추가:

```bash
echo 'export PATH=$PATH:/usr/edb/whpg-dr/bin' >> ~/.bash_profile
source ~/.bash_profile
whpg-dr --version
```

## 6. Cluster 1 백업 설정

Cluster 1 coordinator에 /home/gpadmin/backup.yaml 파일을 생성합니다.

```yaml
cluster_name: whpg_cluster1

storage:
  type: s3
  bucket: whpg-dr
  prefix: whpg_cluster1
  region: us-east-1
  endpoint_url: http://192.168.10.190:9000
  access_key_id: minioadmin
  secret_access_key: minioadmin123

barman_options:
  compression: zstd
  compression_level: medium
  retention_policy: RECOVERY WINDOW OF 4 WEEKS
 #minimum_redundancy: 1 #첫 백업전에는 주석처리
  jobs: 1
  max_archive_size: 10Gi
```

설명:

- cluster_name은 백업 저장소 안에서 사용하는 클러스터 이름입니다.
- DR 설정 파일에서는 이 값을 source_cluster_name으로 동일하게 사용해야 합니다.
- endpoint_url에는 MinIO 콘솔 주소가 아니라 S3 API 주소를 넣어야 합니다.
- S3 방식은 WarehousePG 7.x에서만 지원됩니다.
- 테스트 환경에서는 jobs: 1로 시작하는 것을 권장합니다. jobs를 높이면 segment별 병렬 업로드 worker가 늘어나 메모리를 많이 사용할 수 있습니다.

먼저 dry run으로 확인합니다.

```bash
whpg-dr configure backup /home/gpadmin/backup.yaml --dry-run
```

문제가 없으면 실제 적용합니다.

```bash
whpg-dr configure backup /home/gpadmin/backup.yaml
```

주의: whpg-dr configure backup은 내부적으로 gpstop -ar를 실행하여 WarehousePG 클러스터를 재시작합니다. 실행 전 client session을 정리하세요.

설정 확인:

```bash
whpg-dr check whpg_cluster1
```

## 7. 첫 base backup 수행

Cluster 1 coordinator에서 실행합니다.

```bash
whpg-dr backup whpg_cluster1


Step 1/3: Pre-backup checks...
Running barman checks before backup...
Barman checks passed

Step 2/3: Running barman backup...
Starting backup for 1 coordinator and 16 segments with cluster: whpg_cluster1 (running on segment hosts)
Running barman backup for server: whpg_cluster1-seg15 (content 15)
Running barman backup for server: whpg_cluster1-seg12 (content 12)
Running barman backup for server: whpg_cluster1-seg6 (content 6)
Running barman backup for server: whpg_cluster1-seg3 (content 3)
Running barman backup for server: whpg_cluster1-seg4 (content 4)
Running barman backup for server: whpg_cluster1-seg2 (content 2)
Running barman backup for server: whpg_cluster1-seg5 (content 5)
Running barman backup for server: whpg_cluster1-seg7 (content 7)
Running barman backup for server: whpg_cluster1-seg10 (content 10)
Running barman backup for server: whpg_cluster1-seg-1 (content -1)
Running barman backup for server: whpg_cluster1-seg8 (content 8)
Running barman backup for server: whpg_cluster1-seg1 (content 1)
Running barman backup for server: whpg_cluster1-seg0 (content 0)
Running barman backup for server: whpg_cluster1-seg13 (content 13)
Running barman backup for server: whpg_cluster1-seg14 (content 14)
Running barman backup for server: whpg_cluster1-seg9 (content 9)
Running barman backup for server: whpg_cluster1-seg11 (content 11)
^[All backups completed successfully (1 coordinator and 16 segments)

Step 3/3: Creating restore point...
Per-backup segment configuration saved: s3://whpg-dr/whpg_cluster1/whpg_cluster1/restore-points/20260727T165028_base_backup/segment_configuration.csv
Waiting for WAL archiving to complete for cluster whpg_cluster1...
Restore point created: 20260727-165233R_whpgdr_full_backup (3m 36s)
Restore point metadata: s3://whpg-dr/whpg_cluster1/whpg_cluster1/restore-points/20260727T165028_base_backup/20260727-165233R_whpgdr_full_backup

Step 3/3: Creating restore point...
Restore point created: 20260727-165233R_whpgdr_full_backup
이건 whpg-dr backup이 만든 자동 restore point
이 full backup이 완료된 시점까지 복구 가능하다는 표시입니다.
```

백업 목록 확인:

```bash
whpg-dr list-backup whpg_cluster1


Listing backups...
Listing restore points...
Full backup: 20260727T165028_base_backup
└── Restore Points:
    └── 20260727-165233R_whpgdr_full_backup: 2026-07-27 16:52:33
```

### 7.1 backup과 restore point의 관계

처음 하는 사람이 가장 헷갈리기 쉬운 부분입니다.

whpg-dr backup은 복구의 출발점이 되는 full base backup을 만듭니다.  
whpg-dr create-restore-point는 나중에 복구할 수 있는 도착 시점을 이름으로 표시합니다.  
base backup 이후 변경된 데이터는 WAL로 계속 저장되며, DR 복구 시에는 base backup에 WAL을 적용해서 restore point 시점까지 복구합니다.

개념은 아래처럼 보면 됩니다.

```text
base backup = 복구 출발점
WAL         = base backup 이후 변경 이력
restore point = 복구 도착점 표시
```

예시:

```text
14:00  whpg-dr backup whpg_cluster1 실행
14:30  데이터 INSERT/UPDATE 계속 발생
15:00  whpg-dr create-restore-point whpg_cluster1 --suffix initial_dr_test 실행
```

이 경우 DR 복구는 단순히 14:00 백업 시점으로만 복구하는 것이 아닙니다.  
14:00 base backup + 15:00까지의 WAL을 적용해서 15:00 restore point 시점으로 복구할 수 있습니다.

whpg-dr backup 명령도 full backup이 끝나면 자동으로 restore point를 하나 만듭니다. 예를 들어 아래 이름은 full backup 완료 시점의 자동 restore point입니다.

```text
20260727-165233R_whpgdr_full_backup
```

수동 restore point를 만들면 metadata 경로에 어느 full backup 아래에 만들어졌는지 표시됩니다.

```text
s3://whpg-dr/whpg_cluster1/whpg_cluster1/restore-points/20260727T165028_base_backup/20260727-170538R_initial_dr_test
```

위 예시에서는 20260727-170538R_initial_dr_test restore point가 20260727T165028_base_backup을 출발점으로 사용한다는 뜻입니다.

backup이 2번 실행된 경우도 원리는 같습니다.

```text
14:00  whpg-dr backup whpg_cluster1
15:00  데이터 변경 발생
16:00  whpg-dr backup whpg_cluster1
17:00  whpg-dr create-restore-point whpg_cluster1 --suffix initial_dr_test
```

이 경우 17:00 initial_dr_test restore point는 보통 가장 최근에 성공한 full backup인 16:00 backup을 출발점으로 삼습니다.  
반대로 14:00 backup과 16:00 backup 사이인 15:00 시점에 create-restore-point를 만들지 않았다면, initial_dr_test 같은 이름으로 15:00 지점을 선택할 수 없습니다.

따라서 DR 테스트에서 특정 업무 시점을 검증하려면 아래 순서를 권장합니다.

```bash
whpg-dr backup whpg_cluster1

# 테스트용 데이터 생성 또는 업무 작업 수행

whpg-dr create-restore-point whpg_cluster1 --suffix initial_dr_test

Waiting for WAL archiving to complete for cluster whpg_cluster1...
Restore point created: 20260727-170538R_initial_dr_test (54s)
Restore point metadata: s3://whpg-dr/whpg_cluster1/whpg_cluster1/restore-points/20260727T165028_base_backup/20260727-170538R_initial_dr_test

```

```bash
whpg-dr list-restore-point whpg_cluster1

Listing restore points...

20260727-170538R_initial_dr_test          July 27 2026, 17:05:38
20260727-165233R_whpgdr_full_backup       July 27 2026, 16:52:33
    └─ Base Backup: 20260727T165028_base_backup (Full, July 27 2026, 16:50:30)

Total: 2 restore points from 1 backups
```

수동 restore point 생성:

```bash
whpg-dr create-restore-point whpg_cluster1 --suffix initial_dr_test

Restore point created: 20260727-170538R_initial_dr_test (54s)
Restore point metadata: s3://whpg-dr/whpg_cluster1/whpg_cluster1/restore-points/20260727T165028_base_backup/20260727-170538R_initial_dr_test
```

restore point 목록 확인:

```bash
whpg-dr list-restore-point whpg_cluster1
```

## 8. Cluster 1 restore point 스케줄 등록

Warm DR에서는 restore point 생성 주기가 RPO에 영향을 줍니다.  
예를 들어 15분마다 restore point를 만들면 대략 RPO는 15분 수준입니다.

Cluster 1 coordinator에서 gpadmin으로 crontab -e를 실행하고 아래 내용을 추가합니다.

```cron
0,15,30,45 * * * * /usr/edb/whpg-dr/bin/whpg-dr create-restore-point whpg_cluster1 --suffix scheduled >> $HOME/gpAdminLogs/whpg-dr-crp.log 2>&1
```

백업 retention 처리를 위한 cron도 추가할 수 있습니다.

```cron
0 2 * * * /usr/edb/whpg-dr/bin/whpg-dr cron whpg_cluster1 >> $HOME/gpAdminLogs/whpg-dr-cron.log 2>&1
```

## 9. Cluster 2 restore 준비

Cluster 2 coordinator에서 gpadmin으로 실행합니다.

Cluster 2는 Cluster 1과 같은 OS, 같은 WarehousePG patch version이어야 합니다.  
Cluster 1에서 사용하는 extension package가 있다면 Cluster 2에도 미리 설치해야 합니다.

Cluster 2가 이미 기동 중이면 stop합니다.

```bash
source /usr/edb/whpg7/greenplum_path.sh
export COORDINATOR_DATA_DIRECTORY=/data/coordinator/gpseg-1
gpstop -af
```

full restore 전에 DR data directory를 비웁니다. 실제 data directory 경로가 다르면 환경에 맞게 수정하세요.

```bash
gpssh -h cdw-dr -h sdw1-dr -h sdw2-dr -e "rm -rf /data/coordinator/gpseg-1 /data/primary/gpseg*"
gpssh -h cdw-dr -h sdw1-dr -h sdw2-dr -e "sudo mkdir -p /data/coordinator /data/primary"
gpssh -h cdw-dr -h sdw1-dr -h sdw2-dr -e "sudo chown -R gpadmin:gpadmin /data/coordinator /data/primary"
```

## 10. Cluster 2에 whpg-dr 설치

Cluster 2 coordinator에서 실행합니다.

```bash
export EDB_SUBSCRIPTION_TOKEN=<your-token>
curl -1sSLf "https://downloads.enterprisedb.com/$EDB_SUBSCRIPTION_TOKEN/gpsupp/setup.rpm.sh" | sudo -E bash
sudo dnf download edb-whpg-dr
```

DR host 목록 파일 생성:

```bash
cat > /home/gpadmin/all_hosts_cluster2 <<'EOF'
cdw-dr
sdw1-dr
sdw2-dr
EOF
```

RPM 복사 및 설치:

```bash
gpsync -f /home/gpadmin/all_hosts_cluster2 edb-whpg-dr-<version>.el9.x86_64.rpm =:/tmp/
gpssh -f /home/gpadmin/all_hosts_cluster2 -u gpadmin -e "sudo dnf install -y /tmp/edb-whpg-dr-<version>.el9.x86_64.rpm"
```

PATH 추가:

```bash
echo 'export PATH=$PATH:/usr/edb/whpg-dr/bin' >> ~/.bash_profile
source ~/.bash_profile
whpg-dr --version
```

## 11. Cluster 2 restore 설정

Cluster 2 coordinator에 /home/gpadmin/restore.yaml 파일을 생성합니다.

```yaml
source_cluster_name: whpg_cluster1

storage:
  type: s3
  bucket: whpg-dr
  prefix: whpg_cluster1
  region: us-east-1
  endpoint_url: http://192.168.10.190:9000
  access_key_id: minioadmin
  secret_access_key: minioadmin123

coordinator_host: cdw-dr
coordinator_data_directory: /data/coordinator

segment_hosts:
  - sdw1-dr
  - sdw2-dr

data_directory: /data/primary
data_directory_prefix: gpseg

barman_options:
  compression: zstd
  compression_level: medium
  jobs: 1
  wal_prefetch: 1
```

Cluster 2는 segment host가 2대뿐이지만, source cluster의 primary segment 16개를 복원할 수 있습니다.  
whpg-dr configure restore가 백업 repository에 저장된 source topology를 읽고, 16개 segment content를 DR segment host 2대에 나누어 배치합니다.

dry run:

```bash
whpg-dr configure restore /home/gpadmin/restore.yaml --dry-run
```

실제 적용:

```bash
whpg-dr configure restore /home/gpadmin/restore.yaml
```

백업 확인:

```bash
whpg-dr list-backup whpg_cluster1
```

## 12. Warm DR 테스트

Warm DR은 Cluster 2를 Primary에 가깝게 계속 따라오게 만든 뒤, 장애 또는 테스트 시 Cluster 2를 promote하는 방식입니다.

### 12.1 최초 full restore

Cluster 2 coordinator에서 실행합니다.

```bash
whpg-dr restore whpg_cluster1 --target-name latest

Step 1/4: Resolving and verifying restore target...
Resolved latest restore point: 20260727-170538R_initial_dr_test (backup: 20260727T165028_base_backup)
Verifying topology compatibility between backup 20260727T165028_base_backup and restore configuration...
Checking if coordinator port 5432 is available on cdw...

WARNING: unresolved restore state already exists for this cluster.
Current unresolved goal: mode=full, backup=20260727T165028_base_backup, restore point=20260727-170538R_initial_dr_test, status=failed.
Starting a new full restore will supersede that unresolved goal.


Do you want to supersede the unresolved restore goal and continue? (y/n): y

Verifying backup 20260727T165028_base_backup integrity...
Finding backup...
Checked backup 20260727T165030 on server whpg_cluster1-seg9
Skipping inactive server 'whpg_cluster1-seg9'
Checked backup 20260727T165031 on server whpg_cluster1-seg-1
Skipping inactive server 'whpg_cluster1-seg-1'
Checked backup 20260727T165030 on server whpg_cluster1-seg5
Skipping inactive server 'whpg_cluster1-seg5'
Checked backup 20260727T165030 on server whpg_cluster1-seg3
Skipping inactive server 'whpg_cluster1-seg3'
Checked backup 20260727T165030 on server whpg_cluster1-seg7
Skipping inactive server 'whpg_cluster1-seg7'
Checked backup 20260727T165031 on server whpg_cluster1-seg11
Skipping inactive server 'whpg_cluster1-seg11'
Checked backup 20260727T165030 on server whpg_cluster1-seg1
Skipping inactive server 'whpg_cluster1-seg1'
Checked backup 20260727T165030 on server whpg_cluster1-seg13
Skipping inactive server 'whpg_cluster1-seg13'
Checked backup 20260727T165031 on server whpg_cluster1-seg10
Skipping inactive server 'whpg_cluster1-seg10'
Checked backup 20260727T165030 on server whpg_cluster1-seg14
Skipping inactive server 'whpg_cluster1-seg14'
Checked backup 20260727T165030 on server whpg_cluster1-seg2
Skipping inactive server 'whpg_cluster1-seg2'
Checked backup 20260727T165030 on server whpg_cluster1-seg12
Skipping inactive server 'whpg_cluster1-seg12'
Checked backup 20260727T165030 on server whpg_cluster1-seg0
Skipping inactive server 'whpg_cluster1-seg0'
Checked backup 20260727T165030 on server whpg_cluster1-seg4
Skipping inactive server 'whpg_cluster1-seg4'
Checked backup 20260727T165030 on server whpg_cluster1-seg6
Skipping inactive server 'whpg_cluster1-seg6'
Checked backup 20260727T165030 on server whpg_cluster1-seg8
Skipping inactive server 'whpg_cluster1-seg8'
Checked backup 20260727T165030 on server whpg_cluster1-seg15
Skipping inactive server 'whpg_cluster1-seg15'
All backup checks completed successfully

Step 2/4: Restoring base backup...
Restoring cluster whpg_cluster1 from backup 20260727T165028_base_backup
Using global config: /home/gpadmin/.whpg-dr/whpg_cluster1/restore/barman_conf/whpg_cluster1-global.conf
Found 1 coordinator and 16 segments to restore
Restoring segment 15 on sdw2...
Restoring segment 2 on sdw1...
Restoring segment 9 on sdw2...
Restoring segment 3 on sdw2...
Restoring coordinator on cdw...
Restoring segment 10 on sdw1...
Restoring segment 0 on sdw1...
Restoring segment 7 on sdw2...
Restoring segment 1 on sdw2...
Restoring segment 13 on sdw2...
Restoring segment 12 on sdw1...
Restoring segment 6 on sdw1...
Restoring segment 5 on sdw2...
Restoring segment 4 on sdw1...
Restoring segment 8 on sdw1...
Restoring segment 11 on sdw2...
Restoring segment 14 on sdw1...
coordinator restore completed successfully on cdw
segment 12 restore completed successfully on sdw1
segment 7 restore completed successfully on sdw2
segment 13 restore completed successfully on sdw2
segment 0 restore completed successfully on sdw1
segment 4 restore completed successfully on sdw1
segment 8 restore completed successfully on sdw1
segment 5 restore completed successfully on sdw2
segment 1 restore completed successfully on sdw2
segment 2 restore completed successfully on sdw1
segment 15 restore completed successfully on sdw2
segment 9 restore completed successfully on sdw2
segment 10 restore completed successfully on sdw1
segment 11 restore completed successfully on sdw2
segment 6 restore completed successfully on sdw1
segment 3 restore completed successfully on sdw2
segment 14 restore completed successfully on sdw1
All 1 coordinator and 16 segments restored successfully

Step 3/4: Replaying WAL forward...
Started segment 6 on host sdw1
Started segment 2 on host sdw1
Started segment 3 on host sdw2
Started segment 15 on host sdw2
Started segment 11 on host sdw2
Started segment 10 on host sdw1
Started segment 12 on host sdw1
Started coordinator on host cdw
Started segment 13 on host sdw2
Started segment 14 on host sdw1
Started segment 4 on host sdw1
Started segment 8 on host sdw1
Started segment 5 on host sdw2
Started segment 9 on host sdw2
Started segment 7 on host sdw2
Started segment 0 on host sdw1
Started segment 1 on host sdw2
Waiting for all segments to replay WAL and shut down...
Segments still replaying WAL... (checked 6 times)
Segments still replaying WAL... (checked 12 times)
Segments still replaying WAL... (checked 18 times)
Segments still replaying WAL... (checked 24 times)
Segments still replaying WAL... (checked 30 times)
Segments still replaying WAL... (checked 36 times)
Segments still replaying WAL... (checked 42 times)
All segments have completed WAL replay and shut down
WAL replay completed successfully, all segments are shut down

Step 4/4: Committing restore state...
Restore state committed: 20260727-170538R_initial_dr_test (backup: 20260727T165028_base_backup) (4m 49s)
```

restore 상태 확인:

```bash
whpg-dr list-restore whpg_cluster1

Latest Completed Restore
------------------------
Restore point:  20260727-170538R_initial_dr_test
Backup name:    20260727T165028_base_backup
Restore time:   2026-07-28 02:20:15

Recovery Cluster Segment Status
-------------------------------
Content ID   Status                 Replay End LSN  Host  Path
----------   ------                 --------------  ----  ----
coordinator  shut down in recovery  0/64000148      cdw   /data/coordinator/gpseg-1
segment 0    shut down in recovery  0/48000120      sdw1  /data/primary/gpseg0
segment 1    shut down in recovery  0/44000120      sdw2  /data/primary/gpseg1
segment 2    shut down in recovery  0/50000120      sdw1  /data/primary/gpseg2
segment 3    shut down in recovery  0/50000120      sdw2  /data/primary/gpseg3
segment 4    shut down in recovery  0/44000120      sdw1  /data/primary/gpseg4
segment 5    shut down in recovery  0/44000120      sdw2  /data/primary/gpseg5
segment 6    shut down in recovery  0/44000120      sdw1  /data/primary/gpseg6
segment 7    shut down in recovery  0/40000120      sdw2  /data/primary/gpseg7
segment 8    shut down in recovery  0/44000120      sdw1  /data/primary/gpseg8
segment 9    shut down in recovery  0/48000120      sdw2  /data/primary/gpseg9
segment 10   shut down in recovery  0/40000120      sdw1  /data/primary/gpseg10
segment 11   shut down in recovery  0/48000120      sdw2  /data/primary/gpseg11
segment 12   shut down in recovery  0/44000120      sdw1  /data/primary/gpseg12
segment 13   shut down in recovery  0/44000120      sdw2  /data/primary/gpseg13
segment 14   shut down in recovery  0/40000120      sdw1  /data/primary/gpseg14
segment 15   shut down in recovery  0/48000120      sdw2  /data/primary/gpseg15
```

이 상태의 DR 클러스터는 recovery mode이며 client connection을 받지 않습니다.

### 12.2 delta restore 스케줄 등록

12.2는 Warm DR을 계속 유지할 때만 등록합니다.  
단순히 "복구가 되는지 한 번 테스트하고 바로 promote"할 목적이면 12.2 cron 등록은 건너뛰고 [13. DR 전환 테스트](#13-dr-전환-테스트)로 진행합니다.

12.1의 최초 full restore가 끝난 시점의 Cluster 2는 아래 상태입니다.

```text
Cluster 2 = 특정 restore point까지 복구 완료
상태 = shut down in recovery
client 접속 = 불가
promote = 아직 안 함
```

이 상태로 시간이 지나면 Cluster 1에서는 계속 데이터가 변경됩니다.  
Cluster 2가 그 변경분을 계속 따라오게 하려면 두 가지 작업이 주기적으로 필요합니다.

```text
Cluster 1: 현재 복구 가능한 시점을 restore point로 계속 찍음
Cluster 2: 새 restore point까지 delta restore로 계속 따라감
```

delta restore는 full restore를 처음부터 다시 하는 것이 아니라, 이전 restore 이후 새로 생긴 WAL/변경분만 적용해서 Cluster 2를 최신 restore point까지 끌어올리는 작업입니다.

예시 흐름:

```text
10:00 Cluster 2 최초 full restore 완료
10:15 Cluster 1 restore point 생성
10:20 Cluster 2 delta restore 실행
10:30 Cluster 1 restore point 생성
10:35 Cluster 2 delta restore 실행
```

따라서 12.2의 crontab은 DR 서버를 최신 상태에 가깝게 계속 대기시키기 위한 자동화입니다. 이 cron을 걸지 않으면 Cluster 2는 12.1에서 복구한 restore point에 멈춰 있습니다.

Cluster 2 coordinator에서 gpadmin으로 crontab -e를 실행하고 아래 내용을 추가합니다.

```cron
5,20,35,50 * * * * source /usr/edb/whpg7/greenplum_path.sh && /usr/edb/whpg-dr/bin/whpg-dr restore whpg_cluster1 --target-name latest --delta >> $HOME/gpAdminLogs/whpg-dr-restore.log 2>&1
```

이 예시는 Cluster 1에서 0,15,30,45분에 restore point를 만들고, Cluster 2에서 5분 뒤인 5,20,35,50분에 delta restore를 실행하는 구조입니다.

restore point 생성이 5분보다 오래 걸리면 Cluster 2 cron offset을 더 크게 잡아야 합니다.

### 12.3 동기화 상태 확인

Cluster 1 coordinator:

```bash
whpg-dr list-backup whpg_cluster1
```

Cluster 2 coordinator:

```bash
whpg-dr list-restore whpg_cluster1
```

Cluster 2의 latest completed restore point가 Cluster 1의 최신 restore point와 같아지면 Warm DR이 정상적으로 따라오고 있는 상태입니다.

## 13. DR 전환 테스트

DR 전환 테스트 순서:

1. Cluster 2의 delta restore cron을 중지합니다.
2. Cluster 1이 살아 있다면 Cluster 1의 restore point 생성 cron도 중지합니다.
3. Cluster 2에서 마지막 delta restore를 한 번 실행합니다.
4. Cluster 2를 promote합니다.
5. Cluster 2를 기동합니다.
6. 각 database에 대해 reindex/analyze를 수행합니다.
7. 필요하면 standby coordinator와 mirror를 새로 구성합니다.

Cluster 2 coordinator에서 실행:

```bash
whpg-dr restore whpg_cluster1 --target-name latest --delta
whpg-dr promote whpg_cluster1
```

promote 후 Cluster 2 기동:

```bash
source /usr/edb/whpg7/greenplum_path.sh
export COORDINATOR_DATA_DIRECTORY=/data/coordinator/gpseg-1
gpstart -a
```

상태 확인:

```bash
gpstate
psql -d postgres -c "select version();"
```

각 database별로 system index 재구성 및 통계 수집:

```bash
reindexdb --system -d <database_name> -e
analyzedb -as pg_catalog -d <database_name> -p 10 -v
analyzedb -d <database_name> -p 10 -v
```

여기까지 완료되면 Cluster 2는 writable primary로 동작합니다.

## 14. promote 후 standby coordinator 구성

whpg-dr는 standby coordinator를 복원하지 않습니다.  
Cluster 2 promote와 gpstart 완료 후 scdw-dr를 standby coordinator로 새로 붙입니다.

복구 전에 Cluster 2에 standby coordinator가 이미 구성되어 있었더라도 그대로 재사용하지 않습니다.  
whpg-dr restore로 active coordinator의 data directory가 Primary 백업 기준으로 바뀌기 때문에, 기존 standby coordinator는 새 active coordinator와 맞지 않는 오래된 standby일 수 있습니다.

따라서 promote와 gpstart 완료 후에는 기존 standby를 제거하고 다시 구성하는 것을 권장합니다.

기존 standby 상태 확인:

```bash
gpstate -f
```

기존 standby가 잡혀 있으면 Cluster 2 active coordinator에서 제거합니다.

```bash
gpinitstandby -r
```

주의: 아래 data directory 삭제는 Cluster 2의 scdw-dr에서만 수행합니다. Cluster 1의 scdw에서 실행하면 안 됩니다.

```bash
gpssh -h scdw-dr -e "rm -rf /data/coordinator/gpseg-1"
```

standby coordinator data directory 준비:

```bash
gpssh -h scdw-dr -e "sudo mkdir -p /data/coordinator && sudo chown -R gpadmin:gpadmin /data/coordinator"
```

Cluster 2 active coordinator에서 실행:

```bash
gpinitstandby -s scdw-dr
gpstate -f
```

## 15. Cold DR 테스트

Cold DR은 장애 또는 테스트 시점에 DR restore를 수행하는 방식입니다.  
Warm DR보다 비용은 낮지만, full restore가 전환 시간에 포함되므로 RTO가 더 깁니다.

최소 절차:

```bash
whpg-dr configure restore /home/gpadmin/restore.yaml
whpg-dr list-backup whpg_cluster1
whpg-dr restore whpg_cluster1 --target-name latest
whpg-dr promote whpg_cluster1
source /usr/edb/whpg7/greenplum_path.sh
export COORDINATOR_DATA_DIRECTORY=/data/coordinator/gpseg-1
gpstart -a
```

## 16. PITR 테스트

PITR은 특정 restore point 시점으로 복구할 때 사용합니다.  
예를 들어 잘못된 ETL, 잘못된 delete/update, 특정 과거 데이터 확인 등에 사용합니다.

restore point 목록 확인:

```bash
whpg-dr list-restore-point whpg_cluster1
```

특정 restore point로 복원:

```bash
whpg-dr restore whpg_cluster1 --target-name <restore_point_name>
whpg-dr promote whpg_cluster1
source /usr/edb/whpg7/greenplum_path.sh
export COORDINATOR_DATA_DIRECTORY=/data/coordinator/gpseg-1
gpstart -a
```

예시:

```bash
whpg-dr restore whpg_cluster1 --target-name 20260727-150000R_before_test
```

## 17. 장애 처리 및 확인 명령어

백업 설정 확인:

```bash
whpg-dr check whpg_cluster1 --log-file-level debug
```

백업 무결성 확인:

```bash
whpg-dr check-backup whpg_cluster1 --backup-name <backup_name>
```

설정된 클러스터 목록:

```bash
whpg-dr list-cluster
```

백업 및 restore point 목록:

```bash
whpg-dr list-backup whpg_cluster1
whpg-dr list-restore-point whpg_cluster1
```

DR restore 상태:

```bash
whpg-dr list-restore whpg_cluster1
```

중단된 restore 재개:

```bash
whpg-dr restore whpg_cluster1 --resume-failed
```

create-restore-point가 중단되어 lock이 남은 경우:

```bash
whpg-dr clean-lock-file whpg_cluster1
```

단, clean-lock-file은 다른 whpg-dr 명령이 실행 중이 아닌 것을 확인한 뒤에만 실행합니다.

## 18. 최종 체크리스트

DR 테스트 전 아래 항목을 확인합니다.

- Cluster 1과 Cluster 2의 OS가 동일하다.
- Cluster 1과 Cluster 2의 WarehousePG patch version이 동일하다.
- Cluster 1 primary segment 개수가 16이다.
- Cluster 2가 16개의 primary segment content를 담을 수 있다.
- MinIO bucket whpg-dr가 존재한다.
- MinIO S3 API endpoint가 확인되었다. 보통 http://192.168.10.190:9000이다.
- Cluster 1, Cluster 2의 모든 coordinator/segment host에서 MinIO S3 API endpoint에 접근 가능하다.
- edb-whpg-dr가 필요한 host에 설치되어 있다.
- Cluster 1 backup.yaml의 cluster_name은 whpg_cluster1이다.
- Cluster 2 restore.yaml의 source_cluster_name도 whpg_cluster1이다.
- 최소 1회 whpg-dr backup이 완료되었다.
- 최소 1개 restore point가 존재한다.
- full restore 전 DR data directory가 비어 있다.
- standby coordinator와 mirror는 whpg-dr restore 대상이 아니며, promote 후 새로 구성한다.
