# WarehousePG Tiered Partition 구성 가이드 (PGAA,S3 오프로드 및 재연결)

WarehousePG 자체에는 "Tiered Partition"이라는 완전 자동화 기능은 없습니다. <br>
자동 티어링(`pgaa.convert_to_tiered_table()`)은 **PGD(EDB Postgres Distributed) + PGAA** 조합에서만 지원되는 기능이고, <br>
WarehousePG 단독 환경에서는 **선언적 파티셔닝의 EXCHANGE 혹은 DETACH/ATTACH**와 <br>
**PGAA(Postgres Analytics Accelerator)의 CTAS/스토리지 로케이션**을 조합해 수동으로 구성해야 합니다. <br>
아래는 실제 동작하는 예시입니다 (WarehousePG 7.4+, PGAA v 1.11.0 이상 필요).

## 0. 사전 준비 — PGAA 확장 및 S3 스토리지 로케이션

```sql
CREATE EXTENSION IF NOT EXISTS pgaa CASCADE;

-- S3 버킷을 가리키는 storage location 등록 (자격증명 필요)
SELECT pgfs.create_storage_location(
  'sales_cold_tier',
  's3://my-warehouse-archive/sales',
  '{"aws_region": "us-east-1", "aws_access_key_id": "...", "aws_secret_access_key": "..."}'
);

-- Minio 버킷에 대한 연결 정보 생성   
SELECT pgfs.create_storage_location(
    'my_minio_storage',
    's3://pgaa', 
    '{"endpoint": "http://10.0.8.192:9000", 
       "allow_http": "true"}',
    '{"access_key_id": "minioadmin",
      "secret_access_key": "minioadmin"}'        
);

-- S3 연결 정보 확인
SELECT * FROM pgfs.list_storage_locations();
SELECT * FROM pgfs.get_storage_location('my_minio_storage');

-- S3 연결 삭제
SELECT pgfs.delete_storage_location('my_minio_storage');

-- S3 연결 기본 연결 설정.
SELECT * FROM pgfs.get_default_storage_location();
SELECT pgfs.set_default_storage_location('my_storage');

-- S3 연결 설정 수정.
SELECT pgfs.update_storage_location('my_minio_storage', 's3://my_bucket', '123e4567-e89b-12d3-a456-426614174000', '{}', '{}');

-- 쓰기 권한 확인 (NULL 반환 시 정상)
SELECT pgaa.test_storage_location('my_minio_storage', true);

-- 사용 엔진 확인.
show pgaa.executor_engine;
```

## 1. 파티션 테이블 생성 (일반 range partition)

```sql
-- Parent partitioned table
-- No NOT NULL / CHECK constraints → required for EXCHANGE PARTITION with ext table
DROP TABLE public.events_pgaa CASCADE;
CREATE TABLE public.events_pgaa (
    event_id    BIGINT,
    tenant_id   INT,
    event_type  VARCHAR(64),
    payload     TEXT,
    region      TEXT,
    amount      NUMERIC(12, 2),
    created_at  TIMESTAMP
)
WITH (
  appendoptimized = true,
  orientation = column,
  compresstype = zstd,
  compresslevel = 5
)
DISTRIBUTED BY (tenant_id)
PARTITION BY RANGE (created_at)
(
    PARTITION p202506
        START (TIMESTAMP '2025-06-01') INCLUSIVE
        END   (TIMESTAMP '2025-07-01') EXCLUSIVE,
    PARTITION p202505
        START (TIMESTAMP '2025-05-01') INCLUSIVE
        END   (TIMESTAMP '2025-06-01') EXCLUSIVE,
    PARTITION p202504
        START (TIMESTAMP '2025-04-01') INCLUSIVE
        END   (TIMESTAMP '2025-05-01') EXCLUSIVE,
    PARTITION p202503
        START (TIMESTAMP '2025-03-01') INCLUSIVE
        END   (TIMESTAMP '2025-04-01') EXCLUSIVE,
    PARTITION p202502
        START (TIMESTAMP '2025-02-01') INCLUSIVE
        END   (TIMESTAMP '2025-03-01') EXCLUSIVE,
    PARTITION p202501
        START (TIMESTAMP '2025-01-01') INCLUSIVE
        END   (TIMESTAMP '2025-02-01') EXCLUSIVE
);
-- ... 이후 월별로 계속 생성
```

###  Insert sample rows into all partitions

```sql
-- 1000,000 rows spread across Jan–Jun 2025
INSERT INTO events_pgaa (event_id, tenant_id, event_type, payload,
                    region, amount, created_at)
SELECT
    gs                                                             AS event_id,
    (gs % 5) + 1                                                   AS tenant_id,
    (ARRAY['purchase','view','click','signup','refund'])[(gs%5)+1]  AS event_type,
    '{"source":"lab","v":' || gs || '}'                            AS payload,
    (ARRAY['us-east','us-west','eu-west','ap-seoul','ap-tokyo'])[(gs%5)+1]
                                                                   AS region,
    ROUND((RANDOM() * 500 + 10)::NUMERIC, 2)                      AS amount,
    TIMESTAMP '2025-01-01'
        + ((gs - 1) % 181) * INTERVAL '1 day'
        + (gs % 86400)     * INTERVAL '1 second'                  AS created_at
FROM generate_series(1, 1000000) gs;
```

```sql
-- List all partitions with their boundary and storage type
SELECT
    pt.relid::regclass                          AS partition_table,
    pt.level,
    pt.isleaf,
    pg_get_expr(c.relpartbound, c.oid)          AS partition_bound,
    CASE c.relkind
        WHEN 'r' THEN 'heap (local)'
        WHEN 'f' THEN 'external (PXF)'
    END                                         AS storage_type
FROM   pg_partition_tree('events_pgaa') pt
JOIN   pg_class c ON c.oid = pt.relid
WHERE  pt.isleaf = true
ORDER  BY partition_bound;
```

```sql
-- Direct count per partition child table
SELECT 'p202501' AS part, COUNT(*) FROM events_pgaa_1_prt_p202501
UNION ALL
SELECT 'p202502',          COUNT(*) FROM events_pgaa_1_prt_p202502
UNION ALL
SELECT 'p202503',          COUNT(*) FROM events_pgaa_1_prt_p202503
UNION ALL
SELECT 'p202504',          COUNT(*) FROM events_pgaa_1_prt_p202504
UNION ALL
SELECT 'p202505',          COUNT(*) FROM events_pgaa_1_prt_p202505
UNION ALL
SELECT 'p202506',          COUNT(*) FROM events_pgaa_1_prt_p202506;
```

## 2. 오래된 파티션을 S3로 내보내고 (Cold tier로 이동) , 파티션 교체 및 정리.

먼저 CTAS로 write partition table을 생성하면, S3로 데이터가 이동 됩니다. <br>
그 다음. read partition table를 기존 partition table과 동일한 스키마/분산키로 생성  <br>
마지막으로. 기존 partition 과 exchange <br>
그리고, 필요없는 partition 테이블 삭제하면 됨니다.  <br>

-- ① PGAA로 S3에 export (CTAS는 write-once, 원자적 동작)
```sql
DROP TABLE IF EXISTS public.events_pgaa_1_prt_p202501_write;
CREATE TABLE public.events_pgaa_1_prt_p202501_write
USING PGAA
WITH (
    pgaa.storage_location = 'my_minio_storage',
    pgaa.path = 'events/2025/01',
    pgaa.format = 'iceberg',
    pgaa.purge_data_if_exists = true
)
-- DISTRIBUTED BY (tenant_id)
AS
SELECT * FROM public.events_pgaa_1_prt_p202501;
```

-- pgaa write table 확인 후, 테이블 삭제. 삭제해도 데이타는 Object Storage에 그대로 있음.
```sql
SELECT count(*) FROM public.events_pgaa_1_prt_p202501_write; -- row count 확인
DROP TABLE IF EXISTS public.events_pgaa_1_prt_p202501_write;
```

-- ② pgaa read table 생성., 기존 pgaa write table로 만들어진 데이터로.
```sql
DROP TABLE IF EXISTS public.events_pgaa_1_prt_p202501_read;
CREATE TABLE public.events_pgaa_1_prt_p202501_read( LIKE events_pgaa_1_prt_p202501 )
USING PGAA
WITH (
    pgaa.storage_location = 'my_minio_storage',
    pgaa.path = 'events/2025/01',
    pgaa.format = 'iceberg'
)
DISTRIBUTED BY (tenant_id);

SELECT count(*) FROM public.events_pgaa_1_prt_p202501_read; -- row count 확인
```

-- ③ 단일 레벨 파티션: ALTER PARTITION 없이 바로 EXCHANGE PARTITION , 로컬 파티션 디스크 회수 (검증 후 드롭)
```sql
ALTER TABLE events_pgaa
    EXCHANGE PARTITION p202501
    WITH TABLE public.events_pgaa_1_prt_p202501_read;
```

-- 혹은 DETACH ->  ATTACH ->  RENAME <br>
-- a) DETACH 
```sql
ALTER TABLE events_pgaa
DETACH PARTITION public.events_pgaa_1_prt_p202501;
```
-- b) ATTACH 
```sql
ALTER TABLE events_pgaa
  ATTACH PARTITION public.events_pgaa_1_prt_p202501_read
  FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');
```
-- c) RENAME
```sql
ALTER TABLE events_pgaa_1_prt_p202501_read
  RENAME TO events_pgaa_1_prt_p202501;
```
-- d) 로컬 파티션 디스크 회수 (검증 후 드롭)
```sql
DROP TABLE public.events_pgaa_1_prt_p202501;
```

## 에러 처리.
아래 에러시. <br>
ERROR:  duplicate key value violates unique constraint "table_mapping_pkey"  (seg1 10.0.1.224:6001 pid=2916185) <br>
DETAIL:  Key (rel_namespace, rel_name)=(public, events_pgaa_1_prt_p202501_read) already exists. <br>

```sql
-- 확인
SELECT * FROM pgaa.table_mapping
WHERE rel_namespace = 'public' AND rel_name = 'events_pgaa_1_prt_p202501_read';

-- 삭제 (REPLICATED 테이블이므로 코디네이터에서 실행하면 전 세그먼트에 반영됨)
DELETE FROM pgaa.table_mapping
WHERE rel_namespace = 'public' AND rel_name = 'events_pgaa_1_prt_p202501_read';
```
## 참고 자료

- [Partitioning Large Tables (WarehousePG)](https://www.enterprisedb.com/docs/warehousepg/latest/admin_guide/ddl/ddl-partition)
- [Scaling with WarehousePG (PGAA)](https://www.enterprisedb.com/docs/pgaa/latest/whpg/)
- [Reading from and writing to object storage on WarehousePG](https://www.enterprisedb.com/docs/pgaa/latest/whpg/reading/)
- [Writing to object storage (CTAS)](https://www.enterprisedb.com/docs/pgaa/latest/ctas/)
- [Using tiered storage (PGD)](https://www.enterprisedb.com/docs/pgd/latest/tiered-storage/)
- [Implementing tiered tables (PGD)](https://www.enterprisedb.com/docs/pgd/latest/tiered-storage/tiered-tables/)
- [Offloading to analytics (PGD)](https://www.enterprisedb.com/docs/pgd/latest/tiered-storage/offload/)
- [PGAA known issues](https://www.enterprisedb.com/docs/pgaa/latest/overview/known_issues/)
